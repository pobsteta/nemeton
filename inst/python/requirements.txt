# FORDEAD pipeline (spec 008, ADR-013)
# Pinned per plan.md §1.3. Installed lazily by .ensure_fordead_python()
# into ~/.virtualenvs/nemeton-fordead/.
#
# IMPORTANT: fordead is NOT published on PyPI. The official install
# method (per the INRAE docs at https://fordead.gitlab.io/fordead_package/)
# is `pip install git+https://gitlab.com/fordead/fordead_package`.
# We pin to a specific tag here for reproducibility.
#
# Pinned to v1.11.4 (last 1.x release, 2025-08-13) on purpose: fordead
# 2.x replaced the `fordead.steps.step1_*..step5_*` module layout with
# a single `fordead.workflow.FordeadProcess` class, which our R-side
# pipeline (R/fordead_pipeline.R) does NOT yet support. Migrating to
# 2.x is tracked as future work (spec 008 / ADR-013 amendment).
fordead @ git+https://gitlab.com/fordead/fordead_package@v1.11.4
xarray>=2024.0
dask[complete]>=2024.0
rasterio>=1.3.0
eodag>=3.0
numpy>=1.26
pandas>=2.0
geopandas>=0.14
shapely>=2.0
