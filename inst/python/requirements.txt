# FORDEAD pipeline (spec 008, ADR-013)
# Pinned per plan.md §1.3. Installed lazily by .ensure_fordead_python()
# into ~/.virtualenvs/nemeton-fordead/.
#
# IMPORTANT: fordead is NOT published on PyPI. The official install
# method (per the INRAE docs at https://fordead.gitlab.io/fordead_package/)
# is `pip install git+https://gitlab.com/fordead/fordead_package`.
# We pin to a specific tag here for reproducibility.
#
# Pinned to v2.1.1 — fordead 2.x accepts a simplestac.ItemCollection
# directly via `fordead.workflow.FordeadProcess(collection, output_dir,
# bbox, geometry, config=FordeadConfig())`. Matches our STAC COG cache
# format produced by `ingest_sentinel2_timeseries()`. Migration tracked
# in spec 008 §12 + plan 008 §9 + ADR-013 amendement A1 (v0.23.0).
fordead @ git+https://gitlab.com/fordead/fordead_package@v2.1.1
# simplestac is a transitive dep of fordead 2.x but we pin it explicitly
# for reproducibility (it lives on the INRAE forge, not PyPI).
simplestac @ git+https://forge.inrae.fr/umr-tetis/stac/simplestac@v1.2.5
xarray>=2024.0
dask[complete]>=2024.0
rasterio>=1.3.0
eodag>=3.0
numpy>=1.26
pandas>=2.0
geopandas>=0.14
shapely>=2.0
