# PLAN — Walking Skeleton & chantier en cours

**Source unique de vérité** pour la séquence des épaississements (E1, E2, …) et leur état d'avancement. CLAUDE.md ne duplique plus cette table (règle introduite le 2026-04-25). À chaque release, mettre à jour la table ci-dessous + le journal du chantier en cours (cf. *Consignes de release* étape 8 dans CLAUDE.md).

## Avancement Walking Skeleton

| État | Vague | Description | Livré dans |
|------|-------|-------------|------------|
| ✅ | Squelette initial | CSV/cadastre → indicateurs → radar → perspective IA | — |
| ✅ | E1 | 12 familles complètes, 31 indicateurs | `nemeton` |
| ✅ | E2 | Cartographie (Leaflet, parcelles cadastrales) | `nemetonshiny` |
| ✅ | E3 | Multi-acteurs — 13 profils experts YAML | `nemetonshiny` (commit `1b32943`) |
| ✅ | E4 | Authentification OAuth2/OIDC via shinyOAuth | `nemetonshiny` (commit `3e07c60`) |
| ✅ | E5 | Intégrations & NDP — Open-Canopy CHM (spec 005) + QField export/ingest + sizing échantillon + flag `height_lidar` | `nemeton` v0.16.0 → v0.19.12 + `nemetonshiny` (clôture `0a1eb63` le 2026-04-24) |
| ✅ | **E6** | **Suivi sanitaire** — surveillance rapide (NDVI/NBR rolling-window) **+ diagnostic FORDEAD** (CRSWIR + harmonique). Spec 008, ADR-013. Indicateur **R5 dépérissement**. | E6.a livré `nemeton@v0.20.0` + hardening `v0.20.1` ; E6.b phase 1 livré `nemetonshiny@2909b8f`, phases 4-5 livrées dans `nemetonshiny@v0.21.0` (mode sanitaire UI, carte alertes, persistence métadonnées) ; E6.c.1/.2/.3/.4 + E6.c.5 + E6.d livrés (cœur + app) ; clos par release `nemetonshiny@v0.21.0` (tag rétroactif sur `05cd425` le 2026-05-06). Reliquat backlog : E6.b phase 2 (ingestion S2 async wiring `mod_monitoring`), phase 3 (plotly NDVI/NBR per-plot), phase 6 (smoke shinytest2 E2E) |
| ✅ | **Plan d'actions** *(hors-skeleton, inséré entre E6 et E7)* | Onglet de planification d'actions sylvicoles avec carte, table DT, Kanban drag-and-drop, génération IA, exports GeoPackage + PDF Quarto par UGF. Profil expert `planificateur.yml`. Permissions role-based (Lot 6 S15). | `nemetonshiny@v0.22.0` (2026-05-06, ~2500 LOC `mod_action_plan` + `service_action_plan`, 12 PR mergées) ; polish v0.22.1 (DB auto-migrations + empty plan fix) ; v0.22.2 (Kanban drag-drop, table polish) ; v0.22.3 (sidebar 350 px, regex search OR, Surface totale, pagination 5 lignes, **role-based permissions #40, NEMETON_AUTH_DEV_ROLES #41**) ; v0.22.4 (auth fix démarrage anonyme, lengthMenu sous le tableau, freeze strict UGF+Année, légendes leaflet i18nisées) ; **v0.23.0** (Kanban libre — drag-drop sans DAG, double-clic → modal édition, tri par année, commentaire sur cartes, total tableau à droite, modal "Ajouter une action" : UGF labels + années calendaires) ; **v0.23.1** (chat IA en sidebar gauche persistante, fix sync carte→tableau) ; **v0.23.2** (chat repositionné sous "Tableau des actions" dans la sidebar droite, auto-scroll de l'historique, libellé bouton "Générer les actions (IA)") ; **v0.23.3** (toast "L'IA réfléchit…" avec roue dentée tournante pendant l'appel LLM) ; **v0.23.4** (rôles chat i18nisés : "Vous" / "Assistant" en FR) ; **v0.23.5** (chat IA : radio scope all/selected + checkbox overwrite ; fix bascule langue FR↔EN avec session$reload()). Pas de modif cœur. |
| ⬜ | E7 | RAG perspectives IA (pgvector + base de connaissances forestière, ADR-012) | non démarré — image `timescaledb-ha:pg16` embarque déjà pgvector (cf. journal 2026-05-05). Spec 009 à rédiger. |

Légende : ✅ livré · 🟨 en cours · ⬜ à venir.

---

# Chantier en cours — Stabilisation post-v0.23.5 / cadrage E7

**État au 2026-05-12** : pas de chantier d'épaississement actif. Working tree propre des deux côtés.

- `nemeton@main = f3050a2` (cycle dev `0.20.1.9009`) puis release `v0.21.0` (backend DuckDB local, commit `e037df5`) — patch `v0.21.1` le 2026-05-12 sur la branche `claude/fix-duckdb-sql-error-cpXZ7` : migration `0001_init.sql` corrigée (DuckDB rejette `GENERATED ALWAYS AS IDENTITY` et `ON DELETE CASCADE` en parsing — bascule sur `CREATE SEQUENCE` + `nextval()` et suppression des actions en cascade). Débloque le premier lancement de `nemetonshiny` en mode DuckDB local. Smoke-test contre DuckDB 1.5.2 : 5 tables créées + auto-id + FK encore enforced + ré-exécution idempotente.
  - **2026-05-13** — doc-only follow-up sur v0.21.4 : ajout d'une section « Priming the cache on an existing zone » dans le roxygen de `cache_dir` + bloc `@examples` (wiring `nemetonshiny`-style avec `cache <- file.path(project_path, "cache", "layers", "sentinel2")` et le run d'amorçage `skip_cached = FALSE`). Clarifie le piège d'interaction des deux couches de cache (`skip_cached` sort avant `.extract_scene_obs()`, donc le COG n'est jamais écrit tant qu'on n'a pas un cache-miss DB). Pas de bump (doc pure).
  - **2026-05-13** — `v0.21.5` (même branche) : retry sur erreurs HTTP transitoires côté STAC. `stac_search_s2_cdse()`, `stac_search_s2_pc()`, `.pc_collection_token()` et `.pc_sign_url()` enchaînent maintenant `.with_stac_retry()` (helper interne) qui retente jusqu'à 4 fois sur statuts 429/500/502/503/504 (backoff exponentiel plafonné à 60 s). Override via `NEMETON_STAC_MAX_TRIES`. Quand tous les backends sont épuisés, émission d'un warning agrégé unique (\dQuote{All STAC backends failed after retries}) en plus des warnings par backend, pour que `nemetonshiny` puisse afficher UN seul toast au lieu d'en empiler un par backend. Corrige le symptôme rapporté côté UI : deux toasts \dQuote{STAC backend 'pc' failed: HTTP 504 Gateway Timeout} qui pourrissent l'écran à chaque pic PC. 5 nouveaux tests offline (`.is_stac_transient` sur 429/5xx/4xx, override env var, warning agrégé).
  - **2026-05-12** — `v0.21.4` (même branche) : cache COG sur disque (option C). `ingest_sentinel2_timeseries(..., cache_dir = NULL)` — quand non-NULL, chaque bande croppée est écrite en GeoTIFF tuilé DEFLATE sous `<cache_dir>/{scene_id}/{band}.tif`. Lecture locale sur cache hit (zéro HTTP), VSI + crop + write atomique sur miss. Extent-aware : un fichier dont la bbox ne couvre plus les placettes est écrasé silencieusement. Complémentaire à `skip_cached` (v0.21.3) : `skip_cached` court-circuite au niveau `obs_pixel` (rapide), `cache_dir` court-circuite au niveau **bande raster** (utile quand on doit ré-extraire — ajout de bande, debug, wipe DB). Nouveaux events progress : `s2:band_cached` / `s2:band_fetched` (granularité par bande). Empreinte disque : ~24 MB pour 159 scènes × 3 bandes sur AOI 1 km². 7 nouveaux tests offline (terra writeRaster + lecture). Refactor de `.extract_scene_obs` autour d'un helper `.get_s2_band_raster()`. **Convention de chemin (côté `nemetonshiny`)** : passer `cache_dir = file.path(project_path, "cache", "layers", "sentinel2")` pour respecter le layout `<project>/cache/layers/{lidar_mnh,lidar_mnt,lidar_nuage,sentinel2,...}` déjà utilisé par `detect_ndp_from_cache()` dans `R/ndp.R`. `cache/` doit être dans `.gitignore` (artefacts regénérables) ; **pas** `data/s2_cache` (mentionné par erreur dans la spec « voie propre » initiale — `data/` est réservé aux artefacts possédés par l'utilisateur). La fonction `nemeton` reste path-agnostique, seul le wiring shiny est concerné.
  - **2026-05-12** — `v0.21.3` (même branche) : `ingest_sentinel2_timeseries()` accepte `skip_cached = TRUE` (par défaut). Avant la boucle STAC, on requête `obs_pixel` pour identifier les `obs_date` déjà couvertes pour **toutes** les placettes × **toutes** les bandes demandées : ces scènes sont sautées (zéro HTTP, zéro crop, zéro extraction). Sur un re-run typique (~159 tuiles, 2 bandes), gain réseau ~1-2 GB → 0. La pré-filtration est *partial-coverage-aware* : ajouter une bande à une zone existante ne déclenche pas de faux-positif (la scène est ré-extraite). Deux nouveaux events progress : `s2:cache_lookup` (avec `n_cached` / `n_to_process`) et `s2:scene_cached`. Colonne `n_scenes_cached` ajoutée au summary. 4 nouveaux tests dans `test-monitoring.R`.
  - **2026-05-12** — `v0.21.2` (branche `claude/nemetonshiny-features-1uUD9`) : `run_fordead_dieback()` accepte désormais `progress_callback = NULL` (même convention que `ingest_sentinel2_timeseries`). Phases émises : `fordead:start` → `fordead:phase`/`fordead:phase_done` × {6 sans persistence, 7 avec `con`+`zone_id`} → `fordead:complete` (ou `fordead:error`). `phase_name` ∈ {`vegetation_index`, `train_model`, `forest_mask`, `dieback_detection`, `export_results`, `postprocess`, `persist`}. Exceptions du callback avalées (UI buggée n'aborte jamais FORDEAD). 4 nouveaux tests mockés dans `test-fordead-pipeline.R`. Débloque le wiring "toast persistant FORDEAD" côté `nemetonshiny` (équivalent E6.b phase 2 mais pour le mode diagnostic).
- `nemetonshiny@main = 9596e6c` (release `v0.23.5` poussée sur `origin` le 2026-05-09, GitHub release publiée).

**Backlog identifié** :

1. Reliquat E6.b (low priority, déclenchable à la demande) — phase 2 (wiring ingestion S2 async côté `mod_monitoring` via `progress_callback` déjà câblé côté cœur, cf. journal 2026-05-05 commit `5da2e85`), phase 3 (plotly time series NDVI/NBR par placette en mode rapide), phase 6 (smoke shinytest2 E2E sur les deux modes Monitoring).
2. **E7 RAG perspectives IA** — à cadrer. Spec 009 à rédiger. pgvector déjà disponible dans la stack DB (image `timescaledb-ha:pg16`) donc pas de friction infra. Probable cible `v0.23.0`.

---

# Chantier clos — Épaississement 6 : Suivi sanitaire (release v0.21.0)

**Démarré**     : 2026-04-24 (initialement « monitoring continu »)
**Reframing**   : 2026-04-26 — chantier reciblé en **« suivi sanitaire »** après lecture du rapport ONF/DSF (Bernard & Doridant 2024). Spec 008 + ADR-013 rédigés. Voir `specs/008-suivi-sanitaire/`.
**Clôture**     : 2026-05-06 — tag `nemetonshiny@v0.21.0` poussé rétroactivement sur le commit `05cd425` (daté 2026-04-30) qui portait déjà la version dans DESCRIPTION + l'entrée NEWS complète. Release GitHub créée avec changelog manuel reprenant les highlights E6.c.5.
**Reliquat backlog** : E6.b phases 2/3/6 (cf. *Chantier en cours* ci-dessus).

## Stratégie hybride (validée 2026-04-26)

Deux pipelines complémentaires alimentent la même table `alert` :

| Pipeline | Méthode | Question répondue | Coût | Cas d'usage |
|----------|---------|-------------------|------|-------------|
| **Surveillance rapide** | rolling-window NDVI/NBR (E6.a, déjà livré) | « Choc récent ? » | secondes | coupes, chablis, incendies |
| **Diagnostic sanitaire** | FORDEAD via reticulate (CRSWIR + harmonique, GPL-3) | « Mes peuplements dépérissent-ils ? » | minutes-heures | scolyte / sécheresse / dépérissement progressif |
| **Fusion** | join SQL (window ±30 j) | « Dépérissement ou perturbation mécanique ? » | ms | discrimination des causes |

## Garde-fous applicatifs (issus du rapport ONF/DSF 2024)

- **G1** — filtrage par défaut classes 3-forte + 4-sol-nu (les classes 1-2 ont 50% / 1/3 de faux positifs)
- **G2** — fusion rolling-window × FORDEAD pour distinguer dépérissement / perturbation mécanique
- **G3** — bannières UI géographique (`fordead_validity_zones.geojson` : 88, 39, 01, 73, 74) et essences (≥70% épicéa+sapin)
- **G4** — workflow QField de validation terrain (réutilise E5.b, schéma DSF-aligné)
- **G5** — indicateur R5 pondéré par les taux de bonne détection observés terrain (`FORDEAD_CONFIDENCE_WEIGHTS` = 0.10 / 0.30 / 0.82 / 0.70)

## Découpage actualisé

### E6.a — Squelette TimescaleDB + ingestion S2 + rolling-window — **livré v0.20.0 / v0.20.1**

Voir spec 007 (devient la couche « Surveillance rapide » de spec 008).

- [x] Phase 1-5 livrées dans le commit `28570d4` (release v0.20.0 du 2026-04-25)
- [x] Hardening v0.20.1 (commit `a7ea8d3`) : 2 bugs DB corrigés via tests d'intégration

### E6.b — UI `mod_monitoring` (côté `nemetonshiny`) — **phases 1, 4, 5 livrées ; 2, 3, 6 en backlog**

Livré dans `nemetonshiny@v0.21.0` (2026-04-30) sauf indication contraire.

- [x] **Phase 1 — Scaffolding + DB adapter + zones reactive** (commit `2909b8f`, 2026-04-25)
- [ ] Phase 2 — Ingestion async (ExtendedTask + future_promise) + toasts. Côté cœur : `progress_callback` ajouté à `ingest_sentinel2_timeseries()` le 2026-05-05 (commit `5da2e85`). Reste à câbler côté app.
- [ ] Phase 3 — Time series plotly NDVI/NBR par placette (mode rapide). En mode sanitaire, le plotly distribution `confidence_class` est livré.
- [x] **Phase 4 — Carte leaflet alertes** (POINT, popup, classes 1-4 + filtres G1)
- [x] **Phase 5 — Persistance config** (`monitoring_mode`, `monitoring_threshold_anomaly`, `monitoring_vegetation_index`, `monitoring_dates_training`, validity intersections) dans `metadata.json`
- [ ] Phase 6 — Smoke shinytest2 E2E + finitions UX

### E6.c — Pipeline FORDEAD (côté cœur `nemeton`) — **en cours**

Spec 008 §3, plan.md §2, tasks.md chantiers E6.c.1 à E6.c.5.

- [x] **E6.c.1** — `R/fordead_python.R` (helpers reticulate) + `R/fordead_pipeline.R` (orchestrateur `run_fordead_dieback`) + `inst/python/requirements.txt` + tests mockés (`test-fordead-python.R` 8 tests, `test-fordead-pipeline.R` 12 tests, 44 PASS) — branche `feat/008-fordead-pipeline` (2026-04-29)
- [x] **E6.c.2** — `R/fordead_postprocess.R` (constants + raster→POINT clusters via `terra::patches`, snap-to-plot INSERT, `classify_disturbance()` G2, `list_alerts()` G1) + migration `0002_fordead.sql` + branchement de `run_fordead_dieback` — branche `feat/008-fordead-postprocess` (2026-04-29). 45 nouveaux tests offline, 5 d'intégration TimescaleDB. Suite : 5745 PASS.
- [x] **E6.c.3** — `R/fordead_validity.R` (`load_fordead_validity_zones`, `check_fordead_validity`, constantes `FORDEAD_VALIDITY_DEPARTMENTS`/`FORDEAD_VALIDITY_SPECIES`) + `inst/extdata/fordead_validity_zones.geojson` (5 départements 88/39/01/73/74, ~27 500 km², 80 ko, simplifié 100 m, EPSG:4326) + script reproductible `data-raw/build_fordead_validity_zones.R` (source : `gregoiredavid/france-geojson` car `geo.api.gouv.fr/format=geojson` retourne désormais des attributs sans contour). 16 tests offline (zones + validity, helpers `.is_epicea` / `.is_sapin_pectine` corrects sur la collision Picea abies / Abies alba / Pseudotsuga). Suite : **5866 PASS / 0 FAIL**.
- [x] **E6.c.4** — `R/health_validation.R` : `get_health_validation_schema()` (11 `.field()` DSF-aligned, ValueMap stades + causes, fallback `essence_dominante`), `generate_health_validation_plots()` (stratifié `confidence_class`, GRTS via `spsurvey` ou repli random, `.allocate_health_strata()` largest-remainder, NA typés pour les colonnes éditables, sortie `sf` POINT EPSG:2154 prête pour `create_qfield_project()`), `ingest_health_validation()` (snap par plus-proche-voisin Lambert-93, mapping `stade → (validation_status, validation_cause)` avec règle `coupe_rase × confidence_class` du rapport ONF/DSF, précédence `validated_by`, écrasement par `cause` libre, `details` data.frame avec `reason ∈ {ok, no_alert_within_snap, missing_stade}`). Constantes exportées `HEALTH_VALIDATION_STADES` (7 codes DSF), `HEALTH_VALIDATION_CAUSES` (7 causes). 31 tests : 10 schema, 11 generate (offline + mock du fallback GRTS via `local_mocked_bindings(requireNamespace)`), 10 ingest (intégration TimescaleDB via `with_clean_db`). Suite : **5957 PASS / 0 FAIL**.
- [x] **E6.c.5 livré** dans `nemetonshiny@v0.21.0` (2026-04-30) — Mode 1/Mode 2 toggle, bannières G3 (5 départements 88/39/01/73/74 + ratio épicéa+sapin ≥70%), modal de confirmation hors-domaine, génération QField (n placettes, GRTS/random), sous-onglet "Validation sanitaire" dans `mod_field_ingest`, ingestion via `nemeton::ingest_health_validation()` qui retourne (confirmed / false-positive / unmatched), 9 testthat. ~30 i18n keys (`monitoring_mode_*`, `health_validation_*`, `r5_*`).

### E6.d — Indicateur R5 dépérissement (côté cœur `nemeton`) — **livré 2026-04-30**

Spec 008 §7.

- [x] `R/indicators-deperissement.R` : `indicateur_r5_deperissement(units, fordead_results, weights, min_resineux, include_low_classes, resineux_col)`. Sortie : colonnes `R5` (0-100) et `r5_status` (`calculated / skipped_no_resineux / skipped_no_fordead`).
- [x] Intégration radar : `INDICATOR_FAMILIES$R` étendu à 5 indicateurs (`R1..R5`) — `create_family_index()` détecte R5 via la regex `^R[0-9]` existante.
- [x] 18 tests : cas vide, mono-classe (50% × 3-forte → R5 = 41), multi-classes, Quercus skipped, G1 (exclusion 1-faible/2-moyenne par défaut), `include_low_classes = TRUE`, plafonnement, clusters hors UGF, `resineux_col` custom, `min_resineux`, `weights` custom, sf vide, erreurs typées, intégration radar.

### E6.f — Release v0.21.0 — **clos partiellement (tag rétroactif 2026-05-06)**

- [ ] Smoke shinytest2 E2E (reporté → backlog E6.b phase 6)
- [x] NEWS, DESCRIPTION, CITATION app à jour (v0.21.0, 2026-04-30) ; côté cœur, pas de release stable depuis v0.20.1 — tout E6.c/E6.d est dans le cycle dev `0.20.1.9001..9009`
- [x] Tag `nemetonshiny@v0.21.0` poussé rétroactivement le 2026-05-06 sur `05cd425`. GitHub release créée avec changelog manuel.

### E7 — RAG perspectives IA — **non démarré**

Spec à rédiger (`specs/009-rag-perspectives-ia/`). pgvector + base de connaissances forestière (ADR-012). pgvector déjà disponible dans la stack depuis le 2026-05-05 (image `timescaledb-ha:pg16`). Cible probable `v0.23.0`.

---

## Documents de référence du chantier

- `specs/008-suivi-sanitaire/spec.md` — Spec fonctionnelle (vision, scope, garde-fous, R5, validation)
- `specs/008-suivi-sanitaire/plan.md` — Plan technique (stack, pipeline, fusion, performance, risques)
- `specs/008-suivi-sanitaire/tasks.md` — 71 tâches détaillées (53 cœur + 18 app)
- `specs/008-suivi-sanitaire/ADR-013-suivi-sanitaire-fordead.md` — ADR draft (à porter dans `platform_nemeton/docs/`)
- Spec 007 (`specs/007-monitoring-continu/`) — devient la couche « Surveillance rapide » de spec 008, conservée en référence
- Rapport ONF/DSF Bernard & Doridant 2024 — référence de calibration et de limites

---

## Décisions validées 2026-04-26 (fordead)

1. **Reframing** E6 « monitoring continu » → « **suivi sanitaire** »
2. **Stratégie hybride** : rolling-window (rapide) + FORDEAD (diagnostic) ; pas de throwaway de v0.20.0
3. **Cinq garde-fous** G1-G5 obligatoires (filtrage classes / fusion / bannières / validation QField / R5 pondéré)
4. **Workflow validation QField** intégré dans le même chantier E6.c (pas de release séparée)
5. **Zones de validité géographique** construites depuis IGN ADMIN-EXPRESS via geo.api.gouv.fr (5 départements : 88 Vosges, 39 Jura, 01 Ain, 73 Savoie, 74 Haute-Savoie)
6. **Calibration figée** v0.21.0 sur les paramètres ONF/DSF (CRSWIR, 0.16, 2 ans entraînement, 3 anomalies consécutives) — pas exposée à l'utilisateur final
7. **Paperwork avant code** : spec 008 + ADR-013 publiés avant E6.c

## Décisions validées E6.a (2026-04-24)

1. STAC : **CDSE prioritaire + PC fallback**
2. Bandes rapides : **NDVI + NBR** (B04, B08, B12)
3. Déploiement : **docker-compose service TimescaleDB**
4. Déclenchement : **à la demande** (pas de cron en E6.a / E6.c — reporté E6.f)
5. Granularité rapide : **par placette** (buffer 15 m)

---

## Journal

- **2026-05-09** — Release **`nemetonshiny@v0.23.5`** publiée (commit `nemetonshiny@9596e6c`, tag + GitHub release poussés sur `origin` ; bump *patch*). Deux axes sur le Plan d'actions, sans modif cœur. (1) **Chat IA gagne scope + overwrite**. Deux contrôles ajoutés sous l'historique du chat panel (R/mod_action_plan.R) : `radioButtons("chat_scope")` *Toutes les UGF / Sélection courante* (inline, default "all") et `checkboxInput("chat_overwrite")` *Écraser le plan existant* (default FALSE). Mêmes sémantiques que le modal `gen_run` : au moment du `chat_send`, `target_ugs` est calculé depuis `selected_ug_rv()` puis injecté dans `ctx$ug_ids` avant l'appel à `build_action_plan_chat_prompt()` — le LLM ne voit que les UGF ciblées dans le prompt. Garde-fou : si `scope = "selected"` mais aucune UGF cochée, on remonte le toast `action_plan_no_ug` et on ne tape pas le LLM. `target_ugs` est stashé dans `rv_state$pending_chat_target_ugs` pour rester cohérent entre le tour qui propose les actions et celui qui les applique. Au moment du `chat_apply`, si `input$chat_overwrite` est TRUE, les actions existantes des `pending_chat_target_ugs` sont droppées via `Filter(function(a) !(a$ug_id %in% target_ugs), cur_plan$actions)` avant le `bulk_upsert_actions()` — mirror exact de `gen_run` lignes 1794-1799. Le modal de confirmation `chat_apply` affiche désormais une **bannière `text-warning small`** quand l'overwrite est coché, avec le count des UGF impactées (clé i18n `action_plan_chat_apply_overwrite_warn_fmt` — *"⚠ Les actions existantes des %d UGF ciblées seront remplacées."*). Nouvelle clé `action_plan_chat_scope_sel` ("Sélection courante" / "Current selection") pour le label statique du radio (la sidebar persistante n'a pas le compteur dynamique du modal). (2) **Fix bascule de langue FR ↔ EN**. Deux bugs combinés : (a) `app_server.R:113-115` écrivait `options(nemeton.app_language = ...)` mais `app_ui.R:14` lit `getOption("nemeton.app_options")$language` — clé totalement différente, donc la préférence ne survivait pas au reload et l'UI rebascule sur la langue passée à `run_app()` au démarrage ; (b) le toast *"Rechargez la page pour appliquer"* demandait un reload manuel qui, à cause du bug (a), revenait toujours à la langue initiale. Fix : on persiste dans **la bonne** option `nemeton.app_options$language` (mutation de `get_app_options()` puis `options(nemeton.app_options = app_opts)`), et on appelle directement `session$reload()` pour que le rebuild d'`app_ui` se fasse sans intervention de l'utilisateur. Garde anti-init via `if (identical(new_lang, isolate(app_state$language))) return()` — l'observer ne reload pas quand le selectInput émet sa valeur initiale au démarrage de session. Tests `test-app_server.R` mis à jour : assertion ciblait `nemeton.app_language` → corrigée vers `nemeton.app_options$language`. Clé i18n orpheline `language_changed` retirée + 2 asserts de `i18n_*$has("language_changed")` supprimés. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-09** — Release **`nemetonshiny@v0.23.4`** publiée (commit `nemetonshiny@bd944ac`, tag + GitHub release poussés sur `origin` ; bump *patch*). i18n du chat IA, sans modif cœur. Les libellés de rôle dans l'historique du chat (`output$chat_history_ui` dans `R/mod_action_plan.R`) passent désormais par i18n : `msg$role` brut ("user" / "assistant") n'apparaît plus tel quel dans l'UI. Le rendu produit "**Vous** :" / "**Assistant** :" en FR et "**You** :" / "**Assistant** :" en EN, traduit via `i18n_render$t("action_plan_chat_role_user" | "_assistant")`. Le modèle de données conserve les clés anglaises ("user" / "assistant") puisque c'est la convention LLM (le prompt builder `build_action_plan_chat_prompt()` n'est pas touché) — la traduction est purement cosmétique au display, et bascule dynamiquement avec le switch de langue puisque `i18n_render` est résolu dans la `renderUI` qui dépend de `app_state$language`. Deux nouvelles clés i18n : `action_plan_chat_role_user`, `action_plan_chat_role_assistant`. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-09** — Release **`nemetonshiny@v0.23.3`** publiée (commit `nemetonshiny@aa5f0dd`, tag + GitHub release poussés sur `origin` ; bump *patch*). Polish UX du chat IA, sans modif cœur. Clic sur **Envoyer** dans le chat fait apparaître un **toast persistant en bas à droite** avec une **roue dentée tournante** (Font Awesome `fa-spin` sur `shiny::icon("gear")`) et le label *"L'IA réfléchit…"* (FR) / *"AI is thinking…"* (EN). `duration = NULL`, `closeButton = FALSE`, type `"default"` (toast neutre). Suppression garantie via `on.exit(shiny::removeNotification(thinking_id), add = TRUE)` placée juste après l'ouverture du toast — chaque branche de retour de l'observer (succès LLM, erreur LLM dans `tryCatch`, parse échoué, etc.) déclenche le cleanup. Nouvelle clé i18n `action_plan_chat_thinking`. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-09** — Release **`nemetonshiny@v0.23.2`** publiée (commit `nemetonshiny@b24ef94`, tag + GitHub release poussés sur `origin` ; bump *patch*). Polish UI sur l'onglet Plan d'actions, sans modif cœur. (1) **Chat IA repositionné dans la sidebar droite, sous la carte "Tableau des actions"**. v0.23.1 avait introduit le chat dans une sidebar gauche dédiée ; on revient à un seul `bslib::layout_sidebar` (suppression du double imbriqué) avec deux cartes empilées dans la sidebar droite : *action_panel* en haut (Tableau des actions — Sélection / IA / Manuel / Exports) puis *chat_panel* en dessous (Affiner le plan avec l'IA — historique scrollable + textarea + boutons Effacer / Envoyer). Meilleure densité d'information sur les écrans portables, et le chat reste persistant comme en v0.23.1 — la conversation continue de survivre à la navigation map / table / Kanban. (2) **Auto-scroll de l'historique du chat** : la div `.chat-history` reçoit un `id` stable (`ns("chat_history")`) ; `output$chat_history_ui` injecte à la fin de chaque render un `tags$script` minimaliste qui fait `setTimeout(function(){var el=document.getElementById(<id>);if(el){el.scrollTop=el.scrollHeight;}},0);`. Le `setTimeout(0)` diffère au prochain tick pour que le DOM soit à jour quand `scrollHeight` est lu — sans ça la mesure pourrait précéder le rendu des nouveaux nodes. Le snippet est ré-attaché à chaque render donc il se déclenche aussi bien pour un nouveau message que pour un *Effacer*. (3) **Libellé bouton** : le bouton "Générer (IA)" dans la section IA de l'action_panel devient "Générer les actions (IA)" (FR) / "Generate actions (AI)" (EN) — clé i18n `action_plan_generate_all`. Précise la cible (génération du plan d'actions, pas autre chose comme un rapport). Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-09** — Release **`nemetonshiny@v0.23.1`** publiée (commit `nemetonshiny@d2245d3`, tag + GitHub release poussés sur `origin` ; bump *patch* parce qu'aucun contrat API ne change). Deux axes UX sur l'onglet Plan d'actions, sans modif cœur. (1) **Chat IA en sidebar gauche persistante** : la fenêtre de Q/R quitte la modal et s'installe dans une sidebar de 350 px symétrique de la sidebar droite (action panel). Card collapsible header `bg-info` icône `chat-dots`, historique scrollable (max-height 50 vh, min-height 160 px, fond gris clair `#fafafa`, padding 0.5 rem), `textAreaInput` 3 lignes resize=vertical, boutons *Effacer* / *Envoyer* en flex 50/50. La conversation reste visible pendant que l'utilisateur navigue map / table / Kanban — auparavant le bouton "Ouvrir le chat" déclenchait `showModal()` qui se fermait à chaque interaction. Layout `mod_action_plan_ui` passe à un double `bslib::layout_sidebar` imbriqué (extérieur left = chat / intérieur right = action panel / contenu = navset map+table / kanban). Bouton "Ouvrir le chat" retiré de la section IA de la sidebar droite, observer `input$open_chat` (~30 LOC `showModal`) supprimé. Les deux sidebars exposent le toggle bslib en bordure pour les écrans portables. (2) **Fix sync carte → tableau** : clic sur une parcelle dans la carte ne sélectionnait que la couche orange Selection sans propager au tableau DT. Le handler `input$map_shape_click` (R/mod_action_plan.R:626) ne faisait que `selected_ug_rv(cur)` ; ajout d'un `DT::selectRows(DT::dataTableProxy("action_table"), which(df$ug_id %in% cur))` à la fin du handler — toutes les lignes dont `ug_id ∈ cur` sont maintenant highlight (toggle : 2e clic dé-sélectionne, multi-sélection cohérente avec le `mode = "multiple"` du DT). Pas de boucle réactive : la sélection DT déclenche le reverse-observer `input$action_table_rows_selected` qui rappelle `selected_ug_rv()` avec la même valeur ; `reactiveVal` dedupe par `identical()` donc le round-trip s'arrête au 2e pas. Le sens table → carte (déjà fonctionnel) reste inchangé. (3) **Removed** : 2 clés i18n orphelines `action_plan_open_chat` ("Chat IA"/"AI Chat") et `action_plan_chat_input_label` ("Votre question ou demande"/"Your question or request") après refonte du chat. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-09** — Release **`nemetonshiny@v0.23.0`** publiée (commit `nemetonshiny@cbf44ab`, tag + GitHub release poussés sur `origin` ; bump *minor* parce que le contrat de `update_action_in_plan()` change). Sprint refonte UX du Kanban + de la modal "Ajouter une action", sans modif cœur. (1) **Drag-and-drop Kanban libre** : la sémantique du DAG `proposee → validee → planifiee → realisee → abandonnee` qui empêchait certaines transitions disparaît. `update_action_in_plan()` (R/service_action_plan.R:384) n'enforce plus `is_valid_status_transition()` ; il valide uniquement que le nouveau statut figure dans `ACTION_PLAN_TRANSITIONS` (i.e. *statut connu*, plus d'ordre imposé). `is_valid_status_transition()` et `ACTION_PLAN_TRANSITIONS` restent en place comme documentation du flux naturel mais ne gatent plus les writes. Côté kanban, le validateur dans l'observer `kanban_drop` est retiré, le toast warning "Transition refusée" disparaît, les drops same-column restent un no-op qui re-render pour ré-aligner l'ordre canonique année. (2) **Double-clic sur une fiche Kanban → modal d'édition pré-remplie** (statut / priorité / année calendaire / commentaire). Le commentaire est éditable en `textAreaInput` 6 lignes resize=vertical, c'est le cas d'usage principal — l'édition inline DT est single-line et restait peu pratique. Implémentation : listener délégué `dblclick` au niveau du `kanban_board`, `Shiny.setInputValue` avec nonce, observer R `input$kanban_edit_request` qui showModal + observer `input$kanban_edit_save` qui appelle `update_action_in_plan()` puis `removeModal()`. Cleanup `board._kanbanDblclickHandler` entre re-renders pour éviter les fuites de listeners. Hint via `title=` HTML attribute sur chaque card (i18n `action_plan_kanban_card_hint`). (3) **Bouton "Déplacer" + dispatcher `kanban_move_*` retirés** (~50 LOC d'observer mort). La constante `KANBAN_STATUSES` qui ne servait plus qu'à la dropdown retirée également. (4) **Tri chronologique par colonne** : dans `build_col`, `sub <- sub[order(sub$annee_realisation, na.last = TRUE), ]` — chaque statut Kanban se lit du plus tôt au plus tard du haut vers le bas. (5) **Commentaire affiché sur chaque carte** : `<div class="kanban-card-comment small text-muted mt-1">` avec `word-break: break-word` qui apparaît seulement si `nzchar(row$commentaire)`. (6) **Modal "Ajouter une action" — UGF en label** : la dropdown UGF affichait `ug_id` brut (ex. `ugf_42`) au lieu du libellé humain. Construction d'un `ug_choices = setNames(ids[ord], labels[ord])` à partir de `sf$label` mappé sur `sf$ug_id`, trié par label. Fallback sur `ug_ids()` si `ug_sf_4326()` indisponible. (7) **Modal "Ajouter une action" — Année cible en année calendaire** : `numericInput` montre désormais l'année réelle (default = `base_year + 1`, min = `base_year + 1`, max = `base_year + horizon`, step 1) ; conversion en offset = `year - base_year` au moment du save dans `input$add_run`. La persistence reste en offset (conforme à `validate_action()` qui exige `annee_cible in 1..horizon`). (8) **Tableau d'actions — total à droite** : le `dom` DT passe d'un simple `"frtilp"` à un layout custom `<"top"f>rt<"d-flex … dt-bottom-row"<"d-flex gap-3 align-items-center"lp>i>` ; règles CSS scoped sur `.dt-bottom-row` neutralisent les `float`/`clear` par défaut de `dataTables_info`/`_length`/`_paginate` et alignent l'info à droite via `text-align: right`. (9) **Tests** : `test-service_action_plan.R:145` repurposé pour tester la nouvelle sémantique (free movement OK, rejet uniquement sur statut inconnu) ; 2 tests obsolètes de `test-mod_action_plan.R` (DAG enforcement standalone + i18n key disparue) supprimés. **Suite : 5849 PASS / 2 SKIP / 1 FAIL** en 276 s — l'unique échec est la régression pré-existante `audit_to_dataframe` héritée de PR #25 (classe JSON sur `nouveau[1]`), hors scope. (10) **Tooling** : `.gitignore` ajoute `.claude/` (settings locaux Claude Code + `scheduled_tasks.lock`). 4 nouvelles clés i18n FR/EN (`action_plan_kanban_card_hint`, `action_plan_kanban_edit_title`, `action_plan_kanban_edit_save`, `action_plan_kanban_edit_saved`) ; 2 clés retirées (`action_plan_kanban_move`, `action_plan_kanban_drop_invalid_fmt`) maintenant orphelines. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-09** — Release **`nemetonshiny@v0.22.4`** publiée (commit `nemetonshiny@7c192b9`, tag + GitHub release poussés sur `origin`). Patch + UX polish post-v0.22.3, sans modif cœur. (1) **Fix démarrage anonyme avec dev roles** : `mod_auth_server()` plantait au boot dès que `NEMETON_AUTH_DEV_ROLES` était défini — l'appel `cli::cli_alert_info("…dev roles: {.val {auth_state$user_roles}}.")` faisait évaluer l'interpolation `glue` de `{auth_state$user_roles}` en dehors d'un `reactive()/observe()`, ce que `reactiveValues` rejette (*"Can't access reactive value 'user_roles' outside of reactive consumer"*) ; la valeur parsée passe désormais par une locale `parsed_roles` avant l'assignation. Régression introduite par #41 dans v0.22.3. (2) **Plan d'actions — sélecteur "Afficher 5/10/25/50/All" déplacé sous le tableau** (`dom` DT `"lfrtip"` → `"frtilp"`) — la barre de recherche reste seule au-dessus, info + lengthMenu + pagination s'alignent en bas. (3) **Plan d'actions — figeage strict UGF + Année** lors du scroll horizontal : `DISPLAY_COLS` réordonné pour placer les colonnes cachées (`id`, `ug_id`, `annee_cible`) en queue, `fixedColumns.leftColumns` passe de 5 à 2. L'extension DT `FixedColumns` compte toutes les colonnes du DOM y compris `visible:FALSE`, donc l'ancien `leftColumns = 5L` créait des artefacts d'en-têtes clones ; le nouvel ordre rend le décompte alignement avec ce que l'utilisateur voit. `colname_map` et `hidden_targets` ajustés en conséquence (cibles 13-15) ; `editable_idx` recalculé automatiquement depuis le nouvel ordre, le handler `action_table_cell_edit` reste cohérent puisqu'il résout `field_disp` via `DISPLAY_COLS[col_idx]`. (4) **Plan d'actions — légendes leaflet i18nisées** : les `legend_title` littéraux `"annee"` / `"type"` / `"priority"` passés à `leaflet::addLegend()` remplacés par `i18n$t("action_plan_col_annee" | "_type" | "_priorite")` (réutilisation des clés des en-têtes de colonnes pour cohérence avec le tableau) ; affichent désormais "Année / Type / Priorité" en FR et "Year / Type / Priority" en EN, switch dynamique au changement de langue. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-07** — Release **`nemetonshiny@v0.22.3`** (commit `nemetonshiny@2ed3540`, *tag local pas encore poussé sur `origin`*). Sprint UX polish sur l'onglet Plan d'actions (PR #35..#39, toutes mergées le 2026-05-07). (1) Recherche globale DT en **regex avec sémantique OR** (#35) : taper `eclaircie|plantation` retourne les lignes correspondant à l'un des deux termes ; `caseInsensitive = TRUE` pour rester accent-tolérant. (2) Nouveau badge **Surface totale** dans la barre de totaux au-dessus du tableau (#36) : somme de `surface_ha` sur les lignes visibles dans le DT, formatée 2 décimales + unité `ha`, rendu en `text-primary`. Le badge se place **après Bilan** pour que la séquence Coût / Revenu / Bilan se lise d'abord. Helper `pill()` étendu avec arguments optionnels `unit` (default `"EUR"`) et `digits` (default `0`) pour rétrocompat des trois pills monétaires existantes. (3) Pagination DT à **5 lignes par page** (vs 50 + scroll interne 60 vh) ; `lengthMenu` 10/25/50/All ; `dom` `"frtip"` → `"lfrtip"` pour placer le sélecteur de longueur à gauche du search global ; suppression de `scrollY`+`scrollCollapse` → la card du tableau se contracte autour du contenu visible au lieu de padder à 60 vh. `scrollX = TRUE` et les deux colonnes pinned (UGF + Année) inchangés. (4) Titre de la sidebar avec icône + "Tableau des actions" (#37) pour relier visuellement la sidebar à la card du tableau. (5) Le bloc *Statut Kanban* (qui dupliquait les déplacements Kanban par carte) est remplacé par une **action card collapsible** (#38) — sidebar plus scannable. (6) Sidebar redimensionnée à **350 px** avec **dual collapse** (#39) : la sidebar bslib elle-même est collapsible, et l'action card à l'intérieur a son propre toggle de collapse. Une nouvelle clé i18n `action_plan_total_surface` (FR *Surface totale* / EN *Total area*). Aucun changement cœur. Cycle dev cœur `0.20.1.9009` inchangé.
- **2026-05-07** — **Fix structurel `.pc_sign_url`** (commit `f3050a2`, mergé sur `nemeton@main` le 2026-05-07) : migration du signing Sentinel-2 du endpoint per-href `/api/sas/v1/sign` vers le batch `/api/sas/v1/token/{collection}`. Le diag du 2026-05-05 (`06d6801`) avait surfacé le problème : sur une recherche STAC retournant 20-50 scènes × 3 bandes (B04/B08/B12), la boucle `vapply(..., .pc_sign_url, ...)` faisait 60-150 appels HTTP individuels et explosait le rate limit PC (vague de `PC sign failed: HTTP 429 Too Many Requests` puis fallback URLs non signées → `HTTP 409` Azure → `Scene "..." skipped: [rast] file does not exist:` côté `terra::rast()` dans `mod_monitoring`). Nouvelle stack côté `R/sentinel2.R` : (1) helper `.pc_collection_token(collection, grace_seconds = 60L)` qui appelle `/token/{collection}` une seule fois, parse `msft:expiry`, et stocke le résultat dans un env privé `.pc_token_cache` keyé par collection ; les appels suivants dans la même session R réutilisent le token jusqu'à 60 s avant expiry (default 25 min si parsing échoue, marge sur les 30 min documentés PC) ; (2) helper pur `.pc_apply_token(href, token)` qui normalise un éventuel `?` initial et choisit `?` vs `&` selon la présence de query params dans l'href ; (3) `stac_search_s2_pc()` appelle le helper batch puis applique le token aux trois colonnes de hrefs en `vapply`. Net : 60-150 appels → 1 appel par session, plus de 429. `.pc_sign_url()` reste dans le code comme fallback documenté mais n'est plus invoqué depuis le search path. Six nouveaux test_that (3 sur `.pc_apply_token` : bare href, normalisation `?` initial, query existante ; 3 sur `.pc_collection_token` : cache hit fresh, cache miss expired qui refetch, échec réseau → NULL avec warning ; mocks via `local_mocked_bindings(.package = "httr2")`). Suite globale **6015 PASS / 0 FAIL** (vs 5760 avant E6.c.5, hors skips). Pas d'impact côté `nemetonshiny` : les warnings disparaîtront dès que les utilisateurs réinstallent `nemeton` (`pak::local_install("/home/pascal/dev/nemeton")` ou redéploiement). Cycle dev `0.20.1.9008` → `0.20.1.9009` (commit `f3050a2`, working tree propre, `origin/main` à jour).
- **2026-05-06** — Release **`nemetonshiny@v0.22.2`** publiée (commit `nemetonshiny@6afb940`). Trois groupes de changements sur l'onglet Plan d'actions, sans aucune modif cœur. (1) Tableau DT nettoyé : suppression des colonnes Type libre / Objectifs / RDI / Source (rarement utilisées, élargissaient le scroll horizontal sans valeur ajoutée — les champs sous-jacents restent éditables via le formulaire ligne) ; suppression de la ligne de filtres par colonne (`filter = "top"` → `"none"`), la recherche globale devient le seul filtre exposé ; hauteur de ligne uniforme via `class = "compact stripe hover nowrap"` + règle CSS scoped `.dt-truncate { max-width: 220px; overflow: hidden; text-overflow: ellipsis }` injectée dans `mod_action_plan_ui` ; UGF + Année restent figées (`fixedColumns = list(leftColumns = 5L)` inchangé). (2) Kanban : 4 colonnes côte à côte (Proposée/Validée/Planifiée/Réalisée) en `bslib::layout_columns(col_widths = c(3,3,3,3))`, Abandonnée full-width dessous (`mt-3`) comme couloir d'archive moins prononcé ; helper `build_col(st, body_max_height)` factorisé ; `min-height: 60px` sur les card_body vides pour préserver une drop-zone même quand la colonne est vide. (3) **Drag-and-drop entre colonnes Kanban**. SortableJS 1.15.6 vendoré sous `inst/app/www/js/Sortable-1.15.6.min.js` (MIT, ~45 KB) ; init script `inst/app/www/js/action_plan_kanban.js` qui expose `window.initKanbanSortable(boardId, dropInputId)` — rebind à chaque tick renderUI pour éviter les fuites d'instances ; côté DOM : `data-action-id` sur chaque carte + `data-kanban-col` sur chaque card_body (contrat avec le JS) ; payload JS → `Shiny.setInputValue` avec `priority: "event"` et `nonce` aléatoire pour défaire la déduplication ; côté serveur, observer `input$kanban_drop` qui passe par le validateur existant `is_valid_status_transition()` — transition autorisée → `update_action_in_plan` + `save_action_plan`, refusée → toast warning + `kanban_render_token` bump qui force un re-render et remet la carte en place ; le menu *Déplacer* par carte coexiste, les deux chemins partagent le même validateur. Trois nouveaux test_that (smoke assets vendorés, couverture validateur drag-drop, présence de la clé i18n `action_plan_kanban_drop_invalid_fmt` en FR + EN). Cycle dev cœur `0.20.1.9008` inchangé.
- **2026-05-06** — Release **`nemetonshiny@v0.22.1`** publiée (commit `nemetonshiny@cb74b25`). Patch trois problèmes surfacés au premier `run_app()` sur DB fraîche : (1) `mod_action_plan::actions_df_all()` crashait sur un projet sans aucune action — l'affectation `df$ug_label <- NA_character_` sur un data.frame de 0 ligne échouait avec *"replacement has 1 row, data has 0"* ; la branche vide retourne désormais tôt avec colonnes de longueur 0 explicites ; (2) `nemeton.projects` absent — `nemetonshiny::db_init_schema()` était exporté mais jamais appelé ; auto-déclenché à la 1ère connexion via `get_db_connection()` + flag mémoïsé `.nemeton_env$.app_schema_initialized` ; (3) `monitoring_zone` absent — `nemeton::db_migrate()` jamais invoqué depuis l'app ; auto-déclenché à la 1ère `get_monitoring_db_connection()` + flag mémoïsé `.nemeton_env$.monitoring_schema_initialized`. Les deux migrations sont idempotentes (CREATE TABLE IF NOT EXISTS côté app, table `schema_migration` côté cœur) donc la cache de session évite juste un round-trip réseau, pas une opération destructrice. Pas de modif cœur. Échec test pré-existant `test-service_action_plan.R:296` (classe JSON sur `audit_log_record_change`, hérité de PR #25) noté dans la release notes mais hors scope. Cycle dev cœur `0.20.1.9008` inchangé.
- **2026-05-06** — Release **`nemetonshiny@v0.22.0`** publiée (tag + GitHub release). Chantier app **« Plan d'actions »** livré dans 12 PR mergées le même jour (#23..#34) : nouvel onglet entier (~2500 LOC entre `R/mod_action_plan.R` et `R/service_action_plan.R`), nouveau profil expert `inst/experts/planificateur.yml` pour le steering LLM, nouveau template Quarto `inst/quarto/action_plan_template.qmd` pour l'export PDF par UGF, +~500 LOC de testthat, +263 lignes d'i18n FR/EN (NMT). Lots livrés : Lot 1+2 (#23) scaffold + carte Leaflet + table DT synchronisée ; Lot 3 (#25) génération IA + Kanban + audit modal ; Lot 4 (#30) sparkline solde cumulé + Gantt + ponts Terrain ; Lot 5 (#32) export GeoPackage + PDF par UGF ; plus colonne Bilan + steering vers solde positif (#29), colonne `revenu_eur` (#28), refonte UI (#26, #27, #31, #33) et sidebar bslib à droite (#34, refs #7). Inclut aussi : storage directory dans Informations projet, toast PostGIS sync avec `dbname@host:port`, projet actif → zone Monitoring, tooltip "Enregistrer comme zone", priorité `NEMETON_DB_URL` > `POSTGRESQL_ADDON_*` restaurée, échecs silencieux de `save_samples()` remontés. Commit de release `nemetonshiny@23adace`. Pas de modification cœur dans ce cycle. Côté cœur, cycle dev `0.20.1.9008` inchangé.
- **2026-05-06** — Tag **`nemetonshiny@v0.21.0`** poussé rétroactivement sur `nemetonshiny@05cd425` (commit déjà daté du 2026-04-30 qui portait `DESCRIPTION: 0.21.0` + entrée NEWS complète). Ni le tag git ni la release GitHub n'avaient été créés à l'époque ; comblé maintenant pour préserver la cohérence DESCRIPTION ↔ tag ↔ release exigée par les consignes de release. La release GitHub a été créée avec un changelog manuel reprenant les highlights E6.c.5 (deux modes Monitoring, garde-fous G1-G5, validation QField, persistence métadonnées, 9 testthat).
- **2026-05-05** — Diag `.pc_sign_url()` (commit `06d6801`). Le helper qui signe les hrefs Sentinel-2 via `https://planetarycomputer.microsoft.com/api/sas/v1/sign` masquait jusqu'ici tous ses échecs (`tryCatch(..., error = function(e) NULL)` puis fallback à l'URL non signée). Conséquence observée côté `nemetonshiny@mod_monitoring` : une vague d'erreurs `GDAL Error 1: HTTP error code : 409` sur `sentinel2l2a01.blob.core.windows.net` traduites en `Scene "..." skipped: [rast] file does not exist:` par `terra::rast()`, sans aucun signal de la cause sous-jacente (rate limit ? timeout ? auth ?). Deux `cli::cli_warn()` remontent désormais le `conditionMessage(e)` pour qu'on puisse identifier le mode d'échec exact à la prochaine ingestion et choisir le fix durable (probablement endpoint batch `/api/sas/v1/token/{collection}` plutôt que retry/backoff par-href, mais on attend la mesure). Pas de release : ce commit est uniquement un instrument de diagnostic, le fix structurel viendra dans la même release que le bilan E6.b. Cycle dev `0.20.1.9007` → `0.20.1.9008`.
- **2026-05-05** — `ingest_sentinel2_timeseries()` accepte désormais un argument optionnel `progress_callback` pour streamer l'avancement du téléchargement Sentinel-2 vers un consommateur (typiquement `mod_monitoring` en E6.b phase 2). Pattern repris à l'identique de `nemetonshiny/R/service_compute.R` (14 sites d'émission existants côté téléchargements de couches + calcul d'indicateurs) : payload `list(current = "phase:detail", completed = N, total = M, ...)`. Cinq phases émises côté cœur : `s2:search` (avant STAC, payload `start`/`end`/`n_plots`/`bands`), `s2:search_done` (après STAC, payload `total = nrow(scenes)`), `s2:scene` (avant chaque scène, `completed = i-1` + métadonnées `scene_id`/`obs_date`/`cloud_pct`/`source`), `s2:scene_skipped` (extraction en erreur, ajoute `error_message`), `s2:complete` (fin, `completed = total` + `n_obs_inserted`). Argument par défaut `NULL` → 100% rétrocompatible. Closure locale `emit()` dans `monitoring.R` pour éviter le boilerplate `if (!is.null(progress_callback))` à 5 endroits. Roxygen documente le contrat de chaque phase ; man/Rd régénéré à la main (roxygen2 pas installé localement) — au passage, ordre `\description` avant `\arguments` corrige le warning `R CMD INSTALL` qu'on avait sur ce Rd. Trois nouveaux tests (`test-monitoring.R`) : chemin nominal (5 phases dans l'ordre, vérification des champs), chemin scène en erreur (`s2:scene_skipped` + `error_message`), chemin STAC vide (`search → search_done` avec `total = 0`). Côté `nemetonshiny` : rien fait, le wiring se fera dans `mod_monitoring` au moment d'attaquer E6.b phase 2 (ingestion async + toasts + barre de progression scène par scène). Cycle dev `0.20.1.9006` → `0.20.1.9007` (commit `5da2e85`).
- **2026-05-05** — Stack DB enrichie : `docker-compose.yml` bascule sur `timescale/timescaledb-ha:pg16` (Debian, embarque PostGIS 3 + pgvector — anticipe ADR-012/E7) et `inst/db/migrations/0001_init.sql` active désormais `postgis` à côté de `timescaledb` dès l'initialisation. Le PGDATA de l'image `-ha` étant à `/home/postgres/pgdata` (vs `/var/lib/postgresql/data` sur l'image Alpine de base), le volume `nemeton_pg_data` doit être recréé : `docker compose down && docker volume rm nemeton_pg_data && docker compose up -d timescaledb`. Aucune colonne du schéma n'est encore typée `geometry` — les colonnes `*_wkt TEXT` restent en place et le travail spatial se fait côté R/sf. La migration vers `geometry(Point, 2154)` + index GiST pourra se faire dans une migration ultérieure quand le volume justifie de pousser le snap-to-plot et les filtres `ST_DWithin` côté SQL. Cycle dev `0.20.1.9005` → `0.20.1.9006` (commit `e323ccf`).
- **2026-04-24** — E6 démarré. Spec 007 rédigée (spec.md, plan.md, tasks.md). Décisions 1-5 tranchées. Démarrage Phase 1.
- **2026-04-25** — E6.a phases 1 à 5 livrées dans le commit `28570d4`. Release **v0.20.0** publiée (NEWS.md, tag, GitHub release). Cycle dev `0.20.0.9000` ouvert (`8df05a8`). Working tree propre. Prochain chantier : E6.b (UI `mod_monitoring` côté `nemetonshiny`).
- **2026-04-25** — E6.a hardening : ajout de `tests/testthat/test-monitoring.R` (12 test_that, 49 assertions, dont 9 d'intégration sur TimescaleDB éphémère). Les integration tests ont surfacé **deux vrais bugs** dans le code v0.20.0 : (1) `db_migrate()` plantait sur la migration multi-statements (RPostgres prepared statement vs PostgreSQL multi-commands) → fix `immediate = TRUE` dans `R/db.R` ; (2) `.insert_obs_pixel()` créait une `TEMP TABLE ON COMMIT DROP` hors transaction → table dropée immédiatement → fix : `CREATE TEMP TABLE` dans la même `dbWithTransaction` que les inserts. Aucun de ces bugs n'avait été détecté parce que la couverture intégration sur la DB était à zéro. Release **v0.20.1** publiée. Suite complète : 5760 PASS / 0 FAIL.
- **2026-04-25** — E5.b follow-up livré côté `nemetonshiny` (`c05962e`) : `.apply_field_data_if_present()` dans `service_compute.R` re-applique le GPKG terrain au compute time pour que les indicateurs P1/P2/B2/C1/R2 voient bien les mesures terrain entre deux runs.
- **2026-04-25** — E6.b phase 1 livrée côté `nemetonshiny` (`2909b8f`) : `mod_monitoring` scaffold + `service_monitoring_db.R` (adapter env-var → URL pour `nemeton::db_connect`) + zones reactive + status card + 19 i18n keys + navbar tab. 822 insertions, 5316 PASS / 0 FAIL.
- **2026-04-30** — **E6.d livré** (cœur). Indicateur R5 (dépérissement FORDEAD) implémenté dans `R/indicators-deperissement.R`. Public : `indicateur_r5_deperissement(units, fordead_results = NULL, weights = FORDEAD_CONFIDENCE_WEIGHTS, min_resineux = 0.3, include_low_classes = FALSE, resineux_col = NULL)`. Sortie : sf augmenté de `R5` (numeric 0-100, NA si skip) et `r5_status` (character ∈ `{calculated, skipped_no_resineux, skipped_no_fordead}`). Logique : pour chaque UGF, calcul de la fraction résineux (binaire 0/1 dérivée du dominant species via `.is_epicea` / `.is_sapin_pectine`, ou colonne explicite `resineux_col` clampée [0,1]). Si `fraction < min_resineux` → R5=NA / skipped_no_resineux ; si pas de FORDEAD results ou alerts vide → R5=NA / skipped_no_fordead. Sinon : intersection POINT-in-polygon entre les centroïdes de clusters et l'UGF, somme pondérée `weights[class] × area_m2 / surface_UGF` (Lambert-93), plafonnée à 1, rescalée en 0-100 pour cohérence radar. Garde-fou G1 par défaut : seules les classes `3-forte` et `4-sol-nu` sont incluses (les classes 1-faible / 2-moyenne ont 50%/33% de faux positifs selon le rapport ONF/DSF 2024). `include_low_classes = TRUE` les rajoute si l'utilisateur l'active explicitement. Pondérations issues de `FORDEAD_CONFIDENCE_WEIGHTS` (0.10 / 0.30 / 0.82 / 0.70 calibrées sur le rapport ONF/DSF). Intégration radar : `INDICATOR_FAMILIES$R` étendu de 4 à 5 indicateurs (`R1..R5`) avec labels et tooltips FR/EN ; aucune modif requise dans `R/family-system.R::create_family_index()` car la regex `^R[0-9]` détecte automatiquement `R5`. Vérifié : `famille_risque` reste finie quand R5 est NA (R1..R4 prennent le relais). 18 nouveaux tests offline (`test-indicators-deperissement.R`) : cas vide, mono-classe 50% × 3-forte → 41, multi-classes additif, Quercus → skipped, G1 (exclusion 1-faible/2-moyenne), `include_low_classes`, plafonnement à 100, clusters hors UGF, `resineux_col` custom (override + clamp), `min_resineux` honoré, `weights` custom, sf vide, erreurs typées, intégration `create_family_index`. **Le chantier cœur de la release v0.21.0 est désormais complet** (E6.c.1/.2/.3/.4 + E6.d). Reste l'app (E6.b phases 2-6, E6.c.5) et le smoke E2E (E6.f) pour boucler v0.21.0. Suite : **5988 PASS / 0 FAIL** (vs 5957). Cycle dev `0.20.1.9003` → `0.20.1.9004`.
- **2026-04-30** — **E6.c.4 livré** (cœur). Module `R/health_validation.R` (~430 lignes) qui implémente le garde-fou G4 de la spec 008. Trois fonctions exportées + deux constantes. (1) `get_health_validation_schema(region, lang)` : 11 descripteurs `.field()` DSF-aligned, vocabulaire `HEALTH_VALIDATION_STADES` (7 codes : sain, sain_scolyte_vert_indif, scolyte_vert/rouge/gris, scolyte_rouge_gris_indif, coupe_rase) et `HEALTH_VALIDATION_CAUSES` (7 causes : scolyte / sécheresse / casse_cime / coupe / chablis / phenologie / autre). Réutilise le constructeur `.field()` de `R/field_schema.R` et le mapping species de `list_species_classes()` (avec fallback texte si la config régionale est absente). (2) `generate_health_validation_plots(alerts_sf, n, method, crs)` : tirage stratifié par `confidence_class`, GRTS via `spsurvey::grts()` quand le package est disponible (repli silencieux sur tirage aléatoire intra-strate sinon, taggé dans `sampling_method`), helper `.allocate_health_strata()` qui distribue le budget `n` à la largest-remainder method en respectant la capacité par strate et garantit ≥1 placette par classe présente. Sortie sf POINT EPSG:2154 prête à passer dans `create_qfield_project()`. (3) `ingest_health_validation(con, gpkg_path, zone_id, snap_distance_m, validated_by, layer)` : lecture du GPKG, snap par plus-proche-voisin en Lambert-93 (par défaut 50 m, `reason = "no_alert_within_snap"` au-delà), mapping `stade_deperissement → (validation_status, validation_cause)` via le helper privé `.health_stade_to_status()` qui matérialise la règle ONF/DSF du rapport 2024 sur `coupe_rase × confidence_class` (1-faible / 2-moyenne → false_positive ; 3-forte / 4-sol-nu → confirmed). UPDATE atomique par alerte. Précédence `validated_by` : argument > champ `obs_by` du GPKG > `Sys.info()`. La cause libre saisie sur le terrain écrase la cause auto-mappée. Retour `list(n_updated, n_confirmed, n_false_positive, n_unmatched, n_skipped, details)` avec `details` un data.frame qui trace chaque placette (ok / no_alert_within_snap / missing_stade). Trois nouveaux test files : `test-health-validation-schema.R` (10 tests offline), `test-generate-health-validation-plots.R` (11 tests offline + mock `local_mocked_bindings(requireNamespace)` du fallback GRTS), `test-ingest-health-validation.R` (10 tests d'intégration TimescaleDB via `with_clean_db`, exécutés grâce à `NEMETON_DB_URL_TEST`). Suite : **5957 PASS / 0 FAIL** (vs 5866). Cycle dev `0.20.1.9002` → `0.20.1.9003`.
- **2026-04-30** — **E6.c.3 livré** (cœur). Trois livrables : (1) `data-raw/build_fordead_validity_zones.R` — script reproductible qui fetch les contours départementaux des 5 départements de la calibration FORDEAD (88, 39, 01, 73, 74) depuis le mirror static `gregoiredavid/france-geojson` (snapshot IGN ADMIN-EXPRESS, Etalab 2.0). Pivot par rapport au plan initial : `geo.api.gouv.fr/format=geojson&geometry=contour` ne renvoie plus le contour depuis 2025 (uniquement les attributs `nom`/`code`/`codeRegion`), donc on s'appuie sur le mirror GitHub stable. Simplification 100 m en Lambert-93 puis reprojection EPSG:4326. (2) `inst/extdata/fordead_validity_zones.geojson` — 5 features MULTIPOLYGON, ~27 500 km², 80 ko, attributs `code_dept / nom_dept / source / reference`. (3) `R/fordead_validity.R` — constantes exportées `FORDEAD_VALIDITY_DEPARTMENTS` et `FORDEAD_VALIDITY_SPECIES`, `load_fordead_validity_zones()` (cache de session), `check_fordead_validity(aoi, units, threshold_geo, threshold_species, min_resineux)` qui retourne un dict `geo_valid / geo_intersection_pct / geo_dept_codes / species_valid / species_resineux_pct / species_epc_pct / species_sap_pct / overall_valid / thresholds`. Helpers privés `.is_epicea()` et `.is_sapin_pectine()` qui résolvent proprement la collision `Picea abies` (épicéa) vs `Abies alba` (sapin pectiné) — l'épithète latin "abies" se trouve dans les deux espèces et il a fallu cabler une exclusion explicite. Le sapin de Douglas (Pseudotsuga menziesii) est aussi exclu côté SAP, conformément à la calibration ONF/DSF. 16 tests offline (`test-fordead-validity-zones.R` 4, `test-fordead-validity.R` 12). Suite : **5866 PASS / 0 FAIL** (vs 5815). Cycle dev `0.20.1.9001` → `0.20.1.9002`.
- **2026-04-30** — Hardening DB intégration : activation de `NEMETON_DB_URL_TEST` dans `.Renviron` (gitignore) → 19 tests TimescaleDB précédemment skippés se rejouent désormais à chaque `devtools::test()`. Trois échecs réels surfacés sur `list_alerts()` (`Parameter 2 does not have length 1` côté RPostgres) parce que le helper interne `add_param()` poussait des vecteurs R bruts au binding alors que RPostgres exige des paramètres scalaires. Fix `ee045f0` : nouveau helper privé `.pg_text_array()` qui sérialise un vecteur R en littéral `text[]` Postgres, et placeholder `$n::text[]` dans `WHERE x = ANY(...)`. Suite globale **5815 PASS / 0 FAIL** (vs 5745 avant — +70 PASS issus des intégrations). Cycle dev `0.20.1.9000` → `0.20.1.9001`.
- **2026-04-29** — **E6.c.2 livré** sur la branche `feat/008-fordead-postprocess` (basée sur `feat/008-fordead-pipeline`, pas mergée vers `main`, pas de release — cible E6.f). Trois grosses livraisons côté cœur. (1) Migration SQL `inst/db/migrations/0002_fordead.sql` : ajoute six colonnes au `alert` (`confidence_class`, `stress_index`, `validation_status DEFAULT 'pending'`, `validation_cause`, `validated_by`, `validated_at`) et deux index (`alert_validation_status_idx`, `alert_plot_date_type_idx`) en pleine idempotence. (2) Nouveau module `R/fordead_postprocess.R` : pipeline complet raster → centroïdes en 3 helpers (`.classify_pixels_to_classes` → `.cluster_anomaly_pixels` via `terra::patches` 8-neighbour avec `min_pixels = 5` → `.cluster_to_centroids`), enrichissement `confidence_class / stress_index / trigger_date / n_pixels / area_m2 / cluster_id`, INSERT bulk via `.insert_fordead_alerts` (TEMP staging + ON CONFLICT DO NOTHING, snap au plot le plus proche dans 200 m). (3) Garde-fous G1 et G2 implémentés et exportés : `list_alerts()` (filtre par défaut `c("3-forte", "4-sol-nu")` + filtres `validation_status` / `period` optionnels) et `classify_disturbance()` (cross-référence FORDEAD × rolling-window dans une fenêtre `± window_days`, retourne `mechanical / progressive / recent_event / NA`). Constantes exportées : `FORDEAD_CLASSES` et `FORDEAD_CONFIDENCE_WEIGHTS = c(0.10, 0.30, 0.82, 0.70)` calibrés sur le rapport ONF/DSF 2024. `run_fordead_dieback()` est maintenant câblé : `alerts_sf` est rempli avec les centroïdes ; `n_alerts_inserted` reflète l'INSERT réel quand `con` et `zone_id` sont fournis. Trois nouveaux tests d'intégration TimescaleDB skippés en l'absence de DB. Suite globale : **5745 PASS / 0 FAIL** (vs 5700 avant E6.c.2). Cycle dev `0.20.1.9000` inchangé.
- **2026-04-29** — **E6.c.1 livré** sur la branche `feat/008-fordead-pipeline` (pas encore mergée vers `main`, pas de release — cible E6.f). Trois nouveaux fichiers côté cœur : `R/fordead_python.R` (helpers reticulate idempotents : `.ensure_fordead_python`, `.use_fordead_env`, `.assert_fordead_system` avec garde Python ≥ 3.10, cache de session), `R/fordead_pipeline.R` (orchestrateur `run_fordead_dieback()` exporté, 5 phases FORDEAD via reticulate, validation arguments AOI/EPSG:2154/dates/threshold/VI, retour structuré, tryCatch global, calibration figée CRSWIR + 0.16 conformément à ADR-013), et `inst/python/requirements.txt` (versions pinnées : fordead 2.1.4, xarray, dask, rasterio, eodag, etc.). Suite de tests mockés : 8 tests reticulate (`test-fordead-python.R`) + 12 tests d'orchestration (`test-fordead-pipeline.R`) — 44 PASS, tous offline. Suite globale : **5700 PASS / 0 FAIL**. Dépendance `reticulate (>= 1.34.0)` ajoutée en `Suggests` (chargée à la 1ère utilisation seulement). Le helper `.download_or_use_cached_bd_foret()` est laissé en stub jusqu'à E6.c.3, et le post-process raster→clusters→DB attend E6.c.2. Cycle dev `0.20.1.9000` inchangé.
- **2026-04-26** — **Reframing du chantier E6** après lecture du rapport ONF/DSF *« Méthode FORDEAD — analyse de la validité des détections d'anomalies de végétation par contrôle terrain »* (Bernard & Doridant, mai 2024, 397 relevés sur Vosges/Jura/Alpes/Massif Central). Findings durs intégrés : faux positifs 50% / 1/3 sur classes 1-faible / 2-moyenne, détection précoce médiocre (60% des stades précoces ratés), confusion dépérissement / perturbation mécanique (25-41% selon altération). Décision : adopter FORDEAD comme méthode officielle de **suivi sanitaire** avec 5 garde-fous applicatifs (G1-G5) traduisant chaque limite du rapport en mécanisme code/UI. Conservation du rolling-window en mode complémentaire « Surveillance rapide ». Indicateur R5 dépérissement créé. Spec 008 (`specs/008-suivi-sanitaire/`) et ADR-013 draft rédigés. Découpage E6 actualisé : E6.b phases 2-6 (app rapide) + E6.c.1-5 (cœur FORDEAD + validation QField) + E6.d (R5) + E6.f (release v0.21.0).
