% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indice_priorite_regen.R
\name{indice_priorite_regen}
\alias{indice_priorite_regen}
\title{Regeneration priority index (spec 027 L3)}
\usage{
indice_priorite_regen(
  units,
  species = NULL,
  weights = NULL,
  tolerances = NULL,
  flag_breaks = .REGEN_FLAG_BREAKS,
  ...
)
}
\arguments{
\item{units}{An \code{sf} carrying the exposure and/or water-stress columns.}

\item{species}{Optional target-species code (\code{\link{list_species_classes}}).
\code{NULL} (default) → generic index, no species tuning.}

\item{weights}{Optional named numeric \code{c(exposition=, hydrique=)} to
override the equal weighting of the two volets.}

\item{tolerances}{Optional override of the tolerance thresholds (a
\code{data.frame} like \code{\link{regeneration_tolerances}}, or a single-species
list \code{list(tmax_tol_c=, vpd_tol_kpa=)}).}

\item{flag_breaks}{Numeric \code{c(sensible=, priorite=)} thresholds for the
boolean flags (default \code{c(50, 50)}).}

\item{...}{Unused.}
}
\value{
\code{units} with \code{indice_priorite_regen} (0-100), \code{regen_exposition}
and \code{regen_hydrique} (the two 0-100 sub-scores), \code{parcelle_sensible} and
\code{priorite} (logical flags, §7), and \code{regen_essence} (species used or
\code{"generique"}). \code{couverture_pct}, if present, is preserved.
}
\description{
Cross the \strong{microclimate exposure} (microclimf engine) and the
\strong{soil water stress} (biljouR engine) of each management unit into a
single 0-100 \strong{regeneration priority} — high = the most vulnerable
parcels, to prioritise for adaptation / regeneration (ADR-014, spec 027 v2.1).
This is a head-of-tab score, \strong{not} a radar axis.
}
\details{
The function \strong{consumes} the engine output columns already on \code{units}
(the §7 contract): exposure from \code{sensibilite} (0-100) — or derived from
\code{d_tmax}/\code{d_vpd} — and water stress from \code{njstress} / \code{istress}
/ \code{rew_min}. Each volet is a renormalised mean over the columns present, so
a partially populated \code{units} still yields a score.

By default the index is \strong{generic} (no species). Passing \code{species}
enables the \strong{optional} per-species tuning (decision §10.1): where the raw
summer heat / dryness exceeds the species tolerance
(\code{\link{regeneration_tolerances}}), the priority is pushed up (an
intolerant species on a hot, dry microsite is more urgent).
}
\examples{
\dontrun{
  units <- regen_sensibilite(units, ...)      # microclimf -> sensibilite
  units <- regen_bilan_hydrique(units, ...)   # biljouR   -> njstress, rew_min
  units <- indice_priorite_regen(units)
}
}
\seealso{
\code{\link{regeneration_tolerances}}, \code{\link{indicateur_r6_sensibilite}}
}
