% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconfort_manifest.R
\name{read_reconfort_layer}
\alias{read_reconfort_layer}
\title{Read a RECONFORT output raster, masked to the UGF zone by default (L7)}
\usage{
read_reconfort_layer(
  layer,
  con = NULL,
  zone_id = NULL,
  apply_zone_mask = TRUE,
  mask_polygon = NULL
)
}
\arguments{
\item{layer}{Either a length-1 raster path, or a single row of a
\code{\link{reconfort_layer_manifest}} data.frame (its \code{type} must be
\code{"raster"}; a \code{"vector"} row — the alert centroids — is rejected,
the vector is not masked here, see spec 021 L7 §D3).}

\item{con}{A \code{DBIConnection}, used only to resolve the zone polygon
via \code{.get_zone_aoi()} when \code{mask_polygon} is \code{NULL}. \code{NULL}
skips DB resolution.}

\item{zone_id}{Integer scalar identifying the row in \code{monitoring_zone}
(used with \code{con}).}

\item{apply_zone_mask}{If \code{TRUE} (default), mask the raster to the UGF
polygon. \code{FALSE} returns the raw raster (bbox + OSO broadleaf
extent), the pre-L7 behaviour.}

\item{mask_polygon}{An explicit \code{sf}/\code{sfc} polygon overriding the DB
lookup. When \code{NULL}, the polygon is resolved from \code{con} + \code{zone_id}.}
}
\value{
A \code{terra::SpatRaster} (masked to the UGF zone unless
\code{apply_zone_mask = FALSE} or no polygon could be resolved).
}
\description{
Reader for the raster layers of a RECONFORT run, the analogue of
\code{\link{read_fast_alert_raster}} and \code{\link{read_fordead_dieback_mask}}
(spec 016). By default it restricts the raster to the \strong{UGF zone
polygon} (pixels outside the user's managed perimeter become \code{NA}),
so RECONFORT reaches parity with the two other health pipelines and
the spatial mask lives in the \code{nemeton} core rather than in the
presentation layer (spec 021 L7, ADR-013 amendment A6).

The on-disk IOTA² rasters are not modified: the mask is applied at
\strong{read time} (spec 016 principle "mask at read, not write"). Reuses
the spec 016 helpers \code{.apply_zone_mask()} / \code{.get_zone_aoi()}.
}
\seealso{
\code{\link{reconfort_layer_manifest}}, \code{\link{run_reconfort_dieback}},
\code{\link{read_fast_alert_raster}}, \code{\link{read_fordead_dieback_mask}}
}
