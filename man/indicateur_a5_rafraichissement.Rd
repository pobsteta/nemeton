% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-air.R
\name{indicateur_a5_rafraichissement}
\alias{indicateur_a5_rafraichissement}
\title{Calculate Urban Cooling Index (A5)}
\usage{
indicateur_a5_rafraichissement(
  units,
  lst = NULL,
  reference = NULL,
  buffer_m = 500,
  delta_scale = 5,
  ...
)
}
\arguments{
\item{units}{An sf object with the tree / forest units.}

\item{lst}{A terra SpatRaster of Land Surface Temperature (K or °C). \code{NULL}
(default) -> the indicator is not applicable and \code{A5 = NA} for every unit
(source-conditional, like A3/A4 without a microclimate model).}

\item{reference}{Optional numeric. A fixed reference temperature (same unit as
\code{lst}). \code{NULL} (default) -> a per-unit local reference is used: the
median LST of a ring around the unit (\code{buffer_m}).}

\item{buffer_m}{Numeric. Radius (m) of the local-reference ring around each
unit. Default 500.}

\item{delta_scale}{Numeric. Temperature difference (K/°C) mapped to the full
score swing: a unit \code{delta_scale} cooler than its reference scores 100,
\code{delta_scale} hotter scores 0, equal scores 50. Default 5.}

\item{...}{Unused.}
}
\value{
\code{units} with \code{A5} (0-100, high = cooler than surroundings),
\code{A5_delta} (raw reference − unit LST) and \code{a5_status}.
\code{A5 = NA} where \code{lst} is \code{NULL}, the unit does not overlap the
raster, or no local reference is available.

\code{a5_status} is one of \code{"calculated"}, \code{"skipped_no_lst"} (no
LST raster supplied) or \code{"skipped_no_reference"} (raster supplied but no
unit could be scored --- no overlap, or no local reference). It mirrors
\code{r5_status} and exists so that a consumer can tell an empty indicator
apart from a broken one: outside Thermocity coverage \code{A5 = NA} is the
correct answer, not a failure.
}
\description{
Relative surface-temperature freshness of a forest / tree unit compared with
its local surroundings, from a Land Surface Temperature (LST) raster (e.g.
Theia Thermocity ECOSTRESS/ASTER). High = the unit is markedly \strong{cooler}
than its surroundings — the cooling service trees provide in an urban
heat-island context.
}
\details{
Scope (spec 032, reoriented): this indicator targets the \strong{urban tree /
forest-city interface}, precisely where an LST product is available. Over rural
forests, where no LST covers the area, it is left \code{NA}. Surface
\strong{albedo is deliberately NOT used}: for a tree it is not a valid cooling
proxy (cooling comes from shade + evapotranspiration; canopy albedo is low and
second-order), so LST — the direct temperature signal — is the only physically
sound basis.

The score is a \strong{relative} freshness: the unit's mean LST is compared
with a local reference (median LST of a surrounding ring, or a supplied
\code{reference}). Differences are scale-invariant between kelvin and celsius,
so either unit works.
}
