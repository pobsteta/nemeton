% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tendances_eobs.R
\name{tendances_estivales_eobs}
\alias{tendances_estivales_eobs}
\title{Summer E-OBS climate trends over the project area (spec 027 §6, branch A)}
\usage{
tendances_estivales_eobs(
  aoi,
  tx = NULL,
  rr = NULL,
  years = NULL,
  buffer_m = 25000,
  breaks = NULL,
  precomputed = NULL,
  ...
)
}
\arguments{
\item{aoi}{An \code{sf}/\code{sfc} of the management units (their union is buffered).}

\item{tx}{Per-year summer maximum-temperature \code{SpatRaster} (one layer per
year). Engine path.}

\item{rr}{Per-year summer precipitation \code{SpatRaster} (one layer per year).}

\item{years}{Optional numeric years matching the raster layers (default
\code{seq_len(nlyr)}).}

\item{buffer_m}{Numeric buffer radius in metres around the units. Default
\code{25000} (§10.4).}

\item{breaks}{Optional \code{list(tmax=, precip=)} of two cut points each for a
fixed classification; \code{NULL} → tertiles.}

\item{precomputed}{Optional pre-built trends: an \code{sf} with \code{trend_tmax}
/ \code{trend_precip}, or a 2-layer \code{SpatRaster} named
\code{trend_tmax}/\code{trend_precip}.}

\item{...}{Reserved.}
}
\value{
An \code{sf} of E-OBS cell-centre points within the buffered area, with
\code{trend_tmax}, \code{trend_precip}, \code{classe_tmax}, \code{classe_precip}
(1-3) and \code{classe_bivariee} (1-9).
}
\description{
Per-cell \strong{summer trend} of maximum temperature and precipitation from
the \strong{E-OBS} grid, over the \strong{union of the units plus a buffer}
(decision §10.4, default 25 km — \emph{not} national), with a \strong{bivariate
classification} (warming × drying) for the reGénération context map (spec 027
branch A). The core computes the trends and classes; the app renders the
bivariate map.
}
\details{
Trends are the least-squares slope of the per-year summer values against the
year. Classes are tertiles by default (data-driven over the cropped area), or
fixed \code{breaks}. \code{classe_bivariee} runs 1-9 with
\code{(classe_tmax - 1) * 3 + classe_precip}; "hot & dry" is
\code{classe_tmax == 3 & classe_precip == 1}.

\strong{Data path & degradation}: E-OBS NetCDF is external (research, non
commercial). Supply \code{tx} / \code{rr} as per-year summer \code{SpatRaster}s
to compute the trends, or a \code{precomputed} result to only crop + classify.
With neither, the function fails cleanly.
}
\seealso{
\code{\link{indice_priorite_regen}}
}
