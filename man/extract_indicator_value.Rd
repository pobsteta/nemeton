% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-core.R
\name{extract_indicator_value}
\alias{extract_indicator_value}
\title{Extract an indicator's value column from its result}
\usage{
extract_indicator_value(result, indicator, exclude = character(0))
}
\arguments{
\item{result}{The raw return value of an indicator function (an \code{sf},
a \code{data.frame}, or a numeric vector).}

\item{indicator}{Character. The NMT indicator name (function name),
e.g. \code{"indicateur_p1_volume"}.}

\item{exclude}{Character vector of column names to treat as
pre-existing (not the freshly computed value) when falling back to
the \code{"<Letter><digit>"} pattern. Default none.}
}
\value{
A numeric vector of the indicator's per-unit values.
}
\description{
Single source of truth for the Nemeton indicator naming convention:
most indicator functions return the \code{units} object (an \code{sf} /
\code{data.frame}) with the computed value added under a column named by the
family short code (\code{indicateur_p1_volume} -> \code{"P1"},
\code{indicateur_r1_feu} -> \code{"R1"}, ...). This helper resolves that column to
a plain numeric vector so both the core dispatcher
(\code{\link{nemeton_compute}} via \code{compute_indicator()}) and downstream callers
(e.g. the \code{nemetonshiny} compute loop) share \strong{one} convention and can
never drift apart.
}
\details{
Resolution order for an \code{sf} / \code{data.frame} result:
\enumerate{
  \item the short code derived from the indicator name
    (\code{indicateur_<code>_...} -> upper-case \code{<code>}, e.g. \code{P1});
  \item the NMT indicator name itself (or its upper-case form);
  \item any single \code{"<Letter><digit>"} column (optionally suffixed
    \code{_norm}), preferring columns \strong{not} present in \code{exclude}
    (pass the pre-existing input column names so a freshly added value
    column wins over a same-shaped attribute already on the units).
}
A result that is already a plain vector is returned unchanged.
}
\seealso{
\code{\link{nemeton_compute}}
}
