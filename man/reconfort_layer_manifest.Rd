% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconfort_manifest.R
\name{reconfort_layer_manifest}
\alias{reconfort_layer_manifest}
\title{RECONFORT layer manifest: describe a run's displayable outputs (L6)}
\usage{
reconfort_layer_manifest(result, include_range = FALSE)
}
\arguments{
\item{result}{The list returned by \code{\link{run_reconfort_dieback}}. Only
\code{result$rasters} (a named list of output paths, see
\code{\link{run_reconfort_dieback}}) and, optionally, \code{result$alerts_sf} /
\code{result$n_alerts} are read.}

\item{include_range}{If \code{TRUE}, override the nominal \code{vmin}/\code{vmax} of
the continuous rasters with the actual min/max of their displayed
band (\code{terra::minmax(compute = TRUE)}). Default \code{FALSE} (nominal
domains).}
}
\value{
A \code{data.frame} with one row per available layer and the
columns:
\describe{
\item{id}{Stable layer key (\code{"score"}, \code{"classification"},
\code{"probability"}, \code{"alerts"}).}
\item{label_key}{i18n key (NMT convention) for the display label,
resolved by the viewer.}
\item{type}{\code{"raster"} or \code{"vector"}.}
\item{role}{Semantic role, equal to \code{id} here but kept distinct so
future variants (e.g. masked vs raw) can share a role.}
\item{path}{Raster file path, or \code{NA} for the vector layer.}
\item{categorical}{\code{TRUE} for the classification raster, \code{FALSE}
for the continuous rasters, \code{NA} for the vector.}
\item{palette}{Suggested palette name for continuous rasters
(\code{"RdYlGn"}, \code{"viridis"}), \code{NA} where the viewer should use the
discrete \code{\link{RECONFORT_CLASSES}} colours or its own marker style.}
\item{reverse}{Whether the palette runs high-to-low (the score is
reversed so high = severe = red).}
\item{vmin, vmax}{Value domain for the colour scale (\code{NA} for the
categorical and vector layers).}
\item{default_visible}{Whether the viewer should show the layer on
first render (score and alerts on; the rest off).}
\item{default_opacity}{Suggested initial opacity in \code{0..1}.}
\item{n_features}{Alert feature count for the vector layer,
\code{NA} for rasters.}
}
When the run produced no displayable output the data.frame has zero
rows (the columns and their types are still present).
}
\description{
Translates the result of \code{\link{run_reconfort_dieback}} into a flat
\code{data.frame} describing the map layers a RECONFORT run exposes — the
continuous dieback \strong{score}, the per-pixel \strong{classification}, the
\strong{probability} map, and the \strong{alert} centroids — together with the
rendering hints a viewer needs (palette, direction, value domain,
default visibility and opacity).

The semantics of a RECONFORT output (what a layer \emph{is}, how its values
are scaled, which way the palette runs) are business knowledge and
therefore live in the \code{nemeton} core. A presentation layer
(e.g. \code{nemetonshiny}) consumes this manifest verbatim to build its
layer toggles and opacity control, without hard-coding any RECONFORT
semantics of its own (ADR-009, CLAUDE.md strict rules §1-3).

Only \strong{available} layers are returned: a raster whose path is \code{NA}
(the masked variants only exist once masking ran) is skipped, and the
alert row appears only when the run produced at least one alert. For
the classification and probability layers the masked variant is
preferred and the raw one is used as a fallback.

Value domains are \emph{nominal} by default (score \code{1..100}, probability
\code{0..1000}). Pass \code{include_range = TRUE} to replace them with the actual
min/max of the displayed band, computed by \pkg{terra} even when the
GeoTIFF stores no statistics (the IOTA² case; best-effort: a read
failure or an all-NA raster keeps the nominal domain).

The IOTA² probability map has \strong{one band per class} in the order of
\code{\link{RECONFORT_CLASSES}} (healthy, dieback[, severe]), on a \code{0..1000}
scale. Since v0.199.0 the \code{probability} row describes \strong{P(atteinte)},
the probability that the pixel is affected: the sum of bands \code{2..n}
(dieback + severe; dieback alone for pine), \code{0..1000}, high = bad. It
is derived once into a single-band \verb{p_atteinte_<source>.tif} next to
the source (rebuilt when the source is newer) and \code{path} points to it.
A probability map clamped at 255 (runs made before the iota2 #12
repair, see \code{inst/python/reconfort/repair_iota2_env.sh}) is reported
once per session: its continuous score is compressed too, the run must
be re-run.
}
\seealso{
\code{\link{run_reconfort_dieback}}, \code{\link{RECONFORT_CLASSES}}
}
