% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fordead_pixel_series.R
\name{.crswir_stack_dates}
\alias{.crswir_stack_dates}
\title{Observation dates of a persisted CRSWIR stack}
\usage{
.crswir_stack_dates(r)
}
\arguments{
\item{r}{A `terra::SpatRaster` (the loaded CRSWIR stack).}
}
\value{
A `Date` vector of length `terra::nlyr(r)`.
}
\description{
Reads the per-band dates of a `crswir_stack.tif` written by
[.write_fordead_model_bundle()]. `terra::time()` is the primary
source; the `YYYY-MM-DD` layer names are the fallback (both are
set at write time, but `time()` does not always survive a GeoTIFF
round-trip).
}
\keyword{internal}
