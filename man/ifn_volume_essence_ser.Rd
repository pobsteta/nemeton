% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_volume_ser.R
\name{ifn_volume_essence_ser}
\alias{ifn_volume_essence_ser}
\title{IFN standing volume by species and sylvoecoregion}
\usage{
ifn_volume_essence_ser(espar = NULL, ser = NULL, greco = NULL, niveau = NULL)
}
\arguments{
\item{espar}{Optional IFN species code filter (character, e.g. \code{"09"} for
beech). Note these are the IFN's own codes, not the four-letter codes of
\code{\link{indicateur_p1_volume}}.}

\item{ser}{Optional SER code filter (e.g. \code{"C20"}).}

\item{greco}{Optional GRECO code filter (single letter, e.g. \code{"C"}).}

\item{niveau}{Optional level filter: \code{"ser"}, \code{"greco"} or
\code{"national"}.}
}
\value{
A data.frame with \code{niveau}, \code{ser}, \code{greco}, \code{espar},
\code{n_plac_presence}, \code{n_plac_maille}, \code{vol_ha_present},
\code{vol_ha_maille}, \code{taux_presence}, \code{libelle_essence},
\code{millesime}, \code{source}.
}
\description{
Reference table of standing timber volume per hectare, by IFN species code and
by sylvoecoregion (SER), derived from the raw IFN tree and plot data
(campaigns 2005-2019: 1 015 606 living trees over 93 043 plots).

Three nested levels live in the same table, selected by \code{niveau}:
\code{"ser"} (86 sylvoecoregions), \code{"greco"} (the SER code's first letter
— the greater ecological region) and \code{"national"}. Use
\code{\link{ifn_volume_reference}} to walk them as a fallback ladder.

Two volume columns, deliberately both kept:
\itemize{
\item \code{vol_ha_present} — mean volume per hectare \strong{over the plots
where the species occurs}. This is a stand-level figure, the one comparable to
a P1 computed on a forest unit of that species.
\item \code{vol_ha_maille} — the species' contribution to the mesh's mean
volume per hectare, averaged over \strong{all} its plots. This is a regional
resource figure; it is much lower for a scattered species.
}
}
\section{Standing volume, not harvest}{

These figures are \strong{standing stock}. The IFN harvest estimate comes from
the five-year revisit, which is not part of this dataset — so this table does
\strong{not} supply the harvest rate of \code{\link{volume_mobilisable}}
(spec 040, D4).
}

\section{Sampling depth}{

\code{n_plac_presence} (plots carrying the species) grades reliability and is
not decorative: of the 4 639 species x SER cells, only 1 193 rest on 30 plots
or more and 2 107 rest on fewer than 5. Filter on it, or use
\code{\link{ifn_volume_reference}}, which does.
}

\examples{
\dontrun{
# Beech across all sylvoecoregions, best-sampled first.
b <- ifn_volume_essence_ser(espar = "09", niveau = "ser")
head(b[order(-b$n_plac_presence), c("ser", "n_plac_presence", "vol_ha_present")])
}
}
\references{
Data and aggregation method from the \code{DataForet} and \code{PPtools}
packages by Max Bruciamacchie (AgroParisTech Nancy), GPL-2, reused under GPL-3
with explicit permission. Method after \code{PPtools::CarteEssenceSer()}.
}
\seealso{
\code{\link{ifn_volume_reference}} for the fallback ladder.
}
