% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-microclimate.R
\name{indicateur_r6_sensibilite}
\alias{indicateur_r6_sensibilite}
\title{R6 — microsite climate sensitivity (heatwave vs average year)}
\usage{
indicateur_r6_sensibilite(units, micro_moyenne = NULL, micro_canicule = NULL,
  bounds = .MICRO_BOUNDS$r6, ...)
}
\arguments{
\item{units}{An \code{sf} of UGF.}

\item{micro_moyenne}{Summer microclimate rasters for the \strong{average} year
(needs \code{tmax_understorey}, \code{vpd}).}

\item{micro_canicule}{Summer microclimate rasters for the \strong{heatwave}
year (same layers). \code{NULL}/missing layers → \code{R6 = NA}.}

\item{bounds}{Numeric \code{c(scale_t, scale_v)} standardisation scales
(default \code{c(8, 2)} — ΔT in °C, ΔVPD in kPa).}

\item{...}{Unused.}
}
\value{
\code{units} with \code{R6} (0-100, higher = less sensitive), \code{R6_dtmax}
(raw ΔT°max, °C), \code{R6_dvpd} (raw ΔVPD, kPa), \code{R6_couverture_pct}, and
the \code{"microclimate_model"} augmentation flag.
}
\description{
Per-UGF \strong{sensitivity} of the under-canopy microsite to a hot year
(spec 027 L2, ADR-014): the change in summer heat stress between a
\strong{heatwave} year and an \strong{average} year, with the canopy held fixed
(isolates the climatic effect). Combines the standardised ΔT°max and
ΔVPD; normalised 0-100 \strong{decreasing} (less sensitive = more resilient =
100).

The two years are chosen by the caller — typically auto-detected from
the E-OBS summer series via \code{\link{microclimate_detect_years}}, or set
manually. This function consumes the \strong{two precomputed \code{micro} sets}
(one per year), each carrying \code{tmax_understorey} and \code{vpd}.
}
\seealso{
\code{\link{microclimate_detect_years}}, \code{\link{indicateur_a3_microclimat}}
}
