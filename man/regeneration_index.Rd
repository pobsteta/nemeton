% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regeneration_index.R
\name{regeneration_index}
\alias{regeneration_index}
\title{Composite regeneration potential (spec 027 L3)}
\usage{
regeneration_index(
  units,
  species = NULL,
  weights = NULL,
  tolerances = NULL,
  class_breaks = .REGEN_CLASS_BREAKS,
  ...
)
}
\arguments{
\item{units}{An \code{sf} of UGF, already carrying the sub-indicator columns
from \code{\link{indicateur_a3_microclimat}}, \code{\link{indicateur_a4_tamponnement}},
\code{\link{indicateur_w4_vpd}} and/or \code{\link{indicateur_r6_sensibilite}}. Absent
components are simply skipped; if none are present the score is \code{NA}.}

\item{species}{Optional target-species code (one of
\code{\link{list_species_classes}}\code{$code}). \code{NULL} → generic equiponderated
score, no tolerance penalty.}

\item{weights}{Optional named numeric over \code{c("A3","A4","W4","R6")} to
override the default equal weighting.}

\item{tolerances}{Optional override of the tolerance thresholds: a
\code{data.frame} like \code{\link{regeneration_tolerances}}, or a single-species list
\code{list(tmax_tol_c=, vpd_tol_kpa=)}. \code{NULL} → the packaged table.}

\item{class_breaks}{Numeric \code{c(defavorable, favorable)} cut points on the
0-100 potential (default \code{c(33, 66)}).}

\item{...}{Unused (signature harmonisation).}
}
\value{
\code{units} with \code{regeneration_potentiel} (0-100, rounded),
\code{regeneration_classe} (\code{"favorable"} / \code{"marginal"} / \code{"defavorable"},
NMT, \code{NA} where the potential is \code{NA}) and \code{regeneration_essence}
(the species used, or \code{"generique"}).
}
\description{
Combine the four under-canopy microclimate sub-indicators
(A3 T°max, A4 buffering, W4 VPD, R6 sensitivity) into a single 0-100
\strong{regeneration potential} for a target species (ADR-014). This is a
head-of-tab score, \strong{not} a radar axis; the four sub-indicators keep
their own axes (families A, W, R).
}
\details{
The base score is a weighted mean of whichever sub-indicator score columns
are present on \code{units} (weights renormalised over the available, non-NA
components, so a missing R6 does not void the score). When a target
\code{species} is given, a heat / dryness \strong{tolerance penalty} is applied
from the raw \code{A3_tmax} / \code{W4_vpd} columns: a parcel whose under-canopy
summer T°max or VPD exceeds the species threshold (see
\code{\link{regeneration_tolerances}}) is down-weighted, so the same microsite
scores higher for a thermophilic species than for a mesophilic one.
}
\examples{
\dontrun{
  units <- indicateur_a3_microclimat(units, micro)
  units <- indicateur_w4_vpd(units, micro)
  units <- regeneration_index(units, species = "essence_hetraie")
}
}
\seealso{
\code{\link{regeneration_tolerances}}, \code{\link{indicateur_a3_microclimat}},
\code{\link{indicateur_w4_vpd}}
}
