% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-temporal.R
\name{indicateur_t3_coupes_rases}
\alias{indicateur_t3_coupes_rases}
\title{Calculate Clear-cut Pressure Index (T3)}
\usage{
indicateur_t3_coupes_rases(
  units,
  sufosat_dates = NULL,
  sufosat_proba = NULL,
  window_years = 5L,
  min_proba = 0.9,
  reference_year = NULL
)
}
\arguments{
\item{units}{An sf object with forest units.}

\item{sufosat_dates}{A terra SpatRaster of clear-cut dates (\code{YYDDD}).
\code{NULL} (default) -> the indicator is not applicable and \code{NA} is
returned for every unit (source-conditional, like R5 without FORDEAD).}

\item{sufosat_proba}{A terra SpatRaster of clear-cut probability (percent).
\code{NULL} -> no probability filter is applied.}

\item{window_years}{Integer. Length of the recency window in years. Default
\code{5}. Clear-cuts older than \code{reference_year - window_years + 1} are
ignored.}

\item{min_proba}{Numeric in [0, 1]. Minimum clear-cut probability to count a
pixel, compared against \code{sufosat_proba / 100}. Default \code{0.9}.}

\item{reference_year}{Integer or \code{NULL}. Most-recent year of the recency
window (weight 1). \code{NULL} (default) -> derived from the most recent
clear-cut year found across the extracted units.}
}
\value{
Numeric vector, one value per unit: recency-weighted percentage of the unit
footprint under clear-cut within the window (0-100, high = more
clear-cutting). \code{NA} where \code{sufosat_dates} is \code{NULL} or the
unit does not overlap the raster.
}
\description{
Recency-weighted fraction of a forest unit affected by clear-cuts, derived
from the SUFOSAT national product (CNES/CESBIO; Sentinel-1 radar change
detection, submonthly, mainland France). High = more recent clear-cutting.
This is a \dQuote{high = bad} indicator (like R5 dieback): its normalized
radar value is inverted downstream (see \code{normalization.R}).
}
\details{
SUFOSAT rasters (spec 030, band metadata confirmed on the live Theia MTD
STAC 2026-07-02):
\itemize{
  \item \code{sufosat_dates}: clear-cut date per pixel, encoded \code{YYDDD}
    (YY = year 18-25 -> 2018-2025, DDD = day of year 1-366). \code{0} = no
    clear-cut (nodata).
  \item \code{sufosat_proba}: clear-cut probability in percent (0-100).
    \code{0} = nodata. SUFOSAT publishes detections at >= ~85\%.
}

The score is coverage-fraction weighted, so equal-area pixels cancel and no
cell-area computation is needed: it is the share of the unit footprint under
clear-cut within the recency window, weighted linearly by recency.
}
