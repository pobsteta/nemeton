% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_foret_ancienne.R
\name{load_foret_ancienne_source}
\alias{load_foret_ancienne_source}
\title{Acquire the IGN historical-forest (~1850 état-major) layer for an AOI}
\usage{
load_foret_ancienne_source(
  aoi,
  crs = 2154,
  layer = "BDCARTO_ETAT-MAJOR.NIVEAU3:c_1_1_ocs_ancien",
  ...
)
}
\arguments{
\item{aoi}{An \pkg{sf}/\pkg{sfc} project extent (must have a defined CRS).}

\item{crs}{Target EPSG of the returned layer. Default \code{2154}.}

\item{layer}{WFS layer id. Defaults to the état-major forest layer.}

\item{...}{Passed to \code{\link[happign]{get_wfs}}.}
}
\value{
An \pkg{sf} of ancient-forest polygons (\code{foret_ancienne = TRUE}), clipped
to \code{aoi}, in \code{crs}; a 0-row \pkg{sf} if none; \code{NULL} on failure.
}
\description{
Fetch the ~1850 forest cover from the IGN \emph{carte de l'état-major}
land-cover layer (BD Carto État-Major,
\code{BDCARTO_ETAT-MAJOR.NIVEAU3:c_1_1_ocs_ancien}, Licence Ouverte /
Etalab 2.0) over \code{aoi} via WFS (\pkg{happign}), and return it as the
\code{foret_ancienne} polygon layer consumed by
\code{\link{indicateur_n2_continuite}}.

This is the \strong{historical forest cover} ingredient (forested ~1850): N2
intersects it with the current units, so forest \strong{continuity} ("forest
then \emph{and} now") emerges from the indicator itself. It is \strong{not}
the finished IGN \emph{BD Forêts anciennes} product (état-major × BD Forêt v2,
\code{Nature} classification), which is download-only (departmental
GeoPackage) and not exposed via WFS.

Degrades gracefully — returns \code{NULL} on any failure (no network,
\pkg{happign} absent, WFS error, AOI outside metropolitan France) so the
caller can fall back to current-cover N2. A 0-row \pkg{sf} is returned when
the AOI simply has no ~1850 forest.
}
\seealso{
\code{\link{indicateur_n2_continuite}}, \code{\link{build_foret_ancienne_mask}}
}
