% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-frost.R
\name{indicateur_r7_gel}
\alias{indicateur_r7_gel}
\title{Late-frost risk indicator (R7)}
\usage{
indicateur_r7_gel(
  units,
  tmin = NULL,
  budburst_doy = 100,
  window_end_doy = 180,
  frost_threshold_c = 0,
  max_frost_days = 8,
  ...
)
}
\arguments{
\item{units}{An \code{sf} of management units.}

\item{tmin}{A daily minimum-temperature \code{SpatRaster} (one layer per day, dates
via \code{terra::time()}), or \code{NULL} (→ skip). Multi-year series are supported.}

\item{budburst_doy}{Day-of-year of budburst; frosts before it don't count
(default 100 ≈ 10 April).}

\item{window_end_doy}{Last day-of-year of the sensitivity window (default 180
≈ end June).}

\item{frost_threshold_c}{Tmin threshold (°C) below which a day is a frost
(default 0).}

\item{max_frost_days}{Late-frost days per year mapping to score 0 (default 8).}

\item{...}{Ignored (forward-compat).}
}
\value{
\code{units} with \code{R7} (0–100, high = low frost risk, \code{NA} when skipped),
\code{r7_gel_days} (mean late-frost days/year), and \code{r7_status}
(\code{"calculated"} / \code{"skipped_no_tmin"}).
}
\description{
Per-unit \strong{late-frost risk} — the frequency of spring frost \strong{after
budburst}, the dominant cause of regeneration failure for frost-sensitive
species (oak, beech, Douglas fir). Fed by a daily \strong{minimum temperature}
series (\code{tmin}), typically the meteoland/SAFRAN downscaling of the microclimat
chantier (P4); it also accepts a direct daily Tmin raster.
}
\details{
Conditional like R5 (FORDEAD) and R6 (microclimf): without \code{tmin}, \code{R7} is
\code{NA} and \code{r7_status = "skipped_no_tmin"}. Sense follows R1–R4/R6 — \strong{high
score = low risk} (few late frosts); it is \strong{not} inverted (unlike R5).

A unit's mean number of late-frost days per year (Tmin below
\code{frost_threshold_c}, on days after \code{budburst_doy} up to \code{window_end_doy}) is
mapped to 0–100: \code{0} days → \code{100}, \code{max_frost_days} or more → \code{0}.
}
\seealso{
\code{\link{indicateur_r6_sensibilite}}, \code{\link{eobs_downscale}}
}
