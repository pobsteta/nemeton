% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_tables.R
\name{ifn_taux_prelevement}
\alias{ifn_taux_prelevement}
\title{Reference harvest rate, with a SER -> GRECO -> national fallback}
\usage{
ifn_taux_prelevement(espar, ser = NULL, min_plac = 30,
  mesure = c("maille", "present"))
}
\arguments{
\item{espar}{IFN species code(s), character.}

\item{ser}{SER code, a single string. \code{NULL} starts at the national
level.}

\item{min_plac}{Minimum plots carrying the species for a level to qualify.
Default \code{30}.}

\item{mesure}{\code{"maille"} (default) returns the rate per hectare of forest
in the mesh — the figure whose national sum matches published harvest.
\code{"present"} returns the rate over the plots where the species occurs,
which is much higher for a clear-felled species such as poplar.}
}
\value{
A data.frame with one row per \code{espar}: \code{espar},
\code{libelle_essence}, \code{taux_m3_ha_an}, \code{niveau_utilise},
\code{n_plac_presence}, \code{ser}, \code{greco}.
}
\description{
Returns one harvest rate per requested species, in \strong{m3/ha/year}, walking
the mesh down until the cell rests on enough plots and reporting the level
actually used. This is the rate \code{\link{volume_mobilisable}} consumes.
}
\examples{
\dontrun{
ifn_taux_prelevement(c("62", "09"), ser = "C20")
}
}
\seealso{
\code{\link{ifn_prelevement_essence_ser}}, \code{\link{volume_mobilisable}}.
}
