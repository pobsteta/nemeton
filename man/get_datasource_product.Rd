% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasources.R
\name{get_datasource_product}
\alias{get_datasource_product}
\title{Get a sub-product of a multi-product datasource}
\usage{
get_datasource_product(source_key, product, country = "FR")
}
\arguments{
\item{source_key}{Character. The datasource key (e.g.,
\code{"forms_t"}).}

\item{product}{Character. The sub-product name (e.g.,
\code{"height"}).}

\item{country}{Character. ISO country code. Default \code{"FR"}.}
}
\value{
A list with the sub-product configuration.
}
\description{
Several datasources (e.g. the Theia products \code{forms_t},
\code{theia_soil}, \code{theia_snow}) bundle more than one raster
product under a \code{products} block — for \code{forms_t}, the
\code{height}, \code{volume} and \code{biomass} sub-products. This
helper returns the metadata of one named sub-product (resolution,
unit, value range, conversion notes), so a caller can pick the
right product and apply any documented unit conversion before
feeding it to an indicator.
}
\examples{
h <- get_datasource_product("forms_t", "height", "FR")
h$unit          # "cm"
h$resolution_m  # 10

}
