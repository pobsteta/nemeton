% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eobs_downscale.R
\name{meteoland_daily_grid}
\alias{meteoland_daily_grid}
\title{Downscale a daily meteoland variable to a fine raster stack}
\usage{
meteoland_daily_grid(
  aoi,
  dem,
  years,
  variable = "MinTemperature",
  dates = NULL,
  doy_range = c(60L, 180L),
  buffer_m = 25000,
  max_cells = 5e5,
  min_stations = 5L,
  calibrate = FALSE,
  ...
)
}
\arguments{
\item{aoi}{An \code{sf}/\code{sfc} of the management units.}

\item{dem}{A \code{SpatRaster} DEM — the target grid; the output shares its CRS.}

\item{years}{Integer year(s) of the SAFRAN series to interpolate.}

\item{variable}{meteoland variable to return (default \code{"MinTemperature"};
also \code{"MaxTemperature"}, \code{"MeanTemperature"}, \code{"Precipitation"}, …).}

\item{dates}{Optional explicit \code{Date} vector; when \code{NULL}, the spring window
(day-of-year \code{doy_range}) of each year is used — the season that matters for
late frost.}

\item{doy_range}{Integer length-2 day-of-year window used when \code{dates} is
\code{NULL} (default \code{c(60, 180)} ≈ 1 March–end June).}

\item{buffer_m}{Context buffer (metres) for sampling SAFRAN stations
(default 25000).}

\item{max_cells}{Cell cap for the target grid (default \code{5e5}).}

\item{min_stations}{Minimum SAFRAN pseudo-stations required (default 5).}

\item{calibrate}{Run the (expensive) meteoland LOO calibration first
(default \code{FALSE}).}

\item{...}{Ignored (forward-compat).}
}
\value{
A daily \code{SpatRaster} (one layer per interpolated day, \code{terra::time()}
set, DEM CRS), ready to pass as \code{tmin =} to \code{\link{indicateur_r7_gel}}; or
\code{NULL} when unavailable.
}
\description{
Interpolate a \strong{daily} meteorological variable (e.g. daily minimum
temperature) from SAFRAN pseudo-stations onto the DEM grid with
\pkg{meteoland} (Thornton 1997 + elevation), returning a \code{SpatRaster} with one
layer per day and \code{terra::time()} set. This is the real-data feeder for
\code{\link{indicateur_r7_gel}} (late-frost risk needs a daily Tmin series) and for
any downstream daily-climate use; the reduced-statistic context map goes through
\code{\link{eobs_downscale}} instead.

meteoland lives in Suggests and is \strong{absent from CI} — like microclimf, this
path is validated on real data. Any failure (package absent, GéoSAS down, too
few stations) returns \code{NULL}, never an error, so callers degrade gracefully.
}
\seealso{
\code{\link{eobs_downscale}}, \code{\link{indicateur_r7_gel}}, \code{\link{build_safran_stations}}
}
