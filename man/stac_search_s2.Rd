% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinel2.R
\name{stac_search_s2}
\alias{stac_search_s2}
\title{Search Sentinel-2 L2A scenes via STAC}
\usage{
stac_search_s2(
  zone,
  start,
  end,
  max_cloud = 20,
  source = c("cdse", "pc"),
  limit = 100L
)
}
\arguments{
\item{zone}{An sf or sfc object covering the area of interest.
Re-projected to WGS84 internally.}

\item{start, end}{Date or character `"YYYY-MM-DD"`.}

\item{max_cloud}{Numeric. Maximum scene cloud cover in percent.
Default 20.}

\item{source}{Character vector. Order in which to try backends.
Default `c("cdse", "pc")`.}

\item{limit}{Integer. Maximum number of scenes to return per
backend call. Default 100.}
}
\value{
A tibble with columns `scene_id`, `obs_date`,
  `cloud_pct`, `href_B04`, `href_B08`, `href_B12`, `source`. Empty
  tibble (0 rows) when no scene matches.
}
\description{
Façade around CDSE (priority) and Planetary Computer (fallback).
Returns a tibble with one row per scene, holding the COG hrefs for
bands B04, B08, B12 (used to derive NDVI and NBR downstream).
}
\examples{
\dontrun{
library(sf)
aoi <- st_as_sfc(st_bbox(c(xmin = 4.0, ymin = 47.5,
                           xmax = 4.5, ymax = 48.0), crs = 4326))
scenes <- stac_search_s2(aoi, "2025-06-01", "2025-09-30")
}

}
