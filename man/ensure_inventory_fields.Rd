% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_inventory.R
\name{ensure_inventory_fields}
\alias{ensure_inventory_fields}
\title{Fill in missing inventory fields from a CHM}
\usage{
ensure_inventory_fields(
  units,
  species_field = "species",
  dbh_field = "dbh",
  density_field = "density",
  chm = NULL,
  stocking = 0.75,
  h_dom_percentile = 0.9,
  min_stand_height = 1.3,
  min_merchantable_height = 6
)
}
\arguments{
\item{units}{sf object.}

\item{species_field}{Character. Column holding species codes
(default "species").}

\item{dbh_field}{Character. Column expected to hold \eqn{D_g}
(default "dbh").}

\item{density_field}{Character. Column expected to hold stems / ha
(default "density").}

\item{chm}{Optional \code{SpatRaster} CHM. When \code{NULL}, the
function is a no-op.}

\item{stocking}{Stocking fraction (see
\code{\link{estimate_synthetic_inventory}}).}

\item{h_dom_percentile}{Percentile for \eqn{H_{dom}} extraction.}

\item{min_stand_height}{Numeric (m). Felled/cleared stand threshold;
see \code{\link{estimate_synthetic_inventory}}. Default \code{1.3}.}

\item{min_merchantable_height}{Numeric (m). Dominant height below
which a unit carries no merchantable stock (\code{dbh} /
\code{density} filled with \code{0}, not \code{NA}); see
\code{\link{estimate_synthetic_inventory}}. Default \code{6}. The
\code{chm_suspect} attribute is propagated onto the returned
\code{sf}.}
}
\value{
The input \code{sf} with \code{dbh_field} and
  \code{density_field} filled (when possible). The
  \code{inventory_source} attribute is set to "synthetic_ml" iff
  at least one field was filled from the CHM.
}
\description{
Convenience wrapper around
\code{\link{estimate_synthetic_inventory}} that mutates an
\code{sf} in place: fills the \code{dbh_field} / \code{density_field}
columns only when they are absent or fully \code{NA}, leaves any
partial user-provided values intact, and tags the result with the
attribute \code{inventory_source = "synthetic_ml"} when
substitution actually occurred. Designed to be called at the top
of any indicator function that depends on \eqn{D_g} and \eqn{N}.
}
\keyword{internal}
