% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconfort_ingest.R
\name{reconfort_ingest_s2}
\alias{reconfort_ingest_s2}
\title{Acquire Sentinel-2 scenes for an AOI into the IOTA² layout}
\usage{
reconfort_ingest_s2(
  aoi = NULL,
  tiles = NULL,
  date_from,
  date_to,
  s2_root,
  geodes_config = NULL,
  s2_collection = "THEIA_REFLECTANCE_SENTINEL2_L2A",
  target_crs = 2154,
  buffer_m = .RECONFORT_AOI_BUFFER_M,
  keep_zips = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{aoi}{An `sf`/`sfc` AOI. When supplied, scenes are streamed and
clipped to the AOI window (production path); also used to resolve
`tiles` when those are `NULL`. When `NULL`, the full tile is ingested.}

\item{tiles}{Explicit MGRS tile code(s) (e.g. `"T31UDP"`); resolved
from `aoi` when `NULL`.}

\item{date_from, date_to}{Date range (`"YYYY-MM-DD"`). RECONFORT needs
two full years.}

\item{s2_root}{Root directory for the S2 data (zip + extracted).}

\item{geodes_config}{Path to `pygeodes-config.json`. Default resolves
`options(nemeton.geodes_config)` then the per-user nemeton data dir.}

\item{s2_collection}{GEODES collection id. Default
`"THEIA_REFLECTANCE_SENTINEL2_L2A"` — the THEIA/MUSCATE Sentinel-2
surface-reflectance L2A products. (The bare `MUSCATE_*` name from the
upstream RECONFORT example is not a valid GEODES id and 400s.)}

\item{target_crs}{Integer EPSG of the AOI-streaming output projection
(default 2154). Ignored in full-tile mode.}

\item{buffer_m}{Numeric buffer (m) around the AOI bbox for the clip
window (default 3000). Ignored in full-tile mode.}

\item{keep_zips}{Full-tile mode only: keep the downloaded `.zip`
archives after extraction. Default `FALSE` (each archive is deleted as
soon as it is extracted, capping peak disk). In AOI-streaming mode the
archives are always streamed and removed.}

\item{quiet}{Suppress progress + subprocess output. Default `FALSE`.}
}
\value{
Invisibly, a list: `tiles`, `s2_root`, and `extracted` (the
per-tile extraction directories), for the L2b.3 map-production step.
}
\description{
IOTA²-native ingestion. Two modes:
\itemize{
\item \strong{AOI streaming} (when `aoi` is supplied — the production
path): one GEODES search per tile, then each archive is downloaded,
extracted, clipped + reprojected to the AOI window and deleted one at a
time. Peak disk stays near a single archive (~a few GB) instead of
whole-tile archives + whole-tile extracted (~460 GB for one tile over
two years). The output `<s2_root>/extracted/<tile>/` is already
AOI-cropped in `target_crs`.
\item \strong{Full tile} (when `aoi` is `NULL`): downloads the MUSCATE
L2A archives for each `tiles` entry and unzips the whole tile into
`<s2_root>/extracted/<tile>/` (the v0.94.x behaviour).
}

Requires the conda environment (L2b.1) and a GEODES account; heavy and
opt-in (not run in CI).
}
