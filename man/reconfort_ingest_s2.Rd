% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconfort_ingest.R
\name{reconfort_ingest_s2}
\alias{reconfort_ingest_s2}
\title{Acquire Sentinel-2 scenes for an AOI into the IOTA² layout}
\usage{
reconfort_ingest_s2(
  aoi = NULL,
  tiles = NULL,
  date_from,
  date_to,
  s2_root,
  geodes_config = NULL,
  s2_collection = "MUSCATE_SENTINEL2_SENTINEL2_L2A",
  quiet = FALSE
)
}
\arguments{
\item{aoi}{An `sf`/`sfc` AOI. Ignored when `tiles` is given.}

\item{tiles}{Explicit MGRS tile code(s) (e.g. `"T31UDP"`); resolved
from `aoi` when `NULL`.}

\item{date_from, date_to}{Date range (`"YYYY-MM-DD"`). RECONFORT needs
two full years.}

\item{s2_root}{Root directory for the S2 data (zip + extracted).}

\item{geodes_config}{Path to `pygeodes-config.json`. Default resolves
`options(nemeton.geodes_config)` then the per-user nemeton data dir.}

\item{s2_collection}{GEODES collection. Default the MUSCATE S2 L2A.}

\item{quiet}{Suppress progress + subprocess output. Default `FALSE`.}
}
\value{
Invisibly, a list: `tiles`, `s2_root`, and `extracted` (the
per-tile extraction directories), for the L2b.3 map-production step.
}
\description{
IOTA²-native ingestion: for each tile covering `aoi` (or each tile
in `tiles`), downloads the MUSCATE L2A archives from GEODES via the
vendored `pygeodes` driver, then unzips them into
`<s2_root>/extracted/<tile>/`. Requires the conda environment
(L2b.1) and a GEODES account; heavy and opt-in (not run in CI).
}
