% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_lock.R
\name{project_lock_status}
\alias{project_lock_status}
\title{Inspect the lock state of a project}
\usage{
project_lock_status(con, project_id, ttl_seconds = 120L)
}
\arguments{
\item{con}{A \code{DBIConnection}.}

\item{project_id}{Application project identifier.}

\item{ttl_seconds}{Heartbeat time-to-live used to compute \code{stale}
(default 120).}
}
\value{
\code{NULL} if the project is free, otherwise a list \code{holder_id},
\code{holder_label}, \code{acquired_at}, \code{heartbeat_at}, \code{stale}.
}
\description{
Read-only lookup of who holds \code{project_id}. Returns \code{NULL} when the project
is \strong{free} (no lock row). When a lock exists, returns its holder and stamps
plus a \code{stale} flag (\code{TRUE} when the heartbeat is older than \code{ttl_seconds}),
so the caller can tell \strong{free} (\code{NULL}), \strong{held-fresh} (\code{stale = FALSE})
and \strong{held-expired} (\code{stale = TRUE}) apart.
}
\seealso{
\code{\link{project_lock_acquire}}
}
