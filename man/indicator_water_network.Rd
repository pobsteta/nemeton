% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-families.R
\name{indicator_water_network}
\alias{indicator_water_network}
\title{Hydrographic Network Density (W1)}
\usage{
indicator_water_network(
  units,
  layers,
  watercourse_layer = "water_network",
  buffer = 0,
  proximity_m = 500,
  proximity_ref = 50
)
}
\arguments{
\item{units}{nemeton_units object}

\item{layers}{nemeton_layers object containing watercourse vector layer}

\item{watercourse_layer}{Character. Name of watercourse layer in layers object}

\item{buffer}{Numeric. Buffer distance (meters) for proximity analysis. Default 0.}

\item{proximity_m}{Numeric. Maximum distance (m) for proximity bonus. Default 500.}

\item{proximity_ref}{Numeric. Equivalent density bonus (m/ha) at distance 0. Default 50.}
}
\value{
Numeric vector of network density (m/ha)
}
\description{
Calculates stream/river network length density within or near forest parcels.
Includes a proximity bonus for parcels near watercourses (within 500m)
that are not directly crossed, reflecting the hydrological influence of
nearby streams on water table and microclimate.
}
\examples{
\dontrun{
layers <- nemeton_layers(vectors = list(streams = "watercourses.gpkg"))
results <- indicator_water_network(units, layers, watercourse_layer = "streams")
}
}
