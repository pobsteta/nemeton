% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soil_water.R
\name{ewm_depuis_soilgrids}
\alias{ewm_depuis_soilgrids}
\title{Maximum extractable water (\code{ewm}) per unit, from SoilGrids}
\usage{
ewm_depuis_soilgrids(
  units,
  rooting_depth_cm = 100,
  country = "FR",
  depths = NULL,
  progress_callback = NULL
)
}
\arguments{
\item{units}{An \code{sf} of management units.}

\item{rooting_depth_cm}{Rooting depth in cm (default 100). Depth intervals are
truncated at this value; an interval starting below it is skipped.}

\item{country}{ISO country code for the datasource lookup. Default \code{"FR"}.}

\item{depths}{Optional character vector of SoilGrids depth intervals to use
(default: all those within \code{rooting_depth_cm}, see \code{SOILGRIDS_DEPTHS}).}

\item{progress_callback}{Optional function called with
\code{list(current = "ewm:layer", interval = , i = , n = )} per depth interval,
and \code{list(current = "ewm:complete")} at the end (monitoring pattern).}
}
\value{
Numeric vector of \code{ewm} in mm, length \code{nrow(units)}. \code{NA} for a unit
whose soil data could not be resolved. \code{NULL} if \strong{no} depth interval could
be loaded at all (graceful degradation — the caller falls back to a uniform
\code{ewm}).
}
\description{
Per-unit \strong{maximum extractable water} (\code{ewm}, mm) — the plant-available water
reservoir that drives the whole BILJOU water balance, since
\code{rew = sw / ewm_total} and stress starts below \code{rew_c = 0.4}.
}
\details{
Built from SoilGrids 250 m (ISRIC): for each standard depth interval within
\code{rooting_depth_cm}, the available water capacity is estimated by the
\code{\link{awc_saxton_rawls}} pedotransfer function from clay, sand and organic
carbon, corrected for coarse fragments, then integrated over the horizon
thickness:

\deqn{ewm = \sum_{layers} AWC_i \times thickness_i \times 10}

(the factor 10 converts cm of horizon x m3/m3 into mm of water).

The topographic wetness index is deliberately \strong{not} used here: it measures
lateral convergence, not storage capacity, and it already feeds
\code{\link{indicateur_r3_secheresse}} directly (spec 035, decision D1).
}
\references{
Poggio L. et al. (2021). SoilGrids 2.0. \emph{SOIL} 7:217-240.
}
\seealso{
\code{\link{awc_saxton_rawls}}, \code{\link{build_biljou_soil}}
}
