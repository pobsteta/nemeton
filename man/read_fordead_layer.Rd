% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fordead_layer.R
\name{read_fordead_layer}
\alias{read_fordead_layer}
\title{Read an auxiliary FORDEAD diagnostic raster (model bundle)}
\usage{
read_fordead_layer(
  con,
  zone_id,
  layer,
  run_id = NULL,
  cache_dir = NULL,
  apply_zone_mask = TRUE,
  mask_polygon = NULL
)
}
\arguments{
\item{con}{A `DBIConnection` or `NULL`. Used only to resolve the UGF
AOI for the zone mask (skipped when `NULL`).}

\item{zone_id}{Integer. `monitoring_zone.id`.}

\item{layer}{Character. One of `"first_anomaly"`, `"anomaly_index"`,
`"modelled_pixels"`.}

\item{run_id}{Character or `NULL`. Run timestamp; `NULL` picks the
most recent bundle.}

\item{cache_dir}{Character. Project FORDEAD cache root
(`<project>/cache/layers/fordead`), same as
[read_fordead_dieback_mask()].}

\item{apply_zone_mask}{Logical. Restrict to the UGF perimeter
(default `TRUE`).}

\item{mask_polygon}{Optional `sf`/`sfc` overriding the DB-resolved
AOI.}
}
\value{
A `terra::SpatRaster` (single band) or `NULL`.
}
\description{
Loads one of the map-relevant single-band rasters written to the
per-run model bundle (`zone_<id>/model_<run_id>/`) by the persist
phase of [run_fordead_dieback()], and applies the UGF zone mask the
same way [read_fordead_dieback_mask()] does.
}
\details{
Recognised layers:
\describe{
  \item{`"first_anomaly"`}{First confirmed-anomaly date per pixel
    (days since 1970-01-01) — feeds the "date de première détection"
    display layer.}
  \item{`"anomaly_index"`}{Latest continuous anomaly index — feeds the
    "indice d'anomalie / sévérité continue" display layer.}
  \item{`"modelled_pixels"`}{Binary mask of pixels the harmonic model
    could be fitted on — feeds the "confiance / zone modélisée"
    display layer.}
}

Returns `NULL` (never errors) when the cache, the zone, the bundle or
the layer file is missing — older runs predating a layer simply yield
`NULL`, so the caller degrades gracefully.
}
\seealso{
[read_fordead_dieback_mask()] for the categorical 0-4 mask,
  [read_fordead_pixel_series()] for the per-pixel CRSWIR diagnostic.
}
