% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitoring.R
\name{register_monitoring_zone}
\alias{register_monitoring_zone}
\title{Register a monitoring zone and its plots in the database}
\usage{
register_monitoring_zone(
  con,
  zone_name,
  zone_polygon,
  placettes,
  radius_m = 15
)
}
\arguments{
\item{con}{A `DBIConnection` returned by [db_connect()].}

\item{zone_name}{Character. Display name for the zone.}

\item{zone_polygon}{An sf POLYGON (any CRS — re-projected to WGS84
internally for storage).}

\item{placettes}{An sf POINT object with at least the columns
`plot_id` (character) and optionally `type`.}

\item{radius_m}{Numeric. Sampling radius around each placette in
metres. Default 15.}
}
\value{
The `zone_id` (integer) of the registered zone.
}
\description{
Helper that inserts a `monitoring_zone` row and the associated
`plot` rows. Idempotent on `(zone_id, plot_id)` — within an
existing zone, re-registering the same `plot_id` is a no-op
(UNIQUE constraint + `ON CONFLICT DO NOTHING`). The
`monitoring_zone` table has no uniqueness on `name`, so calling
this function twice with the same `zone_name` creates two
independent zones.
}
