% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fordead_python.R
\name{.ensure_fordead_python}
\alias{.ensure_fordead_python}
\title{Ensure the FORDEAD virtualenv exists and return the loaded module}
\usage{
.ensure_fordead_python(
  env_name = .fordead_default_env(),
  requirements = .fordead_requirements_path(),
  verbose = TRUE
)
}
\arguments{
\item{env_name}{Character. Virtualenv name. Defaults to
`nemeton-fordead`.}

\item{requirements}{Character path. Pinned requirements file.
Defaults to the package-shipped one.}

\item{verbose}{Logical. Print progress via `cli`. Default `TRUE`.}
}
\value{
The imported `fordead` Python module (a
  `python.builtin.module` object usable with `$` syntax).
}
\description{
Idempotent. On first call, creates the virtualenv (via
[reticulate::virtualenv_create()]) and installs the pinned
dependencies from \code{inst/python/requirements.txt}. On
subsequent calls in the same R session, the result is cached.
}
\details{
Set the env var \code{NEMETON_FORDEAD_ENV} to override the default
virtualenv name, e.g. for parallel test runs.
}
\keyword{internal}
