% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qfield_import.R
\name{tag_field_data_sources}
\alias{tag_field_data_sources}
\title{Tag an sf object with field-data NDP attributes}
\usage{
tag_field_data_sources(data, placettes, arbres = NULL)
}
\arguments{
\item{data}{An sf / data.frame to tag.}

\item{placettes}{sf or data.frame of placettes.}

\item{arbres}{sf or data.frame of trees (may be NULL).}
}
\value{
\code{data} with the added attributes.
}
\description{
Sets the attributes \code{field_plots_count} and
\code{field_trees_count} that \code{\link{detect_ndp}} reads to bump
the NDP to 2 (with plots only) or 3 (with >= 10 trees/plot on
average) along the alternative QField path.
}
\examples{
df <- data.frame(x = 1)
placettes <- data.frame(plot_id = c("P1", "P2"))
arbres    <- data.frame(plot_id = rep(c("P1", "P2"), each = 15))
tagged <- tag_field_data_sources(df, placettes, arbres)
detect_ndp(tagged)$level  # 3

}
