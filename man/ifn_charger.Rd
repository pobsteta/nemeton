% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_source.R
\name{ifn_charger}
\alias{ifn_charger}
\title{Load raw French NFI tables}
\usage{
ifn_charger(tables = c("ARBRE", "PLACETTE"), campagne = NULL,
  dest_dir = file.path(tempdir(), "ifn"), visite = NULL, force = FALSE)
}
\arguments{
\item{tables}{Character vector of table names, without extension, e.g.
\code{c("ARBRE", "PLACETTE")}. Available: \code{ARBRE}, \code{PLACETTE},
\code{ECOLOGIE}, \code{COUVERT}, \code{FLORE}, \code{HABITAT},
\code{BOIS_MORT}, \code{metadonnees}, \code{espar-cdref13}.}

\item{campagne}{Campaign year, or \code{NULL} for the most recent.}

\item{dest_dir}{Directory holding (or receiving) the cached archive. Defaults
to a session temporary directory.}

\item{visite}{Optional visit filter applied to \code{PLACETTE} only: \code{1}
(first measurement), \code{2} (the five-year revisit) or \code{c(1, 2)}.
\code{NULL} keeps all.}

\item{force}{Passed to \code{\link{ifn_telecharger}}.}
}
\value{
A named list of data.frames, one per requested table, carrying a
\code{millesime} attribute.
}
\description{
Reads the requested CSV tables out of the IGN export archive and returns them.
Column names are kept \strong{as published by the IGN} (upper case: \code{IDP},
\code{ESPAR}, \code{VEGET5}…), so the IGN documentation shipped inside the
archive applies directly.
}
\section{Improvements over \code{FrenchNFIfindeR::get_NFI()}}{

This is a reimplementation, not a wrapper — \code{nemeton} does not depend on
that package. Five deliberate differences:
\enumerate{
\item \strong{Dynamic campaign} — the download URL is discovered
(\code{\link{ifn_campagne_disponible}}) instead of being pinned in the source,
which leaves the original one campaign behind each autumn.
\item \strong{Non-interactive} — no \code{readline()} prompt; see
\code{\link{ifn_telecharger}}.
\item \strong{Selective tables} — only the tables asked for are read. The
original always reads five, including \code{FLORE.csv} (~58 MB) and
\code{ECOLOGIE.csv}, even when only trees are needed.
\item \strong{Returns a value} — the original defaults to
\code{export_to_env = TRUE}, assigning six objects into the global environment.
Here the tables are returned as a named list.
\item \strong{No silent derivation} — the original imputes missing heights and
increments from plot/species/size-class means, and adds computed basal areas,
as a side effect of loading. Loading here returns the published data unchanged;
derivations belong to the caller, where they can be documented and tested.
}
}

\examples{
\dontrun{
d <- ifn_charger(c("ARBRE", "PLACETTE"), visite = 1)
nrow(d$ARBRE)
}
}
\references{
IGN — Inventaire forestier national français, Données brutes, Campagnes
annuelles 2005 et suivantes, \url{https://inventaire-forestier.ign.fr/dataIFN/}.
Licence Ouverte Etalab v2.0. Approach after \code{FrenchNFIfindeR::get_NFI()}
(J. Borderieux, GPL-3).
}
