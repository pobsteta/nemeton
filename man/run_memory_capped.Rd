% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isolate.R
\name{run_memory_capped}
\alias{run_memory_capped}
\title{Run a heavy nemeton pipeline in a memory-capped child process}
\usage{
run_memory_capped(
  fun,
  args = list(),
  db_url = NULL,
  progress_path = NULL,
  progress_callback = NULL,
  memory_max = NULL,
  poll_ms = 500L,
  quiet = FALSE
)
}
\arguments{
\item{fun}{Name of the exported \code{nemeton} function to run (character).}

\item{args}{Named list of arguments. Must be serialisable: no connections,
no closures, no \code{SpatRaster} — pass file paths.}

\item{db_url}{Database URL. When given, the child opens a connection and
passes it to \code{fun} as \code{con}.}

\item{progress_path}{Path of the progress \code{.json} file. When given, the child
passes \code{fun} a callback writing there.}

\item{progress_callback}{Parent-side callback, fed by replaying the child's
events (requires \code{progress_path}).}

\item{memory_max}{Ceiling as a systemd size string (e.g. \code{"12G"}). Default:
70\% of RAM, or \code{options(nemeton.reconfort_memory_max)}. \code{FALSE} disables it.}

\item{poll_ms}{How often to poll the child for progress, in milliseconds.}

\item{quiet}{Suppress the child's console output.}
}
\value{
Whatever \code{fun} returned.
}
\description{
Runs an exported \code{nemeton} function in a \strong{child R process} placed in a
transient, memory-capped cgroup. A run that overshoots then dies \strong{alone},
with an ordinary error the caller can report — instead of having
\code{systemd-oomd} kill the entire application scope (the R session, the Shiny
app, the terminals) for being the biggest thing under memory pressure.

Use it for the pipelines whose memory lives \emph{inside} the R process and so
cannot be capped in place: \code{\link{run_fordead_dieback}} (Python through
reticulate's embedded interpreter) and the reGénération engines (plain R).
\code{\link{run_reconfort_dieback}} does \strong{not} need it — it already caps the Python
subprocess it spawns.
}
\details{
Two arguments cannot be serialised across a process boundary, and are
therefore rebuilt in the child rather than passed:
\itemize{
\item \strong{\code{con}} — a \code{DBIConnection} is not serialisable. Pass \code{db_url} and the
child opens (and closes) its own via \code{\link{db_connect}}.
\item \strong{\code{progress_callback}} — a closure is not serialisable either. Pass
\code{progress_path}: the child writes each event to \verb{<path>.json} (last event,
written atomically) and appends it to \verb{<path>.ndjson}, and the parent tails
that file and replays every event into the \code{progress_callback} you gave
here. Callers therefore keep receiving events as before — including any
side effects of their callback, such as push notifications.
}

Without \code{systemd-run} (non-Linux, container with no user bus, CI) the work
still runs in a child process — whose memory at least returns to the OS on
exit — but \strong{uncapped}, with a warning. Without a cgroup, an overshoot is
once again the whole scope's problem.
}
\examples{
\dontrun{
res <- run_memory_capped(
  "run_fordead_dieback",
  args       = list(zone_id = 9L, cache_dir = "~/cache"),
  db_url     = Sys.getenv("NEMETON_DB_URL"),
  memory_max = "12G"
)
}

}
\seealso{
\code{\link{run_fordead_dieback}}, \code{\link{run_reticulate_isolated}} (which pins a
Python env rather than capping memory), \code{\link{scratch_dir}}
}
