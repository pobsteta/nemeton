% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fordead_postprocess.R
\name{.insert_fordead_alerts}
\alias{.insert_fordead_alerts}
\title{Persist FORDEAD alert centroids in the `alert` table}
\usage{
.insert_fordead_alerts(con, alerts_sf, zone_id, replace = TRUE)
}
\arguments{
\item{con}{A `DBIConnection`.}

\item{alerts_sf}{An sf POINT (centroids) with columns `trigger_date`,
`confidence_class`, `stress_index` and, when available, `n_pixels`,
`area_m2`. CRS assumed EPSG:2154 when absent.}

\item{zone_id}{Integer. Target monitoring zone.}

\item{replace}{Logical. When `TRUE` (default) every prior
`(zone_id, alert_type)` alert is deleted before insertion, making the
call idempotent across re-runs. When `FALSE`, rows are appended.}
}
\value{
Number of rows inserted (integer).
}
\description{
Thin wrapper over [.insert_health_alerts()] with
`alert_type = "fordead_dieback"`. Pixel/cluster entity, no plot
snapping (spec 008 §15 Phase B).
}
\keyword{internal}
