% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_lock.R
\name{project_lock_heartbeat}
\alias{project_lock_heartbeat}
\title{Refresh the heartbeat on a held project lock}
\usage{
project_lock_heartbeat(con, project_id, holder_id)
}
\arguments{
\item{con}{A \code{DBIConnection}.}

\item{project_id}{Application project identifier.}

\item{holder_id}{The holder refreshing its lock.}
}
\value{
\code{TRUE} if the lock is held by \code{holder_id} after the call (heartbeat
refreshed), \code{FALSE} if \code{holder_id} does not hold it.
}
\description{
Set \code{heartbeat_at = now()} for \code{project_id} \strong{only if} \code{holder_id} still
holds it. A no-op otherwise. The app calls this periodically (every
~30–60 s) so the lock survives past \code{ttl_seconds}.
}
\seealso{
\code{\link{project_lock_acquire}}
}
