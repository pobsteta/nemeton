% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_lock.R
\name{project_lock_acquire}
\alias{project_lock_acquire}
\title{Acquire the edit lock on a project}
\usage{
project_lock_acquire(
  con,
  project_id,
  holder_id,
  holder_label = NULL,
  ttl_seconds = 120L
)
}
\arguments{
\item{con}{A \code{DBIConnection} (PostgreSQL server, or SQLite for local/test).}

\item{project_id}{Application project identifier (string).}

\item{holder_id}{Stable identity of the acquirer (e.g. OAuth email).}

\item{holder_label}{Optional display name shown to other users.}

\item{ttl_seconds}{Heartbeat time-to-live in seconds (default 120). A lock
whose last heartbeat is older than this is stealable.}
}
\value{
On success, a list \code{ok = TRUE}, \code{holder_id}, \code{holder_label},
\code{acquired_at}, \code{heartbeat_at}, \code{stolen} (TRUE when an expired lock was
taken over). On failure, \code{ok = FALSE} with the current holder's
\code{holder_id}, \code{holder_label}, \code{heartbeat_at}.
}
\description{
Try to take the single-writer edit lock on \code{project_id} for \code{holder_id}.
Succeeds when the project is \strong{free}, \strong{already held by \code{holder_id}}
(re-entrant — the heartbeat is refreshed), or \strong{held by a lock whose
heartbeat is older than \code{ttl_seconds}} (expired — the lock is stolen).
Fails when another holder's lock is still fresh.

The whole decision runs in one transaction with a row lock (\code{FOR UPDATE}
on PostgreSQL), so two concurrent acquisitions on a free project yield
exactly one winner. Expiry is judged against the database clock.
}
\seealso{
\code{\link{project_lock_heartbeat}}, \code{\link{project_lock_release}},
\code{\link{project_lock_status}}
}
