% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-microclimate.R
\name{microclimate_run}
\alias{microclimate_run}
\title{Run the under-canopy microclimate model (scaffold — spec 027 L1)}
\usage{
microclimate_run(aoi, year, structure = c("lidarhd", "opencanopy"),
  resolution = 5, cache_dir = NULL, quiet = FALSE)
}
\arguments{
\item{aoi}{An \code{sf}/\code{sfc} AOI (union of parcels + buffer).}

\item{year}{Integer year of the ERA5-Land forcing.}

\item{structure}{Canopy structure source: \code{"lidarhd"} or \code{"opencanopy"}.}

\item{resolution}{Working resolution in metres (default 5).}

\item{cache_dir}{Optional cache directory for the microclimate rasters.}

\item{quiet}{Suppress progress messages.}
}
\value{
A named list of \code{terra::SpatRaster} — \code{tmax_understorey},
\code{tmax_open}, \code{vpd}, \code{rh} (summer JJA) — the \code{micro} contract.
}
\description{
Orchestrator that will produce the summer-aggregated microclimate rasters
(\code{tmax_understorey}, \code{tmax_open}, \code{vpd}, \code{rh}) consumed by the A3/A4/W4
indicators, via the mechanistic model \strong{microclimf} forced by ERA5-Land
and driven by LiDAR-HD canopy structure (PAI + height), with an
\pkg{opencanopy} CHM fallback (ADR-014).

\strong{Scaffold}: the heavy dependencies (\code{microclimf}, \code{mcera5}, \code{ecmwfr},
\code{lidR}, \code{lasR}) are in \code{Suggests}; this entry point validates their
presence and defines the \code{micro} contract. The full microclimf
orchestration (per the spec §6 pipeline) is wired in a later L1 increment
once the forcing/structure data are available — in the meantime the
indicators accept a precomputed \code{micro} set.
}
\seealso{
\code{\link{indicateur_a3_microclimat}}
}
