% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndp.R
\name{canopy_provenance}
\alias{canopy_provenance}
\title{Canonical canopy-structure provenance from NDP augmented flags}
\usage{
canopy_provenance(augmented = character(0))
}
\arguments{
\item{augmented}{Character vector of augmentation flags, as returned in the
\code{augmented} field of \code{\link{detect_ndp}}.}
}
\value{
A single character key:
\describe{
\item{\code{"prosail_s2"}}{Sentinel-2 LAI (PROSAIL inversion) — the NDP-0
satellite fallback used when LiDAR HD is absent (flag \code{lai_ml}).}
\item{\code{"opencanopy"}}{Open-Canopy ML canopy-height model (flag \code{height_ml}).}
\item{\code{"lidar_hd"}}{Native LiDAR HD structure (PAI) — the default when no
satellite / ML canopy flag is present.}
}
}
\description{
Maps the ML-augmentation flags of \code{\link{detect_ndp}} to a single
canonical canopy-structure provenance key, so a UI can badge which data
produced the canopy / LAI used by the regeneration engines without
re-implementing the flag-to-meaning rule (business logic stays in the core).
Priority: the Sentinel-2 fallback (\code{lai_ml}) wins — its presence means
LiDAR HD was absent.
}
\examples{
canopy_provenance(character(0))                  # "lidar_hd"
canopy_provenance("lai_ml")                      # "prosail_s2"
canopy_provenance(c("height_ml", "species_ml"))  # "opencanopy"
}
\seealso{
\code{\link{detect_ndp}}
}
