% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_diversity.R
\name{indicateur_l3_het_spectrale}
\alias{indicateur_l3_het_spectrale}
\title{Indicator L3 — Spectral beta diversity / landscape heterogeneity (family L)}
\usage{
indicateur_l3_het_spectrale(
  units,
  spectral = NULL,
  reflectance = NULL,
  column_name = "L3",
  ...
)
}
\arguments{
\item{units}{sf polygon layer (spatial units / UGF).}

\item{spectral}{Optional precomputed result of
\code{\link{compute_spectral_diversity}} (preferred — compute once, share
with B4). When \code{NULL}, falls back to \code{reflectance}.}

\item{reflectance}{Optional reflectance \code{SpatRaster} / path used to
compute spectral diversity on the fly when \code{spectral} is
\code{NULL}.}

\item{column_name}{Output column name (default \code{"L3"}).}

\item{...}{Passed to \code{\link{compute_spectral_diversity}} when computing
on the fly (e.g. \code{window_size}, \code{mask}, \code{nb_cpu}).}
}
\value{
\code{units} with the numeric \code{column_name} column added.
}
\description{
Per-unit Bray-Curtis beta diversity (spectral-species turnover,
biodivMapR): the compositional heterogeneity of the landscape mosaic,
complementary to L2 (geometric fragmentation). Higher = more diverse
mosaic (spec 028 D1). Backward compatible like
\code{\link{indicateur_b4_div_spectrale}}.
}
\seealso{
\code{\link{compute_spectral_diversity}}
}
