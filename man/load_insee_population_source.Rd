% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_insee_population.R
\name{load_insee_population_source}
\alias{load_insee_population_source}
\title{French population grid (INSEE Filosofi)}
\usage{
load_insee_population_source(
  aoi,
  buffer_m = 21000,
  maille = c("1km", "200m"),
  millesime = 2021,
  crs = 2154,
  territoire = c("FR", "MTQ", "REU"),
  cache_dir = NULL
)
}
\arguments{
\item{aoi}{\code{sf}/\code{sfc} extent. Cells are read within its bounding box grown by
\code{buffer_m}.}

\item{buffer_m}{Metres added around \code{aoi} before clipping. Default \code{21000},
enough for the 20 km ring S3 uses.}

\item{maille}{\code{"1km"} (default) or \code{"200m"}. 200 m is six times heavier and
pointless for 5-20 km rings.}

\item{millesime}{Filosofi vintage. Only \code{2021} is wired.}

\item{crs}{Output CRS. Default \code{2154}.}

\item{territoire}{\code{"FR"} (metropolitan), \code{"MTQ"} or \code{"REU"}.}

\item{cache_dir}{Override the shared cache directory.}
}
\value{
An \code{sf} of grid cells with \code{ind} (individuals) and the imputation
flag, or \code{NULL} when the source cannot be reached — \strong{never a fabricated
fallback} (spec 050, and the rule of v0.187.0).
Carries a \code{part_imputee} attribute: share of cells that are imputed.
}
\description{
Downloads (once per machine) and reads the INSEE \emph{Filosofi} population grid,
clipped to \code{aoi}. Feeds \code{\link{indicateur_s3_population}}.
}
\details{
\strong{The clip happens at read time, not after.} Measured on the Couchey
massif: 1 024 cells read instead of the 374 511 the metropolitan layer
holds. Reading the whole of France to keep 0.3\% of it would cost memory for
nothing.

\strong{Imputed cells are kept.} \code{i_est_1km == 1} marks a cell holding fewer than
11 fiscal households, whose count INSEE \emph{models} rather than observes.
Dropping them would remove real people — the imputation exists precisely so
totals stay right — and it would bite hardest where forests are: 42\% of the
cells around Couchey are imputed, 53\% within 20 km. They are kept, and the
share is reported back so the caller can say so
(see the \code{part_imputee} attribute).

Licence Ouverte / Open Licence 2.0 (INSEE). Attribution required.
}
