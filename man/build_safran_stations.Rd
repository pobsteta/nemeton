% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eobs_downscale.R
\name{build_safran_stations}
\alias{build_safran_stations}
\title{Build the SAFRAN pseudo-station grid for the meteoland engine (spec P4)}
\usage{
build_safran_stations(aoi, buffer_m, years, dem, spacing_m = 8000, fetch = NULL)
}
\arguments{
\item{aoi}{An \code{sf}/\code{sfc} of the management units.}

\item{buffer_m}{Context buffer (metres) sampled around the AOI.}

\item{years}{Integer year(s) of the SAFRAN series.}

\item{dem}{A \code{SpatRaster} DEM, sampled for each point's \code{elevation}.}

\item{spacing_m}{Pseudo-station grid step in metres (default 8000, SAFRAN
resolution).}

\item{fetch}{Acquisition function (for testing); defaults to the internal
GéoSAS SAFRAN reader. Signature \code{function(points, years)} returning a named
list of raw daily data frames keyed by \code{points$id}.}
}
\value{
A list \code{list(points, series)}: \code{points} an \code{sf} of pseudo-stations
(\code{id}, \code{elevation}, geometry in the DEM CRS) restricted to those with a
non-empty series and a finite elevation; \code{series} the matching named list.
\code{NULL} if none resolve.
}
\description{
Sample SAFRAN cells over the AOI + buffer as \strong{pseudo-stations} for the
meteoland interpolator: a point grid at \code{spacing_m} (SAFRAN ~8 km), each point
carrying its DEM elevation and its daily SAFRAN series (fetched from the same
GéoSAS OGC API-EDR already used for BILJOU). SAFRAN is a gridded reanalysis,
not a station network, but that is exactly what meteoland needs as reference
— see the P4 brief. The data source is thus already wired; no new one.
}
\seealso{
\code{\link{eobs_downscale}}, \code{\link{load_biljou_forcing}}
}
