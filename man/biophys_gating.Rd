% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biophys_gating.R
\name{biophys_gating}
\alias{biophys_gating}
\title{Gate the Sentinel-2 biophysical augmentation flag, per unit}
\usage{
biophys_gating(n_obs, pct_masked, oob_frac, area_px,
  thresholds = biophys_gating_thresholds())
}
\arguments{
\item{n_obs}{Integer vector: valid observations in the June-August window, per
unit.}

\item{pct_masked}{Numeric vector in \code{[0, 1]}: cloud/shadow masking rate per
unit.}

\item{oob_frac}{Numeric vector in \code{[0, 1]}: fraction of pixels flagged
out-of-domain by SL2P (input or output) per unit.}

\item{area_px}{Integer vector: unit size in 20 m pixels.}

\item{thresholds}{A list as returned by \code{\link{biophys_gating_thresholds}}.}
}
\value{
A logical vector, one per unit: \code{TRUE} where \strong{all} conditions hold,
\code{FALSE} otherwise (including where any input is \code{NA}).
}
\description{
Returns, for each unit, whether the Sentinel-2 biophysical variables are
reliable enough to carry the \code{"biophysical_s2"} NDP augmentation flag
(spec 043 D6, ADR-015). All four quality conditions must hold; any missing
metric fails the gate (\strong{fail-closed}).

This is a \strong{decision}, not a computation: the biophysical rasters and
their quality metrics are produced upstream (\code{biophysnemeton}, ADR-009).
This function only decides eligibility, so the confidence phi never counts a
variable retrieved on an insufficient basis (ADR-011).
}
\examples{
biophys_gating(n_obs = c(5, 2, 8), pct_masked = c(0.2, 0.1, 0.6),
               oob_frac = c(0.02, 0.01, 0.03), area_px = c(40, 30, 100))
# -> TRUE, FALSE (n_obs<3), FALSE (pct_masked>0.4)
}
\seealso{
\code{\link{biophys_gating_thresholds}}
}
