% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regen_engines.R
\name{lai_max_depuis_pai}
\alias{lai_max_depuis_pai}
\title{Per-unit canopy \code{lai_max} from a LiDAR PAI raster (spec 035 D5)}
\usage{
lai_max_depuis_pai(units, pai, probs = 0.9, min_pai = 0.1)
}
\arguments{
\item{units}{An \code{sf} of management units.}

\item{pai}{A \code{SpatRaster} of Plant Area Index, or a path to one (e.g. the
\code{pai.tif} written by \code{regen_sensibilite(pai_cache = )}).}

\item{probs}{Percentile of the within-unit PAI distribution used as the
plateau. Default \code{0.9}.}

\item{min_pai}{Pixels strictly below this value are excluded as non-canopy.
Default \code{0.1}. Set to \code{0} to keep every pixel.}
}
\value{
Numeric vector of \code{lai_max}, length \code{nrow(units)}. \code{NA} for a unit
with no canopy pixel. Pass it straight to \code{regen_bilan_hydrique(lai_max = )},
which converts it to the id-keyed list \code{biljou_run_grid()} requires.
}
\description{
Aggregate a Plant Area Index raster (typically the cached \code{pai.tif} produced
by \code{\link{pai_depuis_nuage}}) into the per-unit \code{lai_max} expected by
\code{\link{regen_bilan_hydrique}}.
}
\details{
\code{biljouR::biljou_lai()} treats \code{lai_max} as the \strong{plateau} of the phenology
curve (coniferous: constant all year; broadleaved: the flat top of the
trapezoid between \code{budburst + ramp} and \code{leaf_fall - ramp}). A zonal \strong{mean}
under-estimates that plateau — which is what the app's own fallback did. This
function therefore extracts a \strong{high percentile} (default P90), robust to
outlier pixels where a plain \code{max} would not be.

Pixels below \code{min_pai} are treated as non-canopy (gaps, roads, water) and
excluded before the percentile, so a unit with a clearing keeps the PAI of its
stocked part.
}
\seealso{
\code{\link{pai_depuis_nuage}}, \code{\link{regen_bilan_hydrique}}
}
