% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eobs_click_series.R
\name{eobs_summer_series}
\alias{eobs_summer_series}
\title{Summer E-OBS series at a point (year -> value)}
\usage{
eobs_summer_series(stack, point)
}
\arguments{
\item{stack}{A per-year summer \code{SpatRaster} (one layer per year, named by year),
as returned by \code{\link{load_eobs_source}}.}

\item{point}{An \code{sf}/\code{sfc} POINT, or \code{c(lon, lat)} in EPSG:4326 (the leaflet
click).}
}
\value{
A \code{data.frame(year, value)}, one row per layer ordered by year,
NA-safe (a masked/out-of-extent point yields \code{NA} values). Attributes:
\code{var}, \code{unit}. \code{var} is read from the stack's \code{varnames} when present.
}
\description{
Extract the per-year summer (JJA) E-OBS value at a location, feeding charts 1-3
of the regional-context click panel (spec 036): annual series + trend line,
annual anomalies, and regional distribution. Reduction and acquisition already
live in \code{\link{load_eobs_source}}; this only extracts the per-year stack at \code{point},
so the app can pass an already-loaded stack and answer every click without
re-reducing the daily netCDF.
}
\examples{
\dontrun{
stk <- load_eobs_source(aoi, var = "tx", nc = "eobs_tx.nc", source = "nc")
eobs_summer_series(stk, c(6.1, 48.7))
}
}
\seealso{
\code{\link{eobs_monthly_climatology}}, \code{\link{eobs_trend_fit}}, \code{\link{load_eobs_source}}
}
