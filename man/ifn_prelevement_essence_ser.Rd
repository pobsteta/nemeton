% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_tables.R
\name{ifn_prelevement_essence_ser}
\alias{ifn_prelevement_essence_ser}
\title{IFN harvest by species and sylvoecoregion}
\usage{
ifn_prelevement_essence_ser(espar = NULL, ser = NULL, greco = NULL,
  niveau = NULL)
}
\arguments{
\item{espar}{Optional IFN species code filter (character, e.g. \code{"09"}).}

\item{ser}{Optional SER code filter (e.g. \code{"C20"}).}

\item{greco}{Optional GRECO code filter (single letter).}

\item{niveau}{Optional level filter: \code{"ser"}, \code{"greco"} or
\code{"national"}.}
}
\value{
A data.frame with \code{niveau}, \code{ser}, \code{greco}, \code{espar},
\code{n_plac_presence}, \code{n_plac_maille}, \code{prelev_ha_an_present},
\code{prelev_ha_an_maille}, \code{taux_presence}, \code{libelle_essence},
\code{millesime}, \code{source}.
}
\description{
Reference table of \strong{harvested} timber per hectare and per year, by IFN
species code and sylvoecoregion, derived from the five-year revisit of the raw
IFN data (campaigns 2005-2024).

Same key schema and same three nested levels as
\code{\link{ifn_volume_essence_ser}}: \code{"ser"}, \code{"greco"},
\code{"national"}.
}
\section{What counts as harvested}{

Only \code{VEGET5 == "6"} — \emph{arbre coupé vidangé}, felled \strong{and
extracted}. Code \code{7} (\emph{coupé non vidangé}) is deliberately excluded:
that wood stays in the forest and never travels the road network, which is what
this table is meant to size.
}

\section{Two approximations, stated}{

\enumerate{
\item The revisit row carries only the tree's fate — neither its volume nor its
species. Both are taken from the tree's \strong{first-visit} row (key
\code{IDP}, \code{A}), so the harvested volume is the volume at first
measurement: the tree grew somewhat before being felled. 92\% of felled trees
carry such a measurement.
\item Harvest observed over the five-year interval is divided by five to give a
yearly flux. It is an average, not a schedule.
}

As a sanity check, summing \code{prelev_ha_an_maille} over all species at the
national level gives \strong{2.84 m3/ha/year}, consistent with the published
order of magnitude for French forest harvest.
}

\section{Harvest is not prescription}{

These figures describe what \strong{has been} removed, not what \strong{should}
be. Sizing a road network on them assumes management carries on unchanged.
}

\examples{
\dontrun{
ifn_prelevement_essence_ser(espar = "62", niveau = "national")
}
}
\seealso{
\code{\link{ifn_taux_prelevement}} for the fallback ladder,
\code{\link{ifn_volume_essence_ser}} for standing volume.
}
