% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regen_rank_species.R
\name{regen_rank_to_wide}
\alias{regen_rank_to_wide}
\title{Pivot a species ranking to one row per unit (wide)}
\usage{
regen_rank_to_wide(ranked, top_n = 3)
}
\arguments{
\item{ranked}{The long \code{data.frame} returned by \code{\link{regen_rank_species}}.}

\item{top_n}{Number of ranks to spread into columns (default 3).}
}
\value{
A \code{data.frame} with \code{ug_id} and, per rank, \code{essence_r} (species code),
\code{score_r} (suitability), \code{label_r} (name) and \code{facteur_r} (limiting factor).
}
\description{
Reshape the long output of \code{\link{regen_rank_species}} to one row per unit with
\code{essence_r} / \code{score_r} / \code{label_r} / \code{facteur_r} columns for each rank
\code{r = 1..top_n} — convenient for a per-unit table or a map join.
}
\seealso{
\code{\link{regen_rank_species}}
}
