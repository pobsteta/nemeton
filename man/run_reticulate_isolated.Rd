% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reticulate_isolated.R
\name{run_reticulate_isolated}
\alias{run_reticulate_isolated}
\title{Run a Python/reticulate task in an isolated subprocess (pinned env)}
\usage{
run_reticulate_isolated(fun, args = list(), python = NULL,
  virtualenv = NULL, condaenv = NULL, show = TRUE)
}
\arguments{
\item{fun}{A function to run in the subprocess.}

\item{args}{A named list of arguments passed to \code{fun}.}

\item{python}{Direct path to a Python interpreter (highest priority).}

\item{virtualenv}{A virtualenv name (resolved via
\code{\link[reticulate:virtualenv-tools]{reticulate::virtualenv_python}()}).}

\item{condaenv}{A conda env name (resolved via
\code{reticulate::conda_python()}, with a fallback over the usual
conda/mamba install roots).}

\item{show}{Stream the subprocess output to the console (default \code{TRUE})
— the way to surface progress, since R callbacks cannot cross the
process boundary.}
}
\value{
Whatever \code{fun} returns.
}
\description{
Runs \code{fun} in a fresh \pkg{callr} R subprocess whose reticulate is
pinned to a specific Python interpreter (\code{python} / \code{virtualenv} /
\code{condaenv}). The subprocess starts with an \strong{unbound} reticulate, so
it always binds to the requested environment — even if the parent
session already bound reticulate to a different Python (another env,
Theia, or reticulate's uv ephemeral default). This lets several
Python workloads that need \emph{different} environments coexist in one app
session without the single-binding conflict, and \strong{without} a global
\code{RETICULATE_PYTHON} (which can only serve one env).

\code{R_ENVIRON_USER = ""} is set for the subprocess so a \code{RETICULATE_PYTHON}
in the user's \code{~/.Renviron} cannot override the pin.

\code{fun} must be self-contained (callr serialises it): qualify packages
explicitly (\code{pkg::fn}) and pass everything via \code{args}. Return values and
\code{args} must be serialisable — exchange rasters as \strong{file paths}, not
in-memory \code{SpatRaster} objects.

Falls back to running \code{fun} \strong{in-process} when \pkg{callr} is missing or
no Python could be resolved (a \code{cli_warn} is emitted in the latter case).
}
