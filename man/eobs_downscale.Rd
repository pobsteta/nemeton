% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eobs_downscale.R
\name{eobs_downscale}
\alias{eobs_downscale}
\title{Downscale an E-OBS variable to a fine raster}
\usage{
eobs_downscale(
  var = c("tx", "rr"),
  eobs,
  dem,
  aoi,
  engine = c("ked", "meteoland"),
  buffer_m = 25000,
  resolution = NULL,
  covariates = c("dem", "slope", "aspect", "twi"),
  statistic = c("trend", "mean", "value"),
  variogram_model = NULL,
  years = NULL,
  max_cells = 5e+05,
  min_points = 10L,
  cache_path = NULL,
  ...
)
}
\arguments{
\item{var}{\code{"tx"} (v1). \code{"rr"} returns an out-of-scope status.}

\item{eobs}{A \code{SpatRaster} of E-OBS: one layer per year (for
\code{statistic = "trend"}/\code{"mean"}) or a single reduced layer
(\code{statistic = "value"}).}

\item{dem}{A \code{SpatRaster} DEM — the downscaling target grid and main
covariate. The output shares its CRS.}

\item{aoi}{An \code{sf}/\code{sfc} of the management units, buffered to frame the
regional context.}

\item{engine}{\code{"ked"} (default, regression-kriging) or \code{"meteoland"}
(station interpolator; falls back to KED when unavailable).}

\item{buffer_m}{Context buffer around the AOI, in metres (default 25000).}

\item{resolution}{Optional target resolution (DEM units). \code{NULL} keeps the
DEM resolution (aggregated if the grid would exceed \code{max_cells}).}

\item{covariates}{Terrain covariates among \code{"dem"}, \code{"slope"}, \code{"aspect"}
(entered as northness), \code{"twi"} (best-effort). Default all four.}

\item{statistic}{What to downscale: \code{"trend"} (per-decade OLS slope over the
E-OBS years), \code{"mean"}, or \code{"value"} (a pre-reduced single layer).}

\item{variogram_model}{Optional \code{gstat::vgm()} model; \code{NULL} auto-fits.}

\item{years}{Optional numeric years matching the E-OBS layers (else taken
from \code{terra::time()}, else the layer index).}

\item{max_cells}{Cell cap for the target grid (default \code{5e5}); the DEM is
aggregated above it, so the output is never a gigapixel raster.}

\item{min_points}{Minimum E-OBS cells within the buffer to attempt kriging
(default 10); below it, trend-only.}

\item{cache_path}{Optional \code{.tif} path; when given, the result raster is
written there for instant reload (pattern of \code{pai.tif}).}

\item{...}{Ignored (forward-compat).}
}
\value{
A list \code{list(raster, meta)}. \code{raster} is a single-layer \code{SpatRaster}
in the DEM CRS, or \code{NULL} when degraded to nothing. \code{meta} carries the
\strong{output contract} the app renders against: \code{status}
(\code{"ok"}/\code{"out_of_scope"}/\code{"insufficient_data"}), \code{engine} (the engine that
actually ran), \code{method} (\code{"ked"}/\code{"trend_only"}), \code{var}, \code{statistic},
\code{crs} (EPSG code), \code{unit} (e.g. \code{"°C/decade"}), \code{value_label}, \code{palette}
(\code{low}/\code{high} quantile bounds and \code{sense = "hot_unfavorable"} — high =
warmer = red, per the app's red-is-critical rule), \code{n_points}, and, when
degraded, \code{reason} (i18n key).
}
\description{
Turn the coarse E-OBS grid (~0.1°, ~11 km) into a fine continuous
\code{SpatRaster} over the project's regional context, using the DEM (and terrain
covariates) as external drift. Built for the "regional context" map of the
reGénération tab — \strong{not} stand-scale precision, which \code{\link{microclimate_run}}
already produces from microclimf + HD LiDAR.
}
\details{
\strong{v1 covers \code{tx} (maximum temperature) only} — a physically justified
altitudinal signal (~ -0.6 °C / 100 m). \code{rr} (precipitation) is out of scope
(unreliable downscaling; the DEM only helps in mountains). \code{var = "rr"}
returns a status, not a raster.

\strong{Two engines, one contract} (microclimat brief §8):
\itemize{
\item \code{engine = "ked"} (default) — regression-kriging: reduce E-OBS to one value
per cell (the target \code{statistic}), fit the drift
\code{value ~ dem [+ slope + northness + twi]}, krige the residuals (variogram
auto-fit via \pkg{automap} when present, else a spherical \pkg{gstat} model),
sum drift + kriged residuals on the DEM grid. Without \pkg{gstat}, or with too
few E-OBS cells, it \strong{degrades to trend-only} (drift surface) — a documented
fallback, never an error.
\item \code{engine = "meteoland"} — the station-based interpolator (\pkg{meteoland},
Thornton 1997 + elevation, microclimat brief Option A / chantier P4). It
interpolates \strong{daily station series}, so downscaling a pre-reduced trend
needs a per-year restructuring delivered separately. Until then, and whenever
\pkg{meteoland} is absent, this engine \strong{falls back to KED} — the output
contract is identical, so the caller never branches on the engine.
}
}
\references{
E-OBS: Cornes et al. (2018). Regression-kriging: Hengl et al.
(2007). meteoland: De Cáceres et al. (2018).
}
\seealso{
\code{\link{tendances_estivales_eobs}}, \code{\link{microclimate_run}}
}
