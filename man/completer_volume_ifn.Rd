% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/completer_volume_ifn.R
\name{completer_volume_ifn}
\alias{completer_volume_ifn}
\title{Fill missing P1 volumes with the IFN regional reference}
\usage{
completer_volume_ifn(units, volume_col = "P1", species_field = "species",
  ser = NULL, min_plac = 30, mesure = c("present", "maille"),
  source_col = "volume_source")
}
\arguments{
\item{units}{An \code{sf} object.}

\item{volume_col}{Standing-volume column, m3/ha. Default \code{"P1"}.}

\item{species_field}{Column holding the species code, in \strong{any} of the
project's nomenclatures — IFN \code{espar}, four-letter P1 code, snake-case
tolerance code or Latin name. Resolved by \code{\link{resoudre_espar}}.}

\item{ser}{SER code for the units, a single string, or \code{NULL} for
national references.}

\item{min_plac}{Minimum plots for a mesh level to qualify. Default \code{30}.}

\item{mesure}{\code{"present"} (default) or \code{"maille"}; see below.}

\item{source_col}{Name of the added provenance column. Default
\code{"volume_source"}.}
}
\value{
\code{units} with \code{volume_col} completed and \code{source_col} added.
}
\description{
Where the standing-volume column is \code{NA}, substitutes the IFN reference
volume for that species and sylvoecoregion, walking the mesh down
SER -> GRECO -> national as needed (\code{\link{ifn_volume_reference}}).

This addresses the ordinary NDP 0 case: with public data only,
\code{\link{indicateur_p1_volume}} has neither a field inventory nor a CHM to
work from, and returns \code{NA}. A sourced regional figure is more useful than
a hole — provided nobody mistakes it for a measurement, hence
\code{source_col}.
}
\section{A measurement is never overwritten}{

Rows where \code{volume_col} is already filled are left strictly untouched.
Only \code{NA}s are completed. The added \code{source_col} records, per row,
where each value came from: \code{"mesure"} for the original values,
\code{"ifn_ser"}, \code{"ifn_greco"} or \code{"ifn_national"} for completed
ones, and \code{NA} where no reference could be found. Any downstream reader
can therefore separate measured from imputed — which the caller \strong{should}
do before reporting.
}

\section{Which figure is substituted}{

\code{mesure = "present"} (default) uses the mean volume over the plots where
the species actually occurs — a stand-level figure, comparable to a P1 computed
on a forest unit of that species. \code{"maille"} would use the species'
contribution to the regional mean, which is a resource figure and far too low
for this purpose.
}

\examples{
\dontrun{
units <- indicateur_p1_volume(units)          # NA en NDP 0
units <- completer_volume_ifn(units, species_field = "species", ser = "C20")
table(units$volume_source)
}
}
\seealso{
\code{\link{ifn_volume_reference}}, \code{\link{volume_mobilisable}}.
}
