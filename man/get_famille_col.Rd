% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family-system.R
\name{get_famille_col}
\alias{get_famille_col}
\title{Family score column name from family code}
\usage{
get_famille_col(code)
}
\arguments{
\item{code}{Character. Single-letter family code (e.g. \code{"C"}, \code{"B"}),
case-insensitive. Vectorised.}
}
\value{
Character vector of NMT family column names (e.g.
\code{"famille_carbone"}), same length as \code{code}.
}
\description{
Maps a family code to the name of the family score column produced by
\code{\link{create_family_index}} -- \code{"C"} becomes \code{"famille_carbone"}. That name is a
contract between the core and its consumers: it is the column carried by the
computed \code{sf}, and downstream packages use it as a stable identifier (tab
value, database column). Read it here rather than hard-coding the 12
strings; the whole mapping is also available as the \code{family_column} column
of \code{\link{indicator_families}}.
}
\examples{
get_famille_col("C")
get_famille_col(c("C", "b", "N"))

}
\seealso{
\code{\link{get_famille_code}} for the reverse lookup, \code{\link{indicator_families}}.
}
