% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_espar.R
\name{ifn_espar_correspondance}
\alias{ifn_espar_correspondance}
\title{Species-code correspondence across the project's three nomenclatures}
\usage{
ifn_espar_correspondance(espar = NULL, code_p1 = NULL)
}
\arguments{
\item{espar}{Optional filter on the IFN code.}

\item{code_p1}{Optional filter on the four-letter code.}
}
\value{
A data.frame with \code{espar}, \code{lib_espar}, \code{espece_sci},
\code{code_p1}, \code{code_tolerances}, \code{millesime}, \code{source}.
}
\description{
Bridge table between the IFN's own species codes (\code{espar}, e.g.
\code{"09"}), the four-letter codes used by
\code{\link{indicateur_p1_volume}} (e.g. \code{"FASY"}) and the snake-case
codes of \code{\link{european_species_tolerances}} (e.g.
\code{"fagus_sylvatica"}). The Latin binomial is the pivot.
}
\section{Coverage is uneven, by construction}{

Every \code{espar} has a Latin name, so \code{code_tolerances} resolves for
most rows. \code{code_p1} resolves only for the species present in
\code{ifn_volume_equations.csv} — a short list, since P1 falls back to
genus-level equations for anything else. A \code{NA} in \code{code_p1} is
therefore normal and means "no species-specific IFN tarif", not "unknown
species".

Matching is done on the Latin binomial, and also on the \strong{autonym} when
the IGN reference descends to infraspecific rank (\emph{Picea abies} subsp.
\emph{abies}). Where no autonym exists — \emph{Pinus nigra} is published only
as varieties — the row is left \code{NA} rather than assigned an arbitrary
variety.
}

\examples{
\dontrun{
ifn_espar_correspondance(espar = "09")
}
}
\seealso{
\code{\link{resoudre_espar}} to convert a vector of codes.
}
