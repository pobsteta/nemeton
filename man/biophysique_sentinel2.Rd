% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lai_prosail.R
\name{biophysique_sentinel2}
\alias{biophysique_sentinel2}
\title{Biophysical variable from Sentinel-2 via PROSAIL inversion (spec 042)}
\usage{
biophysique_sentinel2(variable = c("lai", "fapar", "fvc", "ccc"), aoi = NULL,
  refl = NULL, start = NULL, end = NULL, reducer = "p90",
  source = "muscate", sensor = "Sentinel_2A", selected_bands = NULL,
  geom_acq = NULL, mask = NULL, cache_dir = NULL, precomputed = NULL, ...)
}
\arguments{
\item{variable}{One of \code{"lai"} (default), \code{"fapar"}, \code{"fvc"}.
\code{"ccc"} is a compound (Cab x LAI), not a direct inversion target: it errors
and is deferred (spec 042 lot 4).}

\item{selected_bands}{S2 bands for the inversion. \code{NULL} picks a
per-variable default (currently \code{c("B4","B5","B8")} for all three —
provisional, spec 042 D3).}

\item{aoi, refl, start, end, reducer, source, sensor, geom_acq, mask, cache_dir, precomputed, ...}{
See \code{\link{lai_sentinel2}}.}
}
\value{
A single-layer \code{SpatRaster} named after \code{variable}, or \code{NULL} on
degradation (no \code{prosail}, no scene, engine failure).
}
\description{
Generalises \code{\link{lai_sentinel2}} to any of the directly-invertible
PROSAIL biophysical variables — \strong{LAI}, \strong{fAPAR} or \strong{FVC}
(fCover) — over \code{aoi} from Sentinel-2 L2A, by the same hybrid inversion
machinery. \code{\link{lai_sentinel2}} is a thin alias (\code{variable = "lai"}),
kept for backward compatibility.

Same two paths as \code{\link{lai_sentinel2}}: \emph{fast-path}
(\code{precomputed}, temporal reduction only) and \emph{engine path} (train/apply
a hybrid model, needs \code{prosail} + real S2 scenes — not runnable in CI).
}
\section{Scope (spec 042 lot 1)}{

Only the three \strong{direct} inversion targets are supported. \strong{CCC}
(canopy chlorophyll content) is a compound (Cab x LAI), not a direct target — it
errors and is deferred (lot 4). Per-variable band selection is
\strong{provisional}: the LAI default \code{c("B4","B5","B8")} is validated
(spec 033); for fAPAR/FVC the red-edge-optimal set is still open (spec 042 D3),
and their inversion is unvalidated pending the GEODES cross-check (lot 3).
}

\seealso{
\code{\link{lai_sentinel2}}
}
