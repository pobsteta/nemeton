% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regen_rank_species.R
\name{regen_rank_species}
\alias{regen_rank_species}
\title{Rank the top-N regeneration species per management unit}
\usage{
regen_rank_species(
  units,
  species_pool = NULL,
  top_n = 3,
  weights = NULL,
  exclude_invasive = TRUE,
  region = "BFC",
  cover_col = NULL,
  lai_col = NULL,
  extinction_k = .REGEN_RANK_K,
  id_col = "ug_id",
  include_atlas = FALSE,
  ...
)
}
\arguments{
\item{units}{An \code{sf} carrying the station columns produced by the regeneration
engines (\code{tmax_moyenne}, \code{d_tmax}, \code{vpd_canicule}, \code{rew_min}, and \code{R7} or
\code{r7_gel_days} for the frost axis).}

\item{species_pool}{\code{NULL} (default → \code{\link{regen_species_choices}} for \code{units}),
a character vector of species codes, or a \code{data.frame} of candidate traits.
Missing trait columns (e.g. \code{frost_late}) are joined from
\code{\link{european_species_tolerances}} by \code{code}.}

\item{top_n}{Number of species to keep per unit (default 3).}

\item{weights}{Optional named numeric overriding the axis weights
\code{c(chaleur_secheresse=, gel=, ombre=)} (default \code{c(0.5, 0.3, 0.2)},
renormalised over the axes actually present).}

\item{exclude_invasive}{Drop species flagged \code{invasif} (default \code{TRUE}).}

\item{region}{Region passed to \code{\link{regen_species_choices}} when building the
default pool.}

\item{cover_col, lai_col}{Optional column of \code{units} giving the per-unit residual
canopy density — a cover fraction (\code{cover_col}, 0-1, or 0-100 auto-rescaled) or
a leaf/plant area index (\code{lai_col}, converted with Beer-Lambert). \code{lai_col}
takes precedence. Both \code{NULL} → shade axis omitted.}

\item{extinction_k}{Beer-Lambert extinction coefficient for \code{lai_col}
(default 0.5).}

\item{id_col}{Unit id column (default \code{"ug_id"}; falls back to the row index).}

\item{include_atlas}{Also list the JRC-Atlas species in the default pool
(default \code{FALSE}).}

\item{...}{Reserved.}
}
\value{
A long \code{data.frame}, one row per \code{(unit × rank)}: \code{ug_id}, \code{rank},
\code{species_code}, \code{label}, \code{type}, \code{suitability} (0-100), \code{limiting_factor}
(\code{"chaleur"}/\code{"secheresse"}/\code{"gel"}/\code{"ombre"}, or \code{NA} when unconstrained),
\code{confidence}, \code{invasif}. Units with no station data yield a single \code{rank = NA}
row (no fabricated recommendation). An empty pool yields a 0-row frame.
The chosen axis weights are attached as \code{attr(, "weights")}.
}
\description{
Deterministic ecological suitability ranking of tree species for regeneration,
per management unit (UGF). For each \code{(unit × species)} it aggregates up to three
0-100 sub-scores (high = well suited) and returns the \code{top_n} species per unit
(spec 039). This is the \strong{deterministic core}; an LLM narrative layer lives in
the app (phase 2).

Axes:
\itemize{
\item \strong{Heat & dryness} — summer T°max (\code{tmax_moyenne + d_tmax}) and VPD
(\code{vpd_canicule}) against the species tolerances (\code{tmax_tol_c}, \code{vpd_tol_kpa}),
plus edaphic dryness (\code{rew_min}) against \code{drought_tol}. Liebig's law of the
minimum caps the axis at the worst stress (reuses \code{\link{indice_priorite_regen}}'s
thresholds \code{.REGEN_TOL_SPAN}).
\item \strong{Late frost} — station frost pressure (\code{R7}, else \code{r7_gel_days}) crossed
with the species \code{frost_late} sensitivity, so the axis \strong{differentiates}
species (an early-flushing beech is penalised on a frost-prone site, a
late-flushing oak is not).
\item \strong{Shade} \emph{(optional)} — per-unit canopy density (\code{cover_col}, or \code{lai_col}
converted with a Beer-Lambert \code{1 - exp(-k·LAI)}) crossed with \code{shade_tol}. When
no density input is supplied the axis is omitted (the score renormalises over
the present axes) and \code{shade_tol} breaks ranking ties instead.
}
}
\examples{
\dontrun{
  units <- indice_priorite_regen(units)          # station columns present
  ranked <- regen_rank_species(units, top_n = 3, lai_col = "lai_max")
  regen_rank_to_wide(ranked, top_n = 3)
}
}
\seealso{
\code{\link{regen_rank_to_wide}}, \code{\link{indice_priorite_regen}},
\code{\link{regen_species_choices}}, \code{\link{european_species_tolerances}}
}
