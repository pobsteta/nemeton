% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knowledge-corpus.R
\name{write_knowledge_manifest}
\alias{write_knowledge_manifest}
\title{Write the knowledge-corpus manifest}
\usage{
write_knowledge_manifest(
  manifest,
  path = knowledge_manifest_path(writable = TRUE),
  validate = TRUE
)
}
\arguments{
\item{manifest}{A data.frame, e.g. from [read_knowledge_manifest()].}

\item{path}{Character. Defaults to the writable project copy
([knowledge_manifest_path()] with `writable = TRUE`).}

\item{validate}{Logical. When `TRUE` (default), abort if
[validate_knowledge_manifest()] reports any `error` issue.}
}
\value{
Invisibly, the path written.
}
\description{
Validates the manifest (unless `validate = FALSE`) and writes it back as
CSV with deterministic, minimal quoting so version-control diffs stay
readable. Refuses to write a manifest carrying `error`-severity issues.
}
\seealso{
[read_knowledge_manifest()], [validate_knowledge_manifest()].
}
