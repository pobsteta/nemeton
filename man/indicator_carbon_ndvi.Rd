% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators-families.R
\name{indicator_carbon_ndvi}
\alias{indicator_carbon_ndvi}
\title{NDVI Mean and Trend Analysis (C2)}
\usage{
indicator_carbon_ndvi(units, layers, ndvi_layer = "ndvi", trend = FALSE)
}
\arguments{
\item{units}{nemeton_units object}

\item{layers}{nemeton_layers object containing NDVI raster(s)}

\item{ndvi_layer}{Character. Name of NDVI layer in layers object}

\item{trend}{Logical. Calculate temporal trend if multiple dates available?
Default FALSE.}
}
\value{
Numeric vector of NDVI mean values (0-1 scale), or list with
  mean and trend if trend = TRUE
}
\description{
Extracts mean NDVI from Sentinel-2 or equivalent satellite imagery.
Optionally calculates NDVI trend over multiple dates (requires temporal rasters).
}
\examples{
\dontrun{
# Single-date NDVI
layers <- nemeton_layers(rasters = list(ndvi = "sentinel2_ndvi.tif"))
results <- indicator_carbon_ndvi(units, layers, ndvi_layer = "ndvi")

# Multi-date NDVI with trend
results <- indicator_carbon_ndvi(units, layers, ndvi_layer = "ndvi", trend = TRUE)
}
}
