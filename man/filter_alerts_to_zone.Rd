% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zone_aoi.R
\name{filter_alerts_to_zone}
\alias{filter_alerts_to_zone}
\title{Restrict an alert layer to the UGF zone, at read time (spec 021 L7)}
\usage{
filter_alerts_to_zone(
  alerts,
  con = NULL,
  zone_id = NULL,
  apply_zone_mask = TRUE,
  mask_polygon = NULL
)
}
\arguments{
\item{alerts}{An \code{sf} POINT layer (e.g. \code{result$alerts_sf} from
\code{\link{run_reconfort_dieback}} / \code{\link{run_fordead_dieback}}, or rows read
from the \code{alert} table). Returned unchanged if not a non-empty \code{sf}.}

\item{con}{A \code{DBIConnection}, used only to resolve the zone polygon
via \code{.get_zone_aoi()} when \code{mask_polygon} is \code{NULL}.}

\item{zone_id}{Integer scalar identifying the row in \code{monitoring_zone}.}

\item{apply_zone_mask}{If \code{TRUE} (default), keep only in-zone alerts.
\code{FALSE} returns the layer unchanged.}

\item{mask_polygon}{An explicit \code{sf}/\code{sfc} polygon overriding the DB
lookup (e.g. a single stratum). When \code{NULL}, the polygon is resolved
from \code{con} + \code{zone_id}.}
}
\value{
An \code{sf} (filtered to the UGF zone unless \code{apply_zone_mask =
FALSE} or no polygon could be resolved).
}
\description{
Vector counterpart of the read-time raster masking
(\code{\link{read_reconfort_layer}}, \code{\link{read_fast_alert_raster}},
\code{\link{read_fordead_dieback_mask}}, spec 016). Keeps only the alert
centroids that fall \strong{inside the UGF zone polygon}, so a viewer no
longer shows alerts outside the user's managed perimeter.

Shared by the three health pipelines (RECONFORT, FORDEAD, FAST): the
filtering operation is identical for any \code{sf} POINT alert layer, so a
single core helper provides true parity and keeps the spatial
predicate out of the presentation layer (ADR-009, CLAUDE.md strict
rules §1-3). The persisted \code{alert} table is \strong{not} modified — the
filter is applied at read/display time only (spec 016 principle
"mask at read, not write"); the per-zone \code{zone_id} provenance is kept.
}
\seealso{
\code{\link{read_reconfort_layer}}, \code{\link{read_fast_alert_raster}},
\code{\link{read_fordead_dieback_mask}}
}
