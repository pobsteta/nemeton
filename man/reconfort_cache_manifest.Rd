% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconfort_manifest.R
\name{reconfort_cache_manifest}
\alias{reconfort_cache_manifest}
\title{Discover the persisted RECONFORT display layers from the cache (L6)}
\usage{
reconfort_cache_manifest(cache_dir, zone_id, run_id = NULL, include_range = FALSE)
}
\arguments{
\item{cache_dir}{Project RECONFORT cache directory. The zone layers are
resolved at \code{<cache_dir>/zone_<zone_id>/} or, as a fallback,
\code{<cache_dir>/reconfort/zone_<zone_id>/}.}

\item{zone_id}{Scalar monitoring-zone id.}

\item{run_id}{Run timestamp, or \code{NULL} (default) to pick the most
recent run in the zone cache.}

\item{include_range}{If \code{TRUE}, fill \code{vmin}/\code{vmax} of the continuous
rasters with their actual \code{terra::minmax()}. Default \code{FALSE}.}
}
\value{
A \code{data.frame} with the same columns as
\code{\link{reconfort_layer_manifest}} (one row per available display raster).
Best-effort: a missing cache / zone / run yields a zero-row frame.
}
\description{
Cache-side counterpart of \code{\link{reconfort_layer_manifest}}: rebuilds the
layer manifest of a RECONFORT run from the rasters persisted under the
project cache, \strong{without} the in-memory \code{result}. This lets a viewer
redraw the RECONFORT rasters after a project reload (parity with
\code{\link{read_fordead_layer}} / \code{\link{read_fordead_dieback_mask}}, which read
their layers from the cache).

Primary source is the IOTA² \code{final/} output dir
(\code{output_zone_<id>/results/iota2_results_classif_labels-z<id>-S2_*/final/}),
where the display rasters persist across reloads:
\code{Final_continuous_score_masked*.tif} (score),
\code{Final_Classif_masked_*.tif} (classification, fallback \code{Classif_Seed_0.tif})
and \code{Final_Proba_map_masked*.tif} (probability, fallback
\code{ProbabilityMap_seed_0.tif}). When that dir is absent (workdir cleaned) or
an older run is requested, it falls back to the run-scoped copies under
\code{zone_<id>/} (\code{reconfort_mask_<run_id>.tif} / \code{reconfort_score_<run_id>.tif}
/ \code{reconfort_proba_<run_id>.tif}). The CRswir / CRre multi-band stacks (a
time series consumed by \code{\link{read_reconfort_pixel_series}}) are \strong{not}
display layers and are excluded.

The output is byte-for-byte interchangeable with
\code{\link{reconfort_layer_manifest}} (same columns, types and rendering hints),
so the caller reuses the same machinery (\code{\link{read_reconfort_layer}},
raster cache, toggles, opacity). Alerts are not included (they are read
from the \code{alert} table independently).
}
\seealso{
\code{\link{reconfort_layer_manifest}}, \code{\link{read_reconfort_layer}},
\code{\link{run_reconfort_dieback}}
}
