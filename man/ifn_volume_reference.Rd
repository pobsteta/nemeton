% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_volume_ser.R
\name{ifn_volume_reference}
\alias{ifn_volume_reference}
\title{Reference standing volume, with a SER -> GRECO -> national fallback}
\usage{
ifn_volume_reference(espar, ser = NULL, min_plac = 30,
  mesure = c("present", "maille"))
}
\arguments{
\item{espar}{IFN species code(s), character.}

\item{ser}{SER code, a single string (e.g. \code{"C20"}). Its first letter
gives the GRECO. \code{NULL} starts the ladder at the national level.}

\item{min_plac}{Minimum number of plots carrying the species for a level to be
accepted. Default \code{30}.}

\item{mesure}{Which volume column to return: \code{"present"} (default, the
stand-level figure) or \code{"maille"} (the regional resource figure). See
\code{\link{ifn_volume_essence_ser}}.}
}
\value{
A data.frame with one row per \code{espar}: \code{espar},
\code{libelle_essence}, \code{vol_ha}, \code{niveau_utilise}
(\code{"ser"}/\code{"greco"}/\code{"national"}, or \code{NA} when no level
qualified), \code{n_plac_presence}, \code{ser}, \code{greco}.
}
\description{
Returns one reference volume per requested species, walking down the mesh
until the cell rests on enough plots: sylvoecoregion, then greater ecological
region, then national. The level actually used is reported, so a caller can
tell a regional figure from a national one — never silently.

This mirrors the fallback already used by \code{\link{indicateur_p1_volume}}
(species -> genus -> conifer/broadleaf): degrade the resolution rather than
return a figure resting on three plots.
}
\examples{
\dontrun{
# Beech and silver fir in sylvoecoregion C20.
ifn_volume_reference(c("09", "61"), ser = "C20")
}
}
\seealso{
\code{\link{ifn_volume_essence_ser}} for the raw table.
}
