% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lai_prosail.R
\name{lai_sentinel2}
\alias{lai_sentinel2}
\title{LAI from Sentinel-2 via PROSAIL inversion — NDP-0 canopy fallback (spec 033)}
\usage{
lai_sentinel2(
  aoi = NULL,
  refl = NULL,
  start = NULL,
  end = NULL,
  reducer = "p90",
  source = "muscate",
  sensor = "Sentinel_2A",
  selected_bands = c("B3", "B4", "B8"),
  geom_acq = NULL,
  mask = NULL,
  cache_dir = NULL,
  precomputed = NULL,
  ...
)
}
\arguments{
\item{aoi}{An \code{sf}/\code{sfc} extent (used by the MUSCATE auto-fetch path).}

\item{refl}{S2 L2A reflectance for the engine path: a \code{SpatRaster} (bands in
\code{srf} order) or a file path / vector of paths (one per date). When
\code{NULL}, a best-effort MUSCATE fetch is attempted over
\code{aoi}/\code{start}/\code{end}.}

\item{start, end}{Date bounds (\code{"YYYY-MM-DD"}) for the S2 search.}

\item{reducer}{Temporal reducer over dates: \code{"p90"} (default, D1),
\code{"max"}, \code{"median"} or \code{"mean"}.}

\item{source}{S2 STAC backend (default \code{"muscate"}, spec 029).}

\item{sensor}{PROSAIL sensor SRF: \code{"Sentinel_2A"} (default),
\code{"Sentinel_2B"}.}

\item{selected_bands}{S2 bands used for the LAI inversion (default
\code{c("B3","B4","B8")}).}

\item{geom_acq}{Optional acquisition-geometry range (\code{list(min=, max=)} of
\code{data.frame(tto, tts, psi)}); default a France-summer range.}

\item{mask}{Optional cloud/shadow mask raster path for the application.}

\item{cache_dir}{Directory for the trained model and intermediate rasters
(default: a session temp dir).}

\item{precomputed}{Optional pre-computed LAI (\code{SpatRaster} or file path).}

\item{...}{Reserved.}
}
\value{
A single-layer LAI \code{SpatRaster} (\code{lai}), or \code{NULL} on degradation
(no \code{prosail}, no scene, engine failure).
}
\description{
Retrieve a \strong{LAI} raster over \code{aoi} from \strong{Sentinel-2 L2A} by
\strong{PROSAIL hybrid inversion} (\code{prosail}), as the NDP-0 fallback for the
reGénération canopy inputs when LiDAR HD is absent — \code{lai_max} of
\code{\link{regen_bilan_hydrique}} (direct fit) and, as a degraded proxy,
\code{pai} of \code{\link{regen_sensibilite}} (LAI is leaves only; PAI includes
wood). \strong{NDP ≥ 1 always keeps the structural LiDAR PAI of
\code{\link{pai_depuis_nuage}}.}
}
\details{
\strong{Two paths.} \emph{Fast-path}: pass \code{precomputed} (a LAI
\code{SpatRaster}, or a multi-date stack) and only the temporal reduction
(\code{reducer}, default \code{"p90"}) is applied. \emph{Engine path}: trains the
PROSAIL hybrid model for the S2 sensor/geometry (cached in \code{cache_dir}),
applies it to the S2 reflectance raster(s) \code{refl}, and reduces the per-date
LAI to one layer. Needs \code{prosail} + real S2 scenes and is \strong{not
runnable in CI} — the engine is validated on real data (training verified;
application per the official tutorial).
}
\seealso{
\code{\link{regen_bilan_hydrique}}, \code{\link{regen_sensibilite}},
\code{\link{pai_depuis_nuage}}
}
