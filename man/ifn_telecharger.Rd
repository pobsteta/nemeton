% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn_source.R
\name{ifn_telecharger}
\alias{ifn_telecharger}
\title{Download the raw French NFI export}
\usage{
ifn_telecharger(dest_dir, campagne = NULL, force = FALSE)
}
\arguments{
\item{dest_dir}{Directory holding the cached archive. Created if needed.}

\item{campagne}{Campaign year to fetch. \code{NULL} (default) resolves the most
recent one via \code{\link{ifn_campagne_disponible}}.}

\item{force}{Re-download even when the archive is already cached.}
}
\value{
The path to the cached \code{.zip}, invisibly, with attributes
\code{campagne} and \code{millesime}.
}
\description{
Fetches the IGN annual export archive and caches it in \code{dest_dir}. An
archive already present is reused, never re-downloaded silently and never by
asking the user a question — see the note below.
}
\section{Non-interactive by design}{

\code{FrenchNFIfindeR::get_NFI()} calls \code{readline()} when raw data is
already on disk, asking whether to reuse or re-download. That blocks in any
non-interactive context — a \code{future} worker, a CI job, a scheduled build —
which is exactly where a download step belongs. Here the choice is an argument:
\code{force = FALSE} reuses, \code{force = TRUE} re-downloads.
}

\examples{
\dontrun{
zip <- ifn_telecharger(tempdir())
}
}
