% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alerts.R
\name{detect_alerts}
\alias{detect_alerts}
\title{Detect drops in NDVI / NBR time series}
\usage{
detect_alerts(
  con,
  zone_id,
  threshold_ndvi_drop = 0.15,
  threshold_nbr_drop = 0.25,
  window_days = 30
)
}
\arguments{
\item{con}{A `DBIConnection` returned by [db_connect()].}

\item{zone_id}{Integer. Restrict detection to plots of this zone.}

\item{threshold_ndvi_drop}{Numeric. Minimum drop in NDVI (rolling
mean − current value) to flag as `ndvi_drop`. Default 0.15.}

\item{threshold_nbr_drop}{Numeric. Minimum drop in NBR. Default 0.25.}

\item{window_days}{Integer. Length of the rolling baseline window
in days. Default 30.}
}
\value{
An sf POINT object (CRS WGS84) with one row per alert,
  columns `plot_id`, `alert_type`, `trigger_date`, `value_before`,
  `value_after`, `delta`. Empty sf when no alert is found.
}
\description{
\strong{Deprecated since v0.58.0; scheduled for removal in v0.60.0.}
This is the legacy per-placette rolling-window alert detector. Since
spec 017 the FAST diagnostic is a pure per-pixel raster from the COG
cache (\code{\link[=read_fast_alert_raster]{read_fast_alert_raster()}} /
\code{\link[=compute_fast_alert_mask]{compute_fast_alert_mask()}}),
independent of the validation sampling plan. The `obs_pixel` table it
reads was dropped in v0.58.0, so this function will fail at the SQL
stage.
}
\keyword{internal}
