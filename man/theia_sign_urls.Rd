% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theia_stac.R
\name{theia_sign_urls}
\alias{theia_sign_urls}
\title{Sign THEIA object URLs via the teledetection signing gateway}
\usage{
theia_sign_urls(
  urls,
  access_key = NULL,
  secret_key = NULL,
  endpoint = NULL,
  country = "FR"
)
}
\arguments{
\item{urls}{Character vector of \verb{https://<...>.meso.umontpellier.fr/...}
object URLs to sign. Non-MESO URLs are returned unchanged.}

\item{access_key, secret_key}{THEIA API key pair (default the
\env{TLD_ACCESS_KEY} / \env{TLD_SECRET_KEY} environment variables; create
one at \url{https://gate.stac.teledetection.fr}).}

\item{endpoint}{Signing gateway base URL (default
\code{services$theia_signing$endpoint} from the country config, else
\code{https://signing.stac.teledetection.fr}).}

\item{country}{Country config key (default \code{"FR"}).}
}
\value{
A named character vector mapping each input URL to its signed
counterpart (names are the original URLs), in the input order.
}
\description{
THEIA / DATA TERRA assets (MUSCATE Sentinel-2, FORMS, …) live on the MESO@UM
S3 store but are \strong{not} readable with the portal API key directly: the
store only recognises \strong{pre-signed URLs} minted by the teledetection
signing gateway (\code{signing.stac.teledetection.fr}), the same model as
Planetary Computer's SAS tokens. This function POSTs the object URLs to the
gateway (authenticated with the API key sent as \code{access-key} /
\code{secret-key} headers) and returns short-lived pre-signed URLs
(\verb{X-Amz-*} query string, ~8 h) that GDAL reads with \verb{/vsicurl/} — no
\code{\link{theia_configure_s3}} needed.
}
\seealso{
\code{\link{theia_configure_s3}}, \code{\link{resolve_theia_assets}}
}
