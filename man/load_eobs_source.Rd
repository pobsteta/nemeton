% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_eobs.R
\name{load_eobs_source}
\alias{load_eobs_source}
\title{Acquire E-OBS per-year summer fields (Tmax / precipitation) for an AOI}
\usage{
load_eobs_source(
  aoi,
  var = "tx",
  years = NULL,
  months = 6:8,
  source = "cds",
  reducer = NULL,
  nc = NULL,
  cache_dir = NULL,
  version = "28.0e",
  resolution = "0.1deg",
  period = NULL,
  progress_callback = NULL,
  ...
)
}
\arguments{
\item{aoi}{An \pkg{sf}/\pkg{sfc} of the management units (their union crops E-OBS).}

\item{var}{E-OBS variable: \code{"tx"} (max temperature, default), \code{"tg"}
(mean temperature) or \code{"rr"} (precipitation).}

\item{years}{Optional integer years to keep (default: all years present).}

\item{months}{Summer months to reduce over (default \code{6:8}, JJA).}

\item{source}{\code{"cds"} for the automatic Copernicus download, anything else
requires \code{nc}.}

\item{reducer}{Temporal reducer over the summer days: default \code{"mean"} for
\code{tx}/\code{tg}, \code{"sum"} for \code{rr}. Also \code{"max"}, \code{"median"}.}

\item{nc}{A daily E-OBS netCDF path (or a dated \code{SpatRaster}) to use instead
of the CDS download.}

\item{cache_dir}{Directory for the CDS download / unzip (default \code{tempdir()}).}

\item{version, resolution}{E-OBS product version (default \code{"28.0e"}) and grid
resolution (default \code{"0.1deg"}) for the CDS request.}

\item{period}{Optional explicit CDS period block (e.g. \code{"2011_2024"});
inferred from \code{years} when \code{NULL}.}

\item{progress_callback}{Optional function called at each step with a
\code{list(current = <key>, ...)} payload (monitoring pattern). Keys:
\code{"eobs:cds_request"}, \code{"eobs:cds_download_done"}, \code{"eobs:unzip"},
\code{"eobs:read"}, \code{"eobs:reduce"}, \code{"eobs:complete"},
\code{"eobs:unavailable"}. The app maps these to bottom-right notifications.}

\item{...}{Ignored (forward-compat).}
}
\value{
A per-year summer \code{SpatRaster} (layers named by year), or \code{NULL} on
graceful degradation.
}
\description{
Build the per-year summer \code{SpatRaster} (one layer per year, named by year)
that \code{\link{microclimate_detect_years}} and
\code{\link{tendances_estivales_eobs}} consume, from the European E-OBS gridded
dataset (ECA&D / Copernicus). Two paths (spec 034):
\itemize{
\item \strong{Injection (\code{nc})} — pass a daily E-OBS netCDF path (downloaded
from the CDS web interface or ECA&D) or a dated \code{SpatRaster}; the core
reduces it to summer-per-year. This is the tested contract.
\item \strong{CDS (\code{source = "cds"})} — best-effort automatic download via
\pkg{ecmwfr} (dataset \code{insitu-gridded-observations-europe}, the same CDS key
as ERA5). Not runnable in CI; validated on real data. Degrades to \code{NULL}.
}
}
\seealso{
\code{\link{microclimate_detect_years}}, \code{\link{tendances_estivales_eobs}}
}
