% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theia_stac.R
\name{stac_search_items}
\alias{stac_search_items}
\title{Search a STAC API for items}
\usage{
stac_search_items(stac_api, collection, bbox, datetime = NULL, limit = 100L)
}
\arguments{
\item{stac_api}{Character. Root URL of the STAC API (the
\code{/search} suffix is appended automatically).}

\item{collection}{Character. STAC collection id to query.}

\item{bbox}{Numeric length 4: \code{c(xmin, ymin, xmax, ymax)} in
WGS84.}

\item{datetime}{Optional character. A STAC datetime filter — a
single instant, a closed interval \code{"start/end"}, or a
half-open one (\code{"start/.."}). \code{NULL} = no time filter.}

\item{limit}{Integer. Maximum number of items to return. Default
\code{100}.}
}
\value{
A list of STAC item features (parsed JSON objects).
}
\description{
Endpoint-agnostic STAC item search: POSTs a search request to a
STAC API \code{/search} endpoint and returns the matching item
features. Pagination is handled by the project-wide STAC paginator.
}
\examples{
\dontrun{
items <- stac_search_items(
  "https://stac.example.org", "forms-t",
  bbox = c(6.0, 47.8, 6.3, 48.0), datetime = "2023-01-01/2023-12-31"
)
}

}
