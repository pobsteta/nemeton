% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migration-famille-l.R
\name{migrer_colonnes_l}
\alias{migrer_colonnes_l}
\title{Rename legacy landscape (L) columns}
\usage{
migrer_colonnes_l(data, quiet = FALSE)
}
\arguments{
\item{data}{A \code{data.frame} or \code{sf} object holding indicator columns.}

\item{quiet}{Logical. \code{TRUE} silences the report of what was renamed.
Default \code{FALSE}.}
}
\value{
\code{data}, with the legacy columns renamed. Attributes, row order,
geometry and values are untouched.
}
\description{
Renames the two landscape columns that were retired in 0.176.0, \strong{without
touching a single value}:

\preformatted{indicateur_l2_fragmentation -> indicateur_l1_effet_lisiere   (sylvosphere)
indicateur_l1_sylvosphere   -> indicateur_l2_morcellement    (fragmentation)
}

Both old names announced the opposite of what they carried -- see spec 045.
The \code{_norm} variants produced by \code{\link{normalize_indicators}} follow.
Short-code columns (\code{L1}, \code{L2}) are left alone: they were already paired
correctly.

Call it once when reading a dataset computed before 0.176.0 (project
parquet, PostGIS table, cached GeoPackage). A dataset that carries neither
legacy column is returned unchanged, so the call is safe to keep in a
reading path.
}
\section{Conflicts}{

When a legacy column and its target both exist, the target is kept, the
legacy one is dropped, and a warning names both -- an already migrated
dataset re-read alongside a stale export must not silently overwrite the
current values.
}

\examples{
old <- data.frame(
  id = 1:2,
  indicateur_l2_fragmentation = c(36.4, 34.5),
  indicateur_l1_sylvosphere = c(71.2, 68.0)
)
names(migrer_colonnes_l(old, quiet = TRUE))

}
\seealso{
\code{\link{indicateur_l1_effet_lisiere}}, \code{\link{indicateur_l2_morcellement}}
}
