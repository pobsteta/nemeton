% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_onf_parcelles.R
\name{load_onf_parcelles_source}
\alias{load_onf_parcelles_source}
\title{Acquire the ONF public-forest parcel layer for an AOI}
\usage{
load_onf_parcelles_source(
  aoi,
  crs = 2154,
  domanialite = c("toutes", "domaniale", "autre"),
  territoire = "FR",
  max_parcelles = 5000L,
  clip = FALSE
)
}
\arguments{
\item{aoi}{An \pkg{sf}/\pkg{sfc} project extent (must have a defined CRS).}

\item{crs}{Target EPSG of the returned layer. Default \code{2154}.}

\item{domanialite}{Ownership filter: \code{"toutes"} (default),
\code{"domaniale"} (state forests only) or \code{"autre"} (local-authority
and other public forests). Parcels whose ownership could not be resolved are
dropped by the two filtering modes.}

\item{territoire}{Territory served by the WFS: \code{"FR"} (metropolitan,
default), \code{"GLP"}, \code{"MTQ"}, \code{"GUF"}, \code{"REU"} or
\code{"MYT"}.}

\item{max_parcelles}{Maximum number of parcels requested (\code{COUNT}).
Guards against pulling a whole region; a warning is emitted when the service
announces more matches than returned. Default \code{5000}.}

\item{clip}{Clip parcels to \code{aoi} instead of keeping whole parcels that
intersect it. Default \code{FALSE} — a UGF is normally the entire parcel.}
}
\value{
An \pkg{sf} of forest parcels in \code{crs} with columns \code{id}
(\code{<forêt>-<parcelle>}), \code{foret_id}, \code{foret_nom},
\code{parcelle}, \code{domaniale}, \code{nom_ugf}, \code{contenance} (m²,
computed in the territory's projected CRS) and \code{surface_ha}; a 0-row
\pkg{sf} if none; \code{NULL} on failure.
}
\description{
Fetch the \strong{forest parcels of French public forests} (state-owned
\emph{domaniales} and local-authority forests under the \emph{régime
forestier}) over \code{aoi} from the ONF "Forêts publiques" WFS (Carmen, ONF
as producer, public diffusion), and return them ready to be turned into
forest management units (UGF): one row per parcel, with a stable id, a
display label, the owning forest and its ownership status.

The forest parcel is the reference frame ONF uses on the ground for
management, so it is a far better UGF seed than a cadastral parcel. Note the
grain is the \strong{parcel}, not the sub-parcel: the finer management unit
of the \emph{aménagement} is not published, so a parcel may still mix stands.

Degrades gracefully — returns \code{NULL} on any failure (no network, service
firewall rejection, OWS exception, unknown territory) so the caller can fall
back to cadastral selection. A 0-row \pkg{sf} is returned when the AOI simply
holds no public forest.
}
\seealso{
\code{\link{load_foret_ancienne_source}}, \code{\link{get_layer_service}}
}
