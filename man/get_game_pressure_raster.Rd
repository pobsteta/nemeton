% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-hunting.R
\name{get_game_pressure_raster}
\alias{get_game_pressure_raster}
\title{Get Game Pressure Raster for R4 Indicator}
\usage{
get_game_pressure_raster(units, pressure_data = NULL, dept_boundaries = NULL)
}
\arguments{
\item{units}{sf object. Forest parcels to determine spatial extent and CRS.}

\item{pressure_data}{Data.frame from \code{\link{compute_game_pressure_index}},
or NULL to compute automatically.}

\item{dept_boundaries}{sf object. Department boundaries with code_dept column,
or NULL to download from IGN.}
}
\value{
A SpatRaster with game pressure index (0-100) per pixel,
  matching the extent and CRS of the input units.
}
\description{
Creates a SpatRaster of game pressure index by department for use with
the \code{\link{indicator_risk_browsing}} function.
}
\details{
This function:
1. Downloads/computes game pressure index by department
2. Downloads department boundaries if not provided
3. Rasterizes the pressure values
4. Returns a raster for use with indicator_risk_browsing(game_density = ...)
}
\examples{
\dontrun{
library(nemeton)
library(sf)

# Load parcels
parcels <- st_read("parcels.gpkg")

# Get game pressure raster
game_raster <- get_game_pressure_raster(parcels)

# Use with R4 indicator
result <- indicator_risk_browsing(
  parcels,
  species_field = "essence",
  game_density = game_raster
)
}
}
\seealso{
Other data-acquisition: 
\code{\link{compute_game_pressure_index}()},
\code{\link{download_hunting_data}()}
}
\concept{data-acquisition}
