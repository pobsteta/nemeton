% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_diversity.R
\name{indicateur_b4_div_spectrale}
\alias{indicateur_b4_div_spectrale}
\title{Indicator B4 — Spectral alpha diversity (family B)}
\usage{
indicateur_b4_div_spectrale(
  units,
  spectral = NULL,
  reflectance = NULL,
  column_name = "B4",
  ...
)
}
\arguments{
\item{units}{sf polygon layer (spatial units / UGF).}

\item{spectral}{Optional precomputed result of
\code{\link{compute_spectral_diversity}} (preferred — compute once, share
with L3). When \code{NULL}, falls back to \code{reflectance}.}

\item{reflectance}{Optional reflectance \code{SpatRaster} / path used to
compute spectral diversity on the fly when \code{spectral} is
\code{NULL}.}

\item{column_name}{Output column name (default \code{"B4"}).}

\item{...}{Passed to \code{\link{compute_spectral_diversity}} when computing
on the fly (e.g. \code{window_size}, \code{mask}, \code{nb_cpu}).}
}
\value{
\code{units} with the numeric \code{column_name} column added.
}
\description{
Per-unit Shannon alpha diversity of spectral species (biodivMapR),
a remote-sensing proxy for compositional biodiversity available from
NDP 0 (Sentinel-2). Strictly backward compatible: with neither
\code{spectral} nor \code{reflectance}, the column is filled with
\code{NA} and the \code{sf} is returned unchanged.
}
\seealso{
\code{\link{compute_spectral_diversity}}
}
