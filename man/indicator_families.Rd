% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator-config.R
\name{indicator_families}
\alias{indicator_families}
\title{Indicator family table}
\usage{
indicator_families(codes = NULL, lang = c("fr", "en"))
}
\arguments{
\item{codes}{Character vector of family codes (case-insensitive), or \code{NULL}
(default) for all 12 families in canonical order
\code{C B W A F L T R S P E N}. When supplied, rows are returned in the order
given.}

\item{lang}{Character. Language copied into the convenience columns \code{name},
\code{description}, \code{labels} and \code{tooltips}: \code{"fr"} (default) or \code{"en"}. The
\code{_fr} / \code{_en} columns are returned regardless.}
}
\value{
A \code{data.frame} with one row per family and the columns:
\describe{
\item{code}{Family code (\code{"C"}, \code{"B"}, ...).}
\item{family_column}{Name of the family score column produced by
\code{\link{create_family_index}} (\code{"famille_carbone"}, ...). See
\code{\link{get_famille_col}}.}
\item{name}{Family name in \code{lang}.}
\item{name_fr, name_en}{Family name in both languages.}
\item{description}{One-line description of the family in \code{lang}.}
\item{description_fr, description_en}{Description in both languages.}
\item{icon}{Bootstrap icon name.}
\item{color}{Semantic hex color (see \emph{Colors}).}
\item{indicators}{List column: character vector of indicator codes
(\code{"C1"}, \code{"C2"}, ...).}
\item{column_names}{List column: character vector of the produced column
names, paired by position with \code{indicators}.}
\item{labels}{List column: named character vector of indicator labels in
\code{lang}, named by indicator code.}
\item{labels_fr, labels_en}{Same, in each language.}
\item{tooltips}{List column: named character vector of indicator
tooltips in \code{lang}, named by indicator code.}
\item{tooltips_fr, tooltips_en}{Same, in each language.}
}
}
\description{
Public, stable view of the 12 indicator families of the Nemeton framework.
This is the canonical source for family codes, display names and the
indicators each family aggregates. Downstream packages (notably
\code{nemetonshiny}) should read it instead of re-declaring the list, so that
renaming a family in the core propagates instead of silently diverging.

The function is pure (no I/O, no state), and therefore safe to call from a
\code{future} worker.
}
\section{Column pairing}{

\code{indicators} and \code{column_names} are always the same length and are paired
\strong{by position}: \code{column_names[[i]]} is the column produced for
\code{indicators[[i]]}. Do \strong{not} derive one from the other by string
manipulation: two families carry a legacy naming swap, where the short code
and the column slug disagree.
\itemize{
\item \code{F1} is \code{indicateur_f2_erosion} and \code{F2} is \code{indicateur_f1_fertilite}.
\item \code{L1} is \code{indicateur_l2_fragmentation} and \code{L2} is
\code{indicateur_l1_sylvosphere}.
}
The swap lives in the \strong{function names}, not in the pairing. A column is
named after the function that fills it (\code{compute_indicator()} looks the
function up by the indicator name), and for these four the function name
contradicts what the function computes:
\code{indicateur_l2_fragmentation()} returns the sylvosphere / edge-effect score
(shape index, matrix contrast, exposure) while \code{indicateur_l1_sylvosphere()}
returns the landscape fragmentation score (landscapemetrics COHESION + AI).
The labels therefore follow the short code \strong{and} the values the paired
column carries -- \code{labels[["L1"]]} says sylvosphere because the column
\code{indicateur_l2_fragmentation} holds sylvosphere values, whatever its slug
suggests. Reading the slug alone inverts the two, and swapping the labels to
match it would mislabel the maps built from these columns.
}

\section{Both languages, always}{

Every translatable field is returned in \strong{both} languages, in dedicated
\code{_fr} / \code{_en} columns. \code{lang} only selects which of them is copied into the
convenience columns \code{name}, \code{description}, \code{labels} and \code{tooltips}. A caller
that switches language at runtime -- or that needs a fallback when one
language is missing -- never has to call the function twice.
}

\section{Colors}{

\code{color} carries the \emph{semantic} palette of the core (forest green for carbon,
water blue for the water family). It is deliberately not the palette used by
the Shiny application, which applies viridis for colorblind accessibility.
Consumers who need an accessible palette should ignore this column.
}

\examples{
fams <- indicator_families()
fams$code
fams[fams$code == "C", "name"]

# Loop over the families to build a menu
for (i in seq_len(nrow(fams))) {
  cat(sprintf("\%s (\%s)\n", fams$name[i], fams$code[i]))
}

# A subset, in English
indicator_families(c("C", "W"), lang = "en")$name

# Both languages are always there, whatever `lang`
fams$labels_en[[1]]

# The family score column produced by create_family_index()
fams$family_column

}
\seealso{
\code{\link{indicator_labels}} for a long-format, one-row-per-indicator view.
}
