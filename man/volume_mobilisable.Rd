% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume_mobilisable.R
\name{volume_mobilisable}
\alias{volume_mobilisable}
\title{Mobilisable timber volume for road-network sizing}
\usage{
volume_mobilisable(units, volume_col = "P1", unite = c("m3_total", "m3_ha"),
  taux_prelevement = NULL, horizon_ans = NULL,
  na_policy = c("na", "zero", "error"),
  column_name = "volume_mobilisable")
}
\arguments{
\item{units}{An \code{sf} object carrying the P1 column.}

\item{volume_col}{Name of the standing-volume column, in m3/ha, as produced by
\code{\link{indicateur_p1_volume}}. Default \code{"P1"}.}

\item{unite}{Output unit: \code{"m3_total"} (default, for \code{calculer_flux()})
or \code{"m3_ha"} (for \code{reseau_desserte()} / \code{optimiser_reseau()}).}

\item{taux_prelevement}{Harvest rate in m3/ha/year: a single number applied to
every unit, or a numeric vector of \code{nrow(units)}. \code{NULL} (default)
would select the not-yet-sourced IFN table and therefore errors.}

\item{horizon_ans}{Planning horizon in years. Required whenever
\code{taux_prelevement} is supplied.}

\item{na_policy}{What to do with units whose volume is \code{NA} — typically the
NDP 0 case, where P1 has neither field inventory nor CHM. \code{"na"} (default)
propagates \code{NA} and warns; \code{"zero"} maps them to 0 (\strong{paints an
uninventoried parcel as "nothing to haul"}); \code{"error"} aborts.}

\item{column_name}{Name of the added column. Default \code{"volume_mobilisable"}.}
}
\value{
\code{units} with the \code{column_name} column added.
}
\description{
Turns the P1 standing-volume indicator into the volume column expected by the
\code{foretaccess} road-network engines, applying a harvest rate over a planning
horizon and converting to the unit the downstream consumer needs.

No volume is computed here: \code{\link{indicateur_p1_volume}} remains the single
owner of the IFN tarif. This function only converts and weights.
}
\section{Choosing \code{unite}}{

The two \code{foretaccess} consumers of \code{volume_champ} expect
\strong{opposite} semantics, and neither raises an error on the wrong one:
\itemize{
\item \code{calculer_flux()} splits the parcel volume across its source points and
accumulates it down the network — it expects a \strong{total in m3}
(\code{unite = "m3_total"});
\item \code{reseau_desserte()} / \code{optimiser_reseau()} rasterise the column
onto the grid, one value per cell — they expect a \strong{density in m3/ha}
(\code{unite = "m3_ha"}). A total there would be counted once per cell,
mechanically over-weighting large parcels.
}
}

\section{Harvest rate}{

\code{taux_prelevement} is a \strong{yearly flux} in m3/ha/year, not a fraction,
so \code{horizon_ans} is required with it:
\code{volume = P1 x taux x horizon_ans}. A harvest rate describes what
\strong{has been} removed, not what \strong{should} be; sizing a road network on
it assumes management carries on unchanged.

The species x sylvoecoregion table (spec 040, D4) is \strong{not implemented
yet}: \code{taux_prelevement = NULL} errors out rather than serve unsourced
figures.
}

\examples{
\dontrun{
units <- indicateur_p1_volume(units)

# For calculer_flux(): a total in m3 over a 10-year horizon.
units <- volume_mobilisable(units, taux_prelevement = 4, horizon_ans = 10)

# For reseau_desserte(): the same rate as a density.
units <- volume_mobilisable(units, unite = "m3_ha",
                            taux_prelevement = 4, horizon_ans = 10)
}
}
\seealso{
\code{\link{indicateur_p1_volume}} for the volume itself.
}
