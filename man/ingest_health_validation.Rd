% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_validation.R
\name{ingest_health_validation}
\alias{ingest_health_validation}
\title{Ingest a QField health-validation GPKG and update `alert` rows}
\usage{
ingest_health_validation(
  con,
  gpkg_path,
  zone_id,
  snap_distance_m = 50,
  validated_by = NULL,
  layer = "placettes"
)
}
\arguments{
\item{con}{A `DBIConnection` to a TimescaleDB instance.}

\item{gpkg_path}{Character. Path to the GPKG returned by the
field crew.}

\item{zone_id}{Integer. The monitoring zone whose alerts the
validation targets.}

\item{snap_distance_m}{Numeric. Maximum distance (metres) for
matching a plot to an alert. Default 50.}

\item{validated_by}{Character or `NULL`. Identity persisted on
the alert (typically the OAuth subject of the ingesting user
— defaults to the value present in `obs_by`, then to
`Sys.info()[["user"]]`).}

\item{layer}{Character. GPKG layer name. Default `"placettes"`.}
}
\value{
A list:
  * `n_updated` (int), `n_confirmed` (int),
    `n_false_positive` (int);
  * `n_unmatched` (int) — plots without an alert within
    `snap_distance_m`;
  * `n_skipped` (int) — plots with no `stade_deperissement`;
  * `details` — a data.frame with one row per processed plot.
}
\description{
Reads the `placettes` layer of `gpkg_path`, snaps each plot to
the nearest alert of the given `zone_id` (within
`snap_distance_m`), maps the observer-selected
`stade_deperissement` to `validation_status` /
`validation_cause`, and issues an `UPDATE alert` per match.
Plots without a `stade_deperissement` are skipped (never edited
in the field).
}
