% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rag.R
\name{retrieve_knowledge}
\alias{retrieve_knowledge}
\title{Retrieve the most relevant knowledge chunks for a query}
\usage{
retrieve_knowledge(
  con,
  query,
  top_k = 8L,
  family_codes = NULL,
  profile_codes = NULL,
  min_similarity = 0.7,
  lang = NULL,
  embed_provider = c("mistral", "openai", "voyage")
)
}
\arguments{
\item{con}{A `DBIConnection`. RAG schema must be enabled.}

\item{query}{Character scalar. Natural-language query.}

\item{top_k}{Integer. Maximum number of chunks to return. Default 8.}

\item{family_codes}{Optional character vector. Keep only documents
tagged with at least one of these family / indicator codes.}

\item{profile_codes}{Optional character vector. Keep only documents
tagged with at least one of these actor-profile codes.}

\item{min_similarity}{Numeric in `[0, 1]`. Drop chunks below this
cosine similarity. Default 0.7.}

\item{lang}{Optional ISO 639-1 code. Keep only documents in this
language.}

\item{embed_provider}{One of `"mistral"` (default), `"openai"`,
`"voyage"`. Must match the provider used at ingestion.}
}
\value{
A `data.frame` sorted by descending `similarity` with columns
  `chunk_id`, `document_id`, `title`, `author`, `pub_date`,
  `source_url`, `lang`, `chunk_index`, `page_number`, `text`,
  `similarity`. Zero rows (canonical empty frame) when nothing clears
  `min_similarity`.
}
\description{
Embeds `query` and returns the top-k chunks by cosine similarity,
optionally filtered by indicator family, actor profile, and language.
On PostgreSQL the ranking uses pgvector's `<=>` operator; on SQLite
it is computed in R.
}
\seealso{
[format_citations()] to render the result as a citation
  block.
}
