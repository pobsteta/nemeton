% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pixel_dieback_prep.R
\name{prepare_pixel_dieback_series}
\alias{prepare_pixel_dieback_series}
\title{Prepare the derived series for the pixel-dieback plot (CRswir + CRre)}
\usage{
prepare_pixel_dieback_series(
  df,
  grid_step = 10L,
  smooth = c("light", "none"),
  summer = c(152L, 273L),
  gap_flag_days = 45L
)
}
\arguments{
\item{df}{A \code{data.frame} from \code{\link{read_reconfort_pixel_series}}: columns
\code{obs_date} (Date), \code{crswir_obs}, \code{crre_obs}. A per-index validity is
derived as \code{!is.na()} (an \code{NA} marks a cloud-masked date).}

\item{grid_step}{Integer number of days of the regular grid.
Default \code{10L}.}

\item{smooth}{\code{"light"} (Savitzky-Golay, \code{p = 2}, \code{n = 5}) or \code{"none"}.
Default \code{"light"}. Strong smoothing is intentionally not offered: it
razes the summer extrema, which are the signal being tracked.}

\item{summer}{Integer length-2 day-of-year window \code{c(start, end)} of
the growing-season peak. Default \code{c(152L, 273L)} (1 Jun - 30 Sep).}

\item{gap_flag_days}{Long-gap threshold in days above which an
interpolated span is flagged in \code{gaps}. Default \code{45L}.}
}
\value{
A named list, each element a \code{data.frame}:
\describe{
  \item{grid_swir, grid_re}{\code{date}, \code{val}, \code{year}, \code{doy} — the
    regular grid, gap-filled and optionally smoothed, per index.}
  \item{obs_swir, obs_re}{\code{date}, \code{val}, \code{year}, \code{doy} — the raw
    valid observations per index (raw-points overlay).}
  \item{trough_swir}{\code{year}, \code{date}, \code{val} — annual summer CRswir
    minimum, on real observations.}
  \item{peak_re}{\code{year}, \code{date}, \code{val} — annual summer CRre maximum.}
  \item{state}{\code{date}, \code{year}, \code{val_sw}, \code{val_re} — summer dates where
    both indices are valid (state-space cloud).}
  \item{centroids}{\code{year}, \code{val_sw}, \code{val_re} — annual mean of
    \code{state} (state-space trajectory).}
  \item{gaps}{\code{from}, \code{to} — interpolated spans longer than
    \code{gap_flag_days}.}
}
The input attributes (\code{species}, \code{v_model}, \code{n_classes}, \code{date_from},
\code{date_to}, \code{dans_zone_validite}) are carried over onto the list.
}
\description{
Pure transform of a RECONFORT pixel series (from
\code{\link{read_reconfort_pixel_series}}) into the derived layers the app's
4-panel plotly needs: a regular gap-filled grid (optionally
light-smoothed), the raw valid observations, the annual summer extrema
(CRswir trough / CRre peak), the annual state-space centroids, and the
long-gap interpolated spans. No plotting, no Shiny — testable on its
own, so the app's rendering function stays free of business logic
(CLAUDE.md rule 3).
}
\examples{
df <- data.frame(
  obs_date   = as.Date("2023-01-01") + seq(0, 700, by = 15),
  crswir_obs = 0.8 - 0.1 * sin(seq(0, 700, by = 15) / 58),
  crre_obs   = 0.5 + 0.1 * sin(seq(0, 700, by = 15) / 58)
)
prep <- prepare_pixel_dieback_series(df)
names(prep)
}
\seealso{
\code{\link{read_reconfort_pixel_series}}
}
