% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/european_species_tolerances.R
\name{european_species_tolerances}
\alias{european_species_tolerances}
\title{European tree-species regeneration tolerances}
\usage{
european_species_tolerances(statut = NULL, confiance = NULL, type = NULL,
  include_invasif = TRUE)
}
\arguments{
\item{statut}{Optional character vector filter on \code{statut}. The
convenience value \code{"frm"} expands to both \code{"frm_1999"} and
\code{"frm_2025"} (the regulatory FRM species).}

\item{confiance}{Optional filter on \code{confidence}
(\code{"eleve"}/\code{"moyen"}/\code{"faible"}).}

\item{type}{Optional filter on \code{type} (\code{"conifere"}/\code{"feuillu"}).}

\item{include_invasif}{Logical; keep INTRO/INVASIVE taxa. Default \code{TRUE}.}
}
\value{
A data.frame with \code{code}, \code{species_sci}, \code{species_fr},
\code{type}, \code{species_class} (rule-based link to the 11
\code{\link{list_species_classes}}, for the UGF-presence flag of
\code{\link{regen_species_choices}}), \code{statut}, \code{tmax_tol_c},
\code{vpd_tol_kpa},
\code{drought_tol}, \code{shade_tol}, \code{waterlog_tol},
\code{frost_winter_min_c}, \code{frost_late}, \code{frost_early},
\code{air_humidity}, \code{thermophily}, \code{confidence}, \code{invasif},
\code{notes}.
}
\description{
Reference table of restocking tolerances for ~193 European tree species,
calibrated per species (spec 027). Complements the broad 11-class
\code{\link{regeneration_tolerances}} (kept for the UGF-class mapping) with the
same \code{tmax_tol_c}/\code{vpd_tol_kpa} axes plus richer autecology:
Niinemets & Valladares (2006) drought / shade / waterlogging tolerances (1–5),
winter cold and frost, air-humidity affinity and thermophily (1–9).
}
\details{
Three stacked scopes in \code{statut}: \code{"frm_1999"} (Directive
1999/105/EC Annex I in force, 47), \code{"frm_2025"} (additions of the
forthcoming FRM regulation, political agreement 2025-12-08, 17) and
\code{"atlas_jrc"} (European Atlas of Forest Tree Species dendroflora outside
the FRM list, ~130, \strong{rule-derived draft} — validate before operational
use). The \code{confidence} column (\code{"eleve"}/\code{"moyen"}/\code{"faible"})
grades reliability; \code{invasif} flags INTRO/INVASIVE taxa (listed for
completeness — \strong{presence is not a recommendation}).
}
\references{
European Atlas of Forest Tree Species (San-Miguel-Ayanz et al. 2016); Caudullo,
Welk & San-Miguel-Ayanz (2017); Niinemets & Valladares (2006); Directive
1999/105/EC. See \code{inst/REFERENCES.md}.
}
\seealso{
\code{\link{regeneration_tolerances}}, \code{\link{indice_priorite_regen}},
\code{\link{regen_species_choices}}
}
