% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_indicators_map}
\alias{plot_indicators_map}
\title{Create thematic maps for indicators}
\usage{
plot_indicators_map(
  data,
  indicators = NULL,
  palette = c("viridis", "RdYlGn", "YlOrRd", "Greens", "Blues"),
  direction = 1,
  title = NULL,
  legend_title = NULL,
  breaks = NULL,
  labels = NULL,
  alpha = 0.9,
  border_color = "white",
  border_size = 0.3,
  facet = TRUE,
  ncol = 2,
  base_size = 11
)
}
\arguments{
\item{data}{An \code{sf} object with indicator values}

\item{indicators}{Character vector of indicator column names to plot.
If NULL and multiple indicators present, creates faceted plot.}

\item{palette}{Character. Color palette to use. Options:
\itemize{
  \item "viridis" - Perceptually uniform, colorblind-friendly (default)
  \item "RdYlGn" - Red-Yellow-Green diverging (low-medium-high)
  \item "YlOrRd" - Yellow-Orange-Red sequential
  \item "Greens" - Green sequential
  \item "Blues" - Blue sequential
}}

\item{direction}{Numeric. Direction of color scale: 1 (default) or -1 (reversed)}

\item{title}{Character. Plot title. If NULL, auto-generated.}

\item{legend_title}{Character. Legend title. If NULL, uses "Value" or indicator name.}

\item{breaks}{Numeric vector. Manual breaks for color scale. If NULL, automatic.}

\item{labels}{Character vector. Labels for breaks. Same length as breaks.}

\item{alpha}{Numeric. Transparency (0-1). Default 0.9.}

\item{border_color}{Character. Border color for polygons. Default "white".}

\item{border_size}{Numeric. Border line width. Default 0.3.}

\item{facet}{Logical. Create faceted plot for multiple indicators? Default TRUE.}

\item{ncol}{Integer. Number of columns for faceted plot. Default 2.}

\item{base_size}{Numeric. Base font size for theme. Default 11.}
}
\value{
A \code{ggplot} object
}
\description{
Generates publication-ready maps visualizing indicator values across spatial units.
}
\details{
Creates thematic choropleth maps using ggplot2. Supports:
\itemize{
  \item Single indicator maps
  \item Multi-indicator faceted maps
  \item Custom color palettes
  \item Flexible styling
}

The function uses \code{geom_sf()} for spatial rendering and applies
perceptually uniform color scales by default (viridis).
}
\examples{
\dontrun{
# Single indicator map
plot_indicators_map(
  results,
  indicators = "carbon",
  palette = "Greens",
  title = "Carbon Stock Distribution"
)

# Multiple indicators (faceted)
plot_indicators_map(
  normalized,
  indicators = c("carbon_norm", "biodiversity_norm", "water_norm"),
  palette = "viridis",
  facet = TRUE,
  ncol = 3
)

# Composite index with custom breaks
plot_indicators_map(
  results,
  indicators = "ecosystem_health",
  palette = "RdYlGn",
  breaks = c(0, 25, 50, 75, 100),
  labels = c("Low", "Medium-Low", "Medium-High", "High", "Very High")
)
}

}
