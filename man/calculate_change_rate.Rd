% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{calculate_change_rate}
\alias{calculate_change_rate}
\title{Calculate Change Rates Between Periods}
\usage{
calculate_change_rate(
  temporal,
  indicators = "all",
  period_start = NULL,
  period_end = NULL,
  type = c("both", "absolute", "relative")
)
}
\arguments{
\item{temporal}{A nemeton_temporal object created by \code{\link{nemeton_temporal}}.}

\item{indicators}{Character vector of indicator names to analyze.
Default "all" uses all indicators present in the temporal dataset.}

\item{period_start}{Character. Label of starting period. Default uses first period.}

\item{period_end}{Character. Label of ending period. Default uses last period.}

\item{type}{Character. Type of change rate: "absolute", "relative", or "both".
Default "both".}
}
\value{
A nemeton_units sf object with added columns:
  \describe{
    \item{<indicator>_rate_abs}{Absolute change per year (e.g., tC/ha/year)}
    \item{<indicator>_rate_rel}{Relative change per year (\%/year)}
  }
}
\description{
Computes annual change rates (absolute and relative) for indicators
across temporal periods.
}
\examples{
\dontrun{
# Calculate carbon change rates
rates <- calculate_change_rate(
  temporal,
  indicators = c("C1", "W3"),
  type = "both"
)

# View change rates
summary(rates[, c("C1_rate_abs", "C1_rate_rel")])
}
}
