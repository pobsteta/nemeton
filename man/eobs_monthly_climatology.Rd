% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eobs_click_series.R
\name{eobs_monthly_climatology}
\alias{eobs_monthly_climatology}
\title{Monthly E-OBS climatology at a point (month -> value)}
\usage{
eobs_monthly_climatology(daily, point, var, years = NULL)
}
\arguments{
\item{daily}{A daily E-OBS \code{SpatRaster} (full year, \code{terra::time} set), or a
path to the cached daily netCDF.}

\item{point}{An \code{sf}/\code{sfc} POINT, or \code{c(lon, lat)} in EPSG:4326.}

\item{var}{\code{"rr"} (monthly precipitation sum, mm) or \code{"tg"}/\code{"tx"} (monthly
mean temperature, °C). Drives the within-month reducer.}

\item{years}{Optional integer years to average over (default: all present).}
}
\value{
A \code{data.frame(month = 1:12, value)} (all twelve months, \code{NA} where a
month has no data). Attributes: \code{var}, \code{unit}, \code{reducer}.
}
\description{
Twelve-month climatology (averaged over \code{years}) of an E-OBS variable at a
location, for the ombrothermic (Gaussen-Bagnouls) diagram — chart 4 of the
regional-context click panel (spec 036). Precipitation is summed within each
month then averaged across years (mm/month); temperature is the monthly mean
(°C). Reads the full-year daily field (a \code{SpatRaster} with \code{terra::time} set,
or a cached netCDF path), so tx/rr need no new acquisition; the Gaussen diagram
proper wants mean temperature (\code{tg}), which has its own netCDF (spec 036 §5.4).
}
\examples{
\dontrun{
eobs_monthly_climatology("eobs_tg_daily.nc", c(6.1, 48.7), var = "tg")
}
}
\seealso{
\code{\link{eobs_summer_series}}, \code{\link{load_eobs_source}}
}
