% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eobs_click_series.R
\name{eobs_trend_fit}
\alias{eobs_trend_fit}
\title{Linear trend of an E-OBS summer series (per decade)}
\usage{
eobs_trend_fit(series)
}
\arguments{
\item{series}{A \code{data.frame} with numeric columns \code{year} and \code{value} (as
returned by \code{\link{eobs_summer_series}}).}
}
\value{
A \code{list(slope_decade, intercept, r2, p_value, n)}. All \code{NA} (with
\code{n} the count of finite pairs) when fewer than two finite points are
available.
}
\description{
Fit the least-squares trend of a per-year summer series (the output of
\code{\link{eobs_summer_series}}) and return the per-decade slope with its goodness of
fit, for the trend line of chart 1 (spec 036). The slope is \code{10 *} the
per-year OLS slope, matching the mapped per-decade slope
(\code{tendances_estivales_eobs()} / \code{eobs_downscale()}), so the click chart and the
map colour agree by construction.
}
\examples{
eobs_trend_fit(data.frame(year = 2011:2020, value = 20 + (0:9) * 0.1))
}
\seealso{
\code{\link{eobs_summer_series}}
}
