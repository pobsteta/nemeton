% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitoring-zones.R
\name{find_zones_by_project}
\alias{find_zones_by_project}
\title{List the monitoring zones bound to a project UUID}
\usage{
find_zones_by_project(con, project_uuid)
}
\arguments{
\item{con}{A `DBIConnection` returned by [db_connect()].}

\item{project_uuid}{Non-empty character scalar. Opaque project id.}
}
\value{
A `data.frame` with columns `id` (integer) and `name`
  (character), ordered by `name`; zero rows when no zone matches.
}
\description{
Returns every `monitoring_zone` row carrying `project_uuid`, ordered by
name. Since spec 020 a project may own several zones
(`<project>_tot/_feu/_res/_mix`), so prefer this over
[find_zone_by_project()] (which returns only the first id).
}
\seealso{
[find_zone_by_project()], [build_project_monitoring_zones()].
}
