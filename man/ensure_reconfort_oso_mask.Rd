% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconfort_mask.R
\name{ensure_reconfort_oso_mask}
\alias{ensure_reconfort_oso_mask}
\title{Fetch (and cache) the RECONFORT deciduous (OSO) binary mask}
\usage{
ensure_reconfort_oso_mask(
  cache_dir = NULL,
  local_path = NULL,
  url = NULL,
  force = FALSE,
  verify = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{cache_dir}{Cache directory. Default a per-user nemeton cache.}

\item{local_path}{Optional path to a mask already on disk (e.g. a
custom broadleaf mask, or `<reconfort_clone>/masks/...`). When set,
no download happens and (for a custom mask) verification is skipped
if `verify = FALSE`.}

\item{url}{Optional explicit download URL overriding the registry
default (from `options(nemeton.reconfort_mask_base_url)`).}

\item{force}{Re-download even if a valid cached copy exists.}

\item{verify}{Verify size + MD5 against \code{\link{RECONFORT_OSO_MASK}}.
Default `TRUE`. Pass `FALSE` when supplying a custom `local_path`.}

\item{quiet}{Suppress progress messages. Default `FALSE`.}
}
\value{
The path to the local mask file.
}
\description{
Ensures a local copy of the default OSO 2021 deciduous mask used to
restrict RECONFORT scoring to broadleaf pixels, and returns its
path. Resolution order mirrors \code{\link{ensure_reconfort_model}}:
\enumerate{
  \item if `local_path` is given, that file is used directly
    (verified unless `verify = FALSE`);
  \item else, a verified cached copy is returned without download;
  \item else the mask is downloaded, verified and cached.
}
}
\details{
The mask is ~54 MB; the first fetch is slow. The cache lives under
`cache_dir` (default `file.path(get_global_cache_dir(),
"reconfort_masks")`).
}
