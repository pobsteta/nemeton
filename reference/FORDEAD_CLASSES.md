# Canonical ordered FORDEAD anomaly classes

Vector of length 5 ordered from healthy to bare soil. Index \`i\`
(1-based) is the class corresponding to the integer code \`i - 1\` in
\`state.tif\`.

## Usage

``` r
FORDEAD_CLASSES
```
